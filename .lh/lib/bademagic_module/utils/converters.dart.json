{
    "sourceFile": "lib/bademagic_module/utils/converters.dart",
    "activeCommit": 0,
    "commits": [
        {
            "activePatchIndex": 107,
            "patches": [
                {
                    "date": 1739986755872,
                    "content": "Index: \n===================================================================\n--- \n+++ \n"
                },
                {
                    "date": 1739986774210,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -110,9 +110,9 @@\n           ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = 0 - textPainter.height / 3.8;\r\n+        double offsetY = centerY - textPainter.height / 3.8;\r\n         textPainter.paint(canvas, Offset(currentX, offsetY));\r\n         currentX += textPainter.width;\r\n       } else {\r\n         // Paint emoji\r\n"
                },
                {
                    "date": 1739986787281,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -96,9 +96,9 @@\n \r\n     double currentX = 0;\r\n     double centerY = height / 2.0;\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize*0.734; \r\n+    double emojiWidth = fontSize; \r\n \r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         // Paint text\r\n@@ -110,9 +110,9 @@\n           ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 3.8;\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n         textPainter.paint(canvas, Offset(currentX, offsetY));\r\n         currentX += textPainter.width;\r\n       } else {\r\n         // Paint emoji\r\n"
                },
                {
                    "date": 1739986835385,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -20,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -51,130 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message for emoji placeholders\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), \r\n-          isEmoji: false\r\n-        ));\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n+\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+  }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize; \r\n-\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -266,10 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739986992660,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -64,161 +64,151 @@\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n   /// The canvas width is set to the total rendered message width plus extra (display width),\r\n   /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message into segments (plain text and emoji)\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Determine the scaled font size and set emoji dimensions.\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n+    if (scaledFontSize > canvasHeight * 0.9) {\r\n+      scaledFontSize = canvasHeight * 0.9;\r\n+    }\r\n+    double emojiWidth = scaledFontSize;\r\n+    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    // Calculate the total width needed for the message\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (segment.isEmoji) {\r\n+        totalWidth += emojiWidth + emojiGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        totalWidth += tp.width;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Adjust canvas width to fit the message with some padding\r\n+    int canvasWidth = max(cols * scale, totalWidth.ceil() + (scale * 2));\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Center the text vertically\r\n+    double centerY = canvasHeight / 2;\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    // Fill the background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n+\r\n+    // Draw the message\r\n+    double currentX = (canvasWidth - totalWidth) / 2; // Center horizontally\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+          textDirection: TextDirection.ltr,\r\n+          maxLines: 1,\r\n+          textWidthBasis: TextWidthBasis.parent,\r\n+        );\r\n+        tp.layout(maxWidth: canvasWidth.toDouble());\r\n+        var metrics = tp.computeLineMetrics();\r\n+        double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+        double offsetY = centerY - (tp.height - descent) / 2;\r\n+        tp.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += tp.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          }\r\n         }\r\n+        currentX += emojiWidth;\r\n+        if (segments.indexOf(segment) < segments.length - 1 && segments[segments.indexOf(segment) + 1].isEmoji) {\r\n+          currentX += emojiGap;\r\n+        }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Convert canvas to image for downsampling\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n+            }\r\n           }\r\n         }\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.5); // Adjust threshold for better visibility\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1739987007048,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -64,151 +64,161 @@\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n   /// The canvas width is set to the total rendered message width plus extra (display width),\r\n   /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message into segments (plain text and emoji)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Determine the scaled font size and set emoji dimensions.\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n-    if (scaledFontSize > canvasHeight * 0.9) {\r\n-      scaledFontSize = canvasHeight * 0.9;\r\n-    }\r\n-    double emojiWidth = scaledFontSize;\r\n-    double emojiGap = scaledFontSize * 0.2;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Calculate the total width needed for the message\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (segment.isEmoji) {\r\n-        totalWidth += emojiWidth + emojiGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        totalWidth += tp.width;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    // Adjust canvas width to fit the message with some padding\r\n-    int canvasWidth = max(cols * scale, totalWidth.ceil() + (scale * 2));\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // Center the text vertically\r\n-    double centerY = canvasHeight / 2;\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    // Fill the background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n-\r\n-    // Draw the message\r\n-    double currentX = (canvasWidth - totalWidth) / 2; // Center horizontally\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-          textDirection: TextDirection.ltr,\r\n-          maxLines: 1,\r\n-          textWidthBasis: TextWidthBasis.parent,\r\n-        );\r\n-        tp.layout(maxWidth: canvasWidth.toDouble());\r\n-        var metrics = tp.computeLineMetrics();\r\n-        double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-        double offsetY = centerY - (tp.height - descent) / 2;\r\n-        tp.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += tp.width;\r\n-      } else {\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          }\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n-        currentX += emojiWidth;\r\n-        if (segments.indexOf(segment) < segments.length - 1 && segments[segments.indexOf(segment) + 1].isEmoji) {\r\n-          currentX += emojiGap;\r\n-        }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.5); // Adjust threshold for better visibility\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1739987565292,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -59,166 +59,175 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+  }) async {\r\n+    // Parse the message into segments (plain text and emoji)\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Determine the scaled font size and set emoji dimensions.\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    // Adjust font size based on actual text height\r\n+    final dummyText = 'Ay';\r\n+    var dummyStyle = textStyle.copyWith(fontSize: scaledFontSize);\r\n+    TextPainter testPainter = TextPainter(\r\n+      text: TextSpan(text: dummyText, style: dummyStyle),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    testPainter.layout();\r\n+    double totalTextHeight = testPainter.height;\r\n+    if (totalTextHeight > canvasHeight * 0.9) {\r\n+      scaledFontSize *= (canvasHeight * 0.9) / totalTextHeight;\r\n+      dummyStyle = dummyStyle.copyWith(fontSize: scaledFontSize);\r\n+      testPainter.text = TextSpan(text: dummyText, style: dummyStyle);\r\n+      testPainter.layout();\r\n+      totalTextHeight = testPainter.height;\r\n+    }\r\n+\r\n+    double emojiWidth = scaledFontSize; // Emojis are square\r\n+    double emojiGap = scaledFontSize * 0.2; // Gap between consecutive emojis\r\n+\r\n+    // Measure one copy of the message (in scaled pixels)\r\n+    double singleCopyWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        singleCopyWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segments[i].text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        singleCopyWidth += tp.width;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Canvas dimensions\r\n+    int fixedWidth = cols * scale;\r\n+    int gapWidth = fixedWidth;\r\n+    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+    double centerY = canvasHeight / 2;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    // Fill the background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    // Draw one copy of the message starting at x=0\r\n+    double currentX = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      final segment = segments[i];\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        var metrics = tp.computeLineMetrics();\r\n+        if (metrics.isNotEmpty) {\r\n+          double ascent = metrics.first.ascent.abs();\r\n+          double descent = metrics.first.descent;\r\n+          double offsetY = centerY - (ascent - descent) / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n         }\r\n+        currentX += tp.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double emojiOffsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, emojiOffsetY), imagePaint);\r\n+          }\r\n+        }\r\n+        currentX += emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          currentX += emojiGap;\r\n+        }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Finalize the drawing and convert the result to an image.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n+            }\r\n           }\r\n         }\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1739987611045,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -59,175 +59,166 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-  }) async {\r\n-    // Parse the message into segments (plain text and emoji)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Determine the scaled font size and set emoji dimensions.\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Adjust font size based on actual text height\r\n-    final dummyText = 'Ay';\r\n-    var dummyStyle = textStyle.copyWith(fontSize: scaledFontSize);\r\n-    TextPainter testPainter = TextPainter(\r\n-      text: TextSpan(text: dummyText, style: dummyStyle),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    testPainter.layout();\r\n-    double totalTextHeight = testPainter.height;\r\n-    if (totalTextHeight > canvasHeight * 0.9) {\r\n-      scaledFontSize *= (canvasHeight * 0.9) / totalTextHeight;\r\n-      dummyStyle = dummyStyle.copyWith(fontSize: scaledFontSize);\r\n-      testPainter.text = TextSpan(text: dummyText, style: dummyStyle);\r\n-      testPainter.layout();\r\n-      totalTextHeight = testPainter.height;\r\n-    }\r\n-\r\n-    double emojiWidth = scaledFontSize; // Emojis are square\r\n-    double emojiGap = scaledFontSize * 0.2; // Gap between consecutive emojis\r\n-\r\n-    // Measure one copy of the message (in scaled pixels)\r\n-    double singleCopyWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        singleCopyWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segments[i].text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        singleCopyWidth += tp.width;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    // Canvas dimensions\r\n-    int fixedWidth = cols * scale;\r\n-    int gapWidth = fixedWidth;\r\n-    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-    double centerY = canvasHeight / 2;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill the background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    // Draw one copy of the message starting at x=0\r\n-    double currentX = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      final segment = segments[i];\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        var metrics = tp.computeLineMetrics();\r\n-        if (metrics.isNotEmpty) {\r\n-          double ascent = metrics.first.ascent.abs();\r\n-          double descent = metrics.first.descent;\r\n-          double offsetY = centerY - (ascent - descent) / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n-        currentX += tp.width;\r\n-      } else {\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double emojiOffsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, emojiOffsetY), imagePaint);\r\n-          }\r\n-        }\r\n-        currentX += emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          currentX += emojiGap;\r\n-        }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Finalize the drawing and convert the result to an image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1739987721919,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,316 +1,519 @@\n import 'dart:math';\r\n+\r\n import 'dart:typed_data';\r\n+\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n+\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n+\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n+\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n+\r\n import 'package:flutter/material.dart';\r\n+\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+class Converters {\r\n \r\n-class Converters {\r\n   InlineImageProvider controllerData =\r\n+\r\n       GetIt.instance.get<InlineImageProvider>();\r\n+\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+\r\n   ImageUtils imageUtils = ImageUtils();\r\n+\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n+\r\n     List<String> hexStrings = [];\r\n+\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n+\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n+\r\n         if (key is List) {\r\n+\r\n           String filename = key[0];\r\n+\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n+\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+\r\n           List<List<int>> imageData =\r\n+\r\n               image!.map((list) => list.cast<int>()).toList();\r\n+\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n+\r\n           x += 5;\r\n+\r\n         } else {\r\n+\r\n           List<String> hs =\r\n+\r\n               await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+\r\n           hexStrings.addAll(hs);\r\n+\r\n           x += 5;\r\n+\r\n         }\r\n+\r\n       } else {\r\n+\r\n         if (converter.charCodes.containsKey(message[x])) {\r\n+\r\n           hexStrings.add(converter.charCodes[message[x]]!);\r\n+\r\n         }\r\n+\r\n       }\r\n+\r\n     }\r\n+\r\n     if (isInverted) {\r\n+\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n+\r\n       hexStrings = padHexString(hexStrings);\r\n+\r\n     }\r\n+\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n+\r\n     return hexStrings;\r\n-  }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n   }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+\r\n+    String message,\r\n+\r\n+    TextStyle textStyle, {\r\n+\r\n+    int cols = 44,\r\n+\r\n+    int rows = 11,\r\n+\r\n+    int scale = 10, // scale factor for better resolution\r\n+\r\n+  }) async {\r\n+\r\n+    // Parse the message for emoji placeholders\r\n+\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+\r\n+    List<_Segment> segments = [];\r\n+\r\n+    int lastMatchEnd = 0;\r\n+\r\n+    for (final match in regex.allMatches(message)) {\r\n+\r\n+      if (match.start > lastMatchEnd) {\r\n+\r\n+        segments.add(_Segment(\r\n+\r\n+          text: message.substring(lastMatchEnd, match.start), \r\n+\r\n+          isEmoji: false\r\n+\r\n+        ));\r\n+\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n+\r\n+      String emojiIndex = match.group(1)!;\r\n+\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+\r\n+      lastMatchEnd = match.end;\r\n+\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    if (lastMatchEnd < message.length) {\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    }\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    // Calculate canvas size\r\n+\r\n+    final int width = cols * scale;\r\n+\r\n+    final int height = rows * scale;\r\n+\r\n+    // Create a PictureRecorder and Canvas\r\n+\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+\r\n+    final Canvas canvas = Canvas(\r\n+\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+\r\n+    // Fill background with white\r\n+\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+\r\n+    canvas.drawRect(\r\n+\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+\r\n+    double currentX = 0;\r\n+\r\n+    double centerY = height / 2.0;\r\n+\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+\r\n+    double emojiWidth = fontSize*0.734; \r\n+\r\n+    for (var segment in segments) {\r\n+\r\n+      if (!segment.isEmoji) {\r\n+\r\n+        // Paint text\r\n+\r\n+        final TextPainter textPainter = TextPainter(\r\n+\r\n+          text: TextSpan(\r\n+\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+\r\n+            style: textStyle.copyWith(\r\n+\r\n+                color: Colors.black, fontSize: fontSize),\r\n+\r\n+          ),\r\n+\r\n+          textDirection: TextDirection.ltr,\r\n+\r\n+        );\r\n+\r\n+        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n+\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+\r\n+        currentX += textPainter.width;\r\n+\r\n+      } else {\r\n+\r\n+        // Paint emoji\r\n+\r\n+        int index = int.parse(segment.text);\r\n+\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+\r\n+        if (index < keys.length) {\r\n+\r\n+          var key = keys[index];\r\n+\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+\r\n+          if (emojiBytes != null) {\r\n+\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+\r\n+            ui.Image emojiImage = fi.image;\r\n+\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+\r\n+            Paint imagePaint = Paint();\r\n+\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+\r\n+            currentX += emojiWidth;\r\n+\r\n+          }\r\n+\r\n         }\r\n+\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n+\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Convert canvas to image for downsampling\r\n+\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+\r\n+    final ByteData? byteData =\r\n+\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+\r\n+    if (byteData == null) {\r\n+\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+\r\n+    }\r\n+\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+\r\n+    List<List<bool>> matrix =\r\n+\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+\r\n+    for (int row = 0; row < rows; row++) {\r\n+\r\n+      for (int col = 0; col < cols; col++) {\r\n+\r\n+        int sum = 0;\r\n+\r\n+        int count = 0;\r\n+\r\n+        for (int y = 0; y < scale; y++) {\r\n+\r\n+          for (int x = 0; x < scale; x++) {\r\n+\r\n+            int pixelX = col * scale + x;\r\n+\r\n+            int pixelY = row * scale + y;\r\n+\r\n+            int index =\r\n+\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+\r\n+            if (index + 3 < data.length) {\r\n+\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+\r\n+              sum += brightness;\r\n+\r\n+              count++;\r\n+\r\n+            }\r\n+\r\n           }\r\n+\r\n         }\r\n+\r\n+        double avgBrightness = sum / count;\r\n+\r\n+        matrix[row][col] = avgBrightness < 128;\r\n+\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n+\r\n     }\r\n+\r\n+    return matrix;\r\n+\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n+  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n \r\n-  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n+\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n+\r\n     int finalSum = 0;\r\n+\r\n     for (int j = 0; j < width; j++) {\r\n+\r\n       int sum = 0;\r\n+\r\n       for (int i = 0; i < height; i++) {\r\n+\r\n         sum += image[i][j];\r\n+\r\n       }\r\n+\r\n       if (sum == 0 && trim) {\r\n+\r\n         for (int i = 0; i < height; i++) {\r\n+\r\n           image[i][j] = -1;\r\n+\r\n         }\r\n+\r\n       } else {\r\n+\r\n         finalSum += j;\r\n+\r\n         break;\r\n+\r\n       }\r\n+\r\n     }\r\n+\r\n     for (int j = width - 1; j >= 0; j--) {\r\n+\r\n       int sum = 0;\r\n+\r\n       for (int i = 0; i < height; i++) {\r\n+\r\n         sum += image[i][j];\r\n+\r\n       }\r\n+\r\n       if (sum == 0 && trim) {\r\n+\r\n         for (int i = 0; i < height; i++) {\r\n+\r\n           image[i][j] = -1;\r\n+\r\n         }\r\n+\r\n       } else {\r\n+\r\n         finalSum += (height - j - 1);\r\n+\r\n         break;\r\n+\r\n       }\r\n+\r\n     }\r\n+\r\n     int diff = 0;\r\n+\r\n     if ((height - finalSum) % 8 > 0) {\r\n+\r\n       diff = 8 - (height - finalSum) % 8;\r\n+\r\n     }\r\n+\r\n     int rOff = (diff / 2).floor();\r\n+\r\n     int lOff = (diff / 2).ceil();\r\n+\r\n     List<List<int>> list =\r\n+\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+\r\n     for (int i = 0; i < height; i++) {\r\n+\r\n       int k = 0;\r\n+\r\n       for (int j = 0; j < rOff; j++) {\r\n+\r\n         list[i][k++] = 0;\r\n+\r\n       }\r\n+\r\n       for (int j = 0; j < width; j++) {\r\n+\r\n         if (image[i][j] != -1) {\r\n+\r\n           list[i][k++] = image[i][j];\r\n+\r\n         }\r\n+\r\n       }\r\n+\r\n       for (int j = 0; j < lOff; j++) {\r\n+\r\n         list[i][k++] = 0;\r\n+\r\n       }\r\n+\r\n     }\r\n+\r\n     logger.d(\"Padded image: $list\");\r\n+\r\n     List<String> allHexs = [];\r\n+\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+\r\n       StringBuffer lineHex = StringBuffer();\r\n+\r\n       for (int k = 0; k < height; k++) {\r\n+\r\n         StringBuffer stBuilder = StringBuffer();\r\n+\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n+\r\n           stBuilder.write(list[k][j]);\r\n+\r\n         }\r\n+\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n+\r\n             .toRadixString(16)\r\n+\r\n             .padLeft(2, '0');\r\n+\r\n         lineHex.write(hex);\r\n+\r\n       }\r\n+\r\n       allHexs.add(lineHex.toString());\r\n+\r\n     }\r\n+\r\n     return allHexs;\r\n+\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n+\r\n     StringBuffer invertedHex = StringBuffer();\r\n+\r\n     for (int i = 0; i < hex.length; i++) {\r\n+\r\n       String invertedHexDigit =\r\n+\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+\r\n       invertedHex.write(invertedHexDigit);\r\n+\r\n     }\r\n+\r\n     return invertedHex.toString();\r\n+\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n+\r\n     List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n+\r\n     }).toList();\r\n+\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n+\r\n       hexArray[i].insert(0, 1);\r\n+\r\n       hexArray[i].add(1);\r\n+\r\n     }\r\n+\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n+\r\n   }\r\n+\r\n+}\r\n+\r\n+class _Segment {\r\n+\r\n+  final String text;\r\n+\r\n+  final bool isEmoji;\r\n+\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739987820996,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,519 +1,316 @@\n import 'dart:math';\r\n-\r\n import 'dart:typed_data';\r\n-\r\n import 'dart:ui' as ui;\r\n-\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n-\r\n import 'package:flutter/material.dart';\r\n-\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n-\r\n   InlineImageProvider controllerData =\r\n-\r\n       GetIt.instance.get<InlineImageProvider>();\r\n-\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-\r\n   ImageUtils imageUtils = ImageUtils();\r\n-\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n-\r\n     List<String> hexStrings = [];\r\n-\r\n     for (int x = 0; x < message.length; x++) {\r\n-\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n-\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n-\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n-\r\n         if (key is List) {\r\n-\r\n           String filename = key[0];\r\n-\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-\r\n           List<List<int>> imageData =\r\n-\r\n               image!.map((list) => list.cast<int>()).toList();\r\n-\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n-\r\n           x += 5;\r\n-\r\n         } else {\r\n-\r\n           List<String> hs =\r\n-\r\n               await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n-\r\n           hexStrings.addAll(hs);\r\n-\r\n           x += 5;\r\n-\r\n         }\r\n-\r\n       } else {\r\n-\r\n         if (converter.charCodes.containsKey(message[x])) {\r\n-\r\n           hexStrings.add(converter.charCodes[message[x]]!);\r\n-\r\n         }\r\n-\r\n       }\r\n-\r\n     }\r\n-\r\n     if (isInverted) {\r\n-\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n-\r\n       hexStrings = padHexString(hexStrings);\r\n-\r\n     }\r\n-\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n-\r\n     return hexStrings;\r\n+  }\r\n \r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n   }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-\r\n-    String message,\r\n-\r\n-    TextStyle textStyle, {\r\n-\r\n-    int cols = 44,\r\n-\r\n-    int rows = 11,\r\n-\r\n-    int scale = 10, // scale factor for better resolution\r\n-\r\n-  }) async {\r\n-\r\n-    // Parse the message for emoji placeholders\r\n-\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-\r\n-    List<_Segment> segments = [];\r\n-\r\n-    int lastMatchEnd = 0;\r\n-\r\n-    for (final match in regex.allMatches(message)) {\r\n-\r\n-      if (match.start > lastMatchEnd) {\r\n-\r\n-        segments.add(_Segment(\r\n-\r\n-          text: message.substring(lastMatchEnd, match.start), \r\n-\r\n-          isEmoji: false\r\n-\r\n-        ));\r\n-\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n-\r\n-      String emojiIndex = match.group(1)!;\r\n-\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-\r\n-      lastMatchEnd = match.end;\r\n-\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    if (lastMatchEnd < message.length) {\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    }\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    // Calculate canvas size\r\n-\r\n-    final int width = cols * scale;\r\n-\r\n-    final int height = rows * scale;\r\n-\r\n-    // Create a PictureRecorder and Canvas\r\n-\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-\r\n-    final Canvas canvas = Canvas(\r\n-\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n-\r\n-    // Fill background with white\r\n-\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-\r\n-    canvas.drawRect(\r\n-\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n-\r\n-    double currentX = 0;\r\n-\r\n-    double centerY = height / 2.0;\r\n-\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-\r\n-    double emojiWidth = fontSize*0.734; \r\n-\r\n-    for (var segment in segments) {\r\n-\r\n-      if (!segment.isEmoji) {\r\n-\r\n-        // Paint text\r\n-\r\n-        final TextPainter textPainter = TextPainter(\r\n-\r\n-          text: TextSpan(\r\n-\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-\r\n-            style: textStyle.copyWith(\r\n-\r\n-                color: Colors.black, fontSize: fontSize),\r\n-\r\n-          ),\r\n-\r\n-          textDirection: TextDirection.ltr,\r\n-\r\n-        );\r\n-\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-\r\n-        currentX += textPainter.width;\r\n-\r\n-      } else {\r\n-\r\n-        // Paint emoji\r\n-\r\n-        int index = int.parse(segment.text);\r\n-\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-\r\n-        if (index < keys.length) {\r\n-\r\n-          var key = keys[index];\r\n-\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-\r\n-          if (emojiBytes != null) {\r\n-\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-\r\n-            ui.Image emojiImage = fi.image;\r\n-\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-\r\n-            Paint imagePaint = Paint();\r\n-\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-\r\n-            currentX += emojiWidth;\r\n-\r\n-          }\r\n-\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n-\r\n       }\r\n-\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Convert canvas to image for downsampling\r\n-\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-\r\n-    final ByteData? byteData =\r\n-\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-\r\n-    if (byteData == null) {\r\n-\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-\r\n-    }\r\n-\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-\r\n-    List<List<bool>> matrix =\r\n-\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-\r\n-    for (int row = 0; row < rows; row++) {\r\n-\r\n-      for (int col = 0; col < cols; col++) {\r\n-\r\n-        int sum = 0;\r\n-\r\n-        int count = 0;\r\n-\r\n-        for (int y = 0; y < scale; y++) {\r\n-\r\n-          for (int x = 0; x < scale; x++) {\r\n-\r\n-            int pixelX = col * scale + x;\r\n-\r\n-            int pixelY = row * scale + y;\r\n-\r\n-            int index =\r\n-\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-\r\n-            if (index + 3 < data.length) {\r\n-\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-\r\n-              sum += brightness;\r\n-\r\n-              count++;\r\n-\r\n-            }\r\n-\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n-\r\n         }\r\n-\r\n-        double avgBrightness = sum / count;\r\n-\r\n-        matrix[row][col] = avgBrightness < 128;\r\n-\r\n       }\r\n-\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+  }\r\n+  return matrix;\r\n+}\r\n \r\n-    return matrix;\r\n \r\n-  }\r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n-\r\n     int height = image.length;\r\n-\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n-\r\n     int finalSum = 0;\r\n-\r\n     for (int j = 0; j < width; j++) {\r\n-\r\n       int sum = 0;\r\n-\r\n       for (int i = 0; i < height; i++) {\r\n-\r\n         sum += image[i][j];\r\n-\r\n       }\r\n-\r\n       if (sum == 0 && trim) {\r\n-\r\n         for (int i = 0; i < height; i++) {\r\n-\r\n           image[i][j] = -1;\r\n-\r\n         }\r\n-\r\n       } else {\r\n-\r\n         finalSum += j;\r\n-\r\n         break;\r\n-\r\n       }\r\n-\r\n     }\r\n-\r\n     for (int j = width - 1; j >= 0; j--) {\r\n-\r\n       int sum = 0;\r\n-\r\n       for (int i = 0; i < height; i++) {\r\n-\r\n         sum += image[i][j];\r\n-\r\n       }\r\n-\r\n       if (sum == 0 && trim) {\r\n-\r\n         for (int i = 0; i < height; i++) {\r\n-\r\n           image[i][j] = -1;\r\n-\r\n         }\r\n-\r\n       } else {\r\n-\r\n         finalSum += (height - j - 1);\r\n-\r\n         break;\r\n-\r\n       }\r\n-\r\n     }\r\n-\r\n     int diff = 0;\r\n-\r\n     if ((height - finalSum) % 8 > 0) {\r\n-\r\n       diff = 8 - (height - finalSum) % 8;\r\n-\r\n     }\r\n-\r\n     int rOff = (diff / 2).floor();\r\n-\r\n     int lOff = (diff / 2).ceil();\r\n-\r\n     List<List<int>> list =\r\n-\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n-\r\n     for (int i = 0; i < height; i++) {\r\n-\r\n       int k = 0;\r\n-\r\n       for (int j = 0; j < rOff; j++) {\r\n-\r\n         list[i][k++] = 0;\r\n-\r\n       }\r\n-\r\n       for (int j = 0; j < width; j++) {\r\n-\r\n         if (image[i][j] != -1) {\r\n-\r\n           list[i][k++] = image[i][j];\r\n-\r\n         }\r\n-\r\n       }\r\n-\r\n       for (int j = 0; j < lOff; j++) {\r\n-\r\n         list[i][k++] = 0;\r\n-\r\n       }\r\n-\r\n     }\r\n-\r\n     logger.d(\"Padded image: $list\");\r\n-\r\n     List<String> allHexs = [];\r\n-\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n-\r\n       StringBuffer lineHex = StringBuffer();\r\n-\r\n       for (int k = 0; k < height; k++) {\r\n-\r\n         StringBuffer stBuilder = StringBuffer();\r\n-\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-\r\n           stBuilder.write(list[k][j]);\r\n-\r\n         }\r\n-\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n-\r\n             .toRadixString(16)\r\n-\r\n             .padLeft(2, '0');\r\n-\r\n         lineHex.write(hex);\r\n-\r\n       }\r\n-\r\n       allHexs.add(lineHex.toString());\r\n-\r\n     }\r\n-\r\n     return allHexs;\r\n-\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n-\r\n     StringBuffer invertedHex = StringBuffer();\r\n-\r\n     for (int i = 0; i < hex.length; i++) {\r\n-\r\n       String invertedHexDigit =\r\n-\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-\r\n       invertedHex.write(invertedHexDigit);\r\n-\r\n     }\r\n-\r\n     return invertedHex.toString();\r\n-\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-\r\n     List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n-\r\n     }).toList();\r\n-\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-\r\n       hexArray[i].insert(0, 1);\r\n-\r\n       hexArray[i].add(1);\r\n-\r\n     }\r\n-\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n-\r\n   }\r\n-\r\n-}\r\n-\r\n-class _Segment {\r\n-\r\n-  final String text;\r\n-\r\n-  final bool isEmoji;\r\n-\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739987961056,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -68,153 +68,161 @@\n   String message,\r\n   TextStyle textStyle, {\r\n   int cols = 44,\r\n   int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n+  int scale = 10,\r\n }) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n+  // Parse segments (text and emojis)\r\n   final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n   List<_Segment> segments = [];\r\n   int lastMatchEnd = 0;\r\n   for (final match in regex.allMatches(message)) {\r\n     if (match.start > lastMatchEnd) {\r\n       segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n     lastMatchEnd = match.end;\r\n   }\r\n   if (lastMatchEnd < message.length) {\r\n     segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n   }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n+  // Calculate dimensions\r\n   double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n   int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+  scaledFontSize = min(scaledFontSize, canvasHeight * 0.9);\r\n+  double emojiSize = scaledFontSize;\r\n+  double emojiGap = scaledFontSize * 0.2;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n-      }\r\n-      singleCopyWidth += widthWithGap;\r\n+  // Measure total width\r\n+  double totalWidth = 0;\r\n+  for (final segment in segments) {\r\n+    if (segment.isEmoji) {\r\n+      totalWidth += emojiSize;\r\n     } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+      final tp = TextPainter(\r\n+        text: TextSpan(text: segment.text, style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n         textDirection: TextDirection.ltr,\r\n       );\r\n       tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n+      totalWidth += tp.width;\r\n     }\r\n   }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+  // Create canvas (display width + scrollable content)\r\n+  int displayWidth = cols * scale;\r\n+  int canvasWidth = (totalWidth * 2 + displayWidth).ceil(); // Allow seamless scrolling\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  // Setup canvas\r\n+  final recorder = ui.PictureRecorder();\r\n+  final canvas = Canvas(recorder, Rect.fromLTRB(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  canvas.drawColor(Colors.white, BlendMode.srcOver);\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n+  // Draw segments\r\n+  double currentX = displayWidth.toDouble(); // Start off-screen for initial position\r\n+  for (final segment in segments) {\r\n+    if (segment.isEmoji) {\r\n       int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-        }\r\n+      var image = await _loadEmojiImage(index, emojiSize);\r\n+      if (image != null) {\r\n+        drawImageCentered(canvas, image, currentX, canvasHeight, emojiSize);\r\n+        currentX += emojiSize;\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n+    } else {\r\n+      currentX = drawTextSegment(\r\n+        canvas,\r\n+        segment.text,\r\n+        textStyle,\r\n+        scaledFontSize,\r\n+        currentX,\r\n+        canvasHeight,\r\n+      );\r\n     }\r\n   }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  // Convert to pixel matrix\r\n+  return await _convertCanvasToMatrix(\r\n+    recorder,\r\n+    canvasWidth,\r\n+    canvasHeight,\r\n+    cols,\r\n+    rows,\r\n+    scale,\r\n+  );\r\n+}\r\n+\r\n+Future<ui.Image?> _loadEmojiImage(int index, double size) async {\r\n+  // Your existing emoji loading logic\r\n+}\r\n+\r\n+void drawImageCentered(Canvas canvas, ui.Image image, double x, double canvasHeight, double size) {\r\n+  final y = (canvasHeight - size) / 2;\r\n+  canvas.drawImageRect(\r\n+    image,\r\n+    Rect.fromLTRB(0, 0, image.width.toDouble(), image.height.toDouble()),\r\n+    Rect.fromLTWH(x, y, size, size),\r\n+    Paint(),\r\n+  );\r\n+}\r\n+\r\n+double drawTextSegment(\r\n+  Canvas canvas,\r\n+  String text,\r\n+  TextStyle style,\r\n+  double fontSize,\r\n+  double x,\r\n+  double canvasHeight,\r\n+) {\r\n+  final tp = TextPainter(\r\n+    text: TextSpan(text: text, style: style.copyWith(fontSize: fontSize)),\r\n+    textDirection: TextDirection.ltr,\r\n+  );\r\n+  tp.layout();\r\n+  final y = (canvasHeight - tp.height) / 2; // Vertical center\r\n+  tp.paint(canvas, Offset(x, y));\r\n+  return x + tp.width;\r\n+}\r\n+\r\n+Future<List<List<bool>>> _convertCanvasToMatrix(\r\n+  ui.PictureRecorder recorder,\r\n+  int canvasWidth,\r\n+  int canvasHeight,\r\n+  int cols,\r\n+  int rows,\r\n+  int scale,\r\n+) async {\r\n+  final image = await recorder.endRecording().toImage(canvasWidth, canvasHeight);\r\n+  final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  final pixels = byteData!.buffer.asUint8List();\r\n+\r\n+  List<List<bool>> matrix = List.generate(rows, (_) => List.filled(cols, false));\r\n+\r\n   for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int darkPixels = 0;\r\n       for (int y = 0; y < scale; y++) {\r\n         for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n-          }\r\n+          final pixelX = col * scale + x;\r\n+          final pixelY = row * scale + y;\r\n+          if (pixelX >= canvasWidth) continue;\r\n+\r\n+          final index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          final r = pixels[index];\r\n+          final g = pixels[index + 1];\r\n+          final b = pixels[index + 2];\r\n+          final a = pixels[index + 3];\r\n+\r\n+          // Blend with white background and calculate brightness\r\n+          final alpha = a / 255.0;\r\n+          final brightness = (r * alpha + 255 * (1 - alpha)) * 0.2126 +\r\n+                            (g * alpha + 255 * (1 - alpha)) * 0.7152 +\r\n+                            (b * alpha + 255 * (1 - alpha)) * 0.0722;\r\n+\r\n+          if (brightness < 150) darkPixels++;\r\n         }\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n+      matrix[row][col] = darkPixels > (scale * scale * 0.25);\r\n     }\r\n   }\r\n   return matrix;\r\n }\r\n"
                },
                {
                    "date": 1739987969078,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -134,9 +134,9 @@\n         segment.text,\r\n         textStyle,\r\n         scaledFontSize,\r\n         currentX,\r\n-        canvasHeight,\r\n+        canvasHeight as double,\r\n       );\r\n     }\r\n   }\r\n \r\n"
                },
                {
                    "date": 1739988165385,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,36 +1,27 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n-\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,176 +50,92 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10,\r\n-}) async {\r\n-  // Parse segments (text and emojis)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Calculate dimensions\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  scaledFontSize = min(scaledFontSize, canvasHeight * 0.9);\r\n-  double emojiSize = scaledFontSize;\r\n-  double emojiGap = scaledFontSize * 0.2;\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // Measure total width\r\n-  double totalWidth = 0;\r\n-  for (final segment in segments) {\r\n-    if (segment.isEmoji) {\r\n-      totalWidth += emojiSize;\r\n-    } else {\r\n-      final tp = TextPainter(\r\n-        text: TextSpan(text: segment.text, style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      totalWidth += tp.width;\r\n-    }\r\n-  }\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // Create canvas (display width + scrollable content)\r\n-  int displayWidth = cols * scale;\r\n-  int canvasWidth = (totalWidth * 2 + displayWidth).ceil(); // Allow seamless scrolling\r\n+    // Prepare the text painter\r\n+    // Multiply fontSize by scale to maintain sharpness\r\n+    final TextPainter textPainter = TextPainter(\r\n+      text: TextSpan(\r\n+        text: message,\r\n+        style: textStyle.copyWith(\r\n+            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n+      ),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    textPainter.layout(maxWidth: width.toDouble());\r\n+    // Center the text on the canvas\r\n+    final Offset offset = Offset(\r\n+      (width - textPainter.width) / 2,\r\n+      (height - textPainter.height) / 2,\r\n+    );\r\n+    textPainter.paint(canvas, offset);\r\n \r\n-  // Setup canvas\r\n-  final recorder = ui.PictureRecorder();\r\n-  final canvas = Canvas(recorder, Rect.fromLTRB(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-  canvas.drawColor(Colors.white, BlendMode.srcOver);\r\n+    // End recording and get the image\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Draw segments\r\n-  double currentX = displayWidth.toDouble(); // Start off-screen for initial position\r\n-  for (final segment in segments) {\r\n-    if (segment.isEmoji) {\r\n-      int index = int.parse(segment.text);\r\n-      var image = await _loadEmojiImage(index, emojiSize);\r\n-      if (image != null) {\r\n-        drawImageCentered(canvas, image, currentX, canvasHeight, emojiSize);\r\n-        currentX += emojiSize;\r\n-      }\r\n-    } else {\r\n-      currentX = drawTextSegment(\r\n-        canvas,\r\n-        segment.text,\r\n-        textStyle,\r\n-        scaledFontSize,\r\n-        currentX,\r\n-        canvasHeight as double,\r\n-      );\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n-  }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Convert to pixel matrix\r\n-  return await _convertCanvasToMatrix(\r\n-    recorder,\r\n-    canvasWidth,\r\n-    canvasHeight,\r\n-    cols,\r\n-    rows,\r\n-    scale,\r\n-  );\r\n-}\r\n-\r\n-Future<ui.Image?> _loadEmojiImage(int index, double size) async {\r\n-  // Your existing emoji loading logic\r\n-}\r\n-\r\n-void drawImageCentered(Canvas canvas, ui.Image image, double x, double canvasHeight, double size) {\r\n-  final y = (canvasHeight - size) / 2;\r\n-  canvas.drawImageRect(\r\n-    image,\r\n-    Rect.fromLTRB(0, 0, image.width.toDouble(), image.height.toDouble()),\r\n-    Rect.fromLTWH(x, y, size, size),\r\n-    Paint(),\r\n-  );\r\n-}\r\n-\r\n-double drawTextSegment(\r\n-  Canvas canvas,\r\n-  String text,\r\n-  TextStyle style,\r\n-  double fontSize,\r\n-  double x,\r\n-  double canvasHeight,\r\n-) {\r\n-  final tp = TextPainter(\r\n-    text: TextSpan(text: text, style: style.copyWith(fontSize: fontSize)),\r\n-    textDirection: TextDirection.ltr,\r\n-  );\r\n-  tp.layout();\r\n-  final y = (canvasHeight - tp.height) / 2; // Vertical center\r\n-  tp.paint(canvas, Offset(x, y));\r\n-  return x + tp.width;\r\n-}\r\n-\r\n-Future<List<List<bool>>> _convertCanvasToMatrix(\r\n-  ui.PictureRecorder recorder,\r\n-  int canvasWidth,\r\n-  int canvasHeight,\r\n-  int cols,\r\n-  int rows,\r\n-  int scale,\r\n-) async {\r\n-  final image = await recorder.endRecording().toImage(canvasWidth, canvasHeight);\r\n-  final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  final pixels = byteData!.buffer.asUint8List();\r\n-\r\n-  List<List<bool>> matrix = List.generate(rows, (_) => List.filled(cols, false));\r\n-\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int darkPixels = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          final pixelX = col * scale + x;\r\n-          final pixelY = row * scale + y;\r\n-          if (pixelX >= canvasWidth) continue;\r\n-\r\n-          final index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          final r = pixels[index];\r\n-          final g = pixels[index + 1];\r\n-          final b = pixels[index + 2];\r\n-          final a = pixels[index + 3];\r\n-\r\n-          // Blend with white background and calculate brightness\r\n-          final alpha = a / 255.0;\r\n-          final brightness = (r * alpha + 255 * (1 - alpha)) * 0.2126 +\r\n-                            (g * alpha + 255 * (1 - alpha)) * 0.7152 +\r\n-                            (b * alpha + 255 * (1 - alpha)) * 0.0722;\r\n-\r\n-          if (brightness < 150) darkPixels++;\r\n+    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              // Calculate brightness using average of R, G, B channels.\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n+          }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkPixels > (scale * scale * 0.25);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1739988219323,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,27 +1,36 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n+\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -50,92 +59,176 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10,\r\n+}) async {\r\n+  // Parse segments (text and emojis)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Calculate dimensions\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  scaledFontSize = min(scaledFontSize, canvasHeight * 0.9);\r\n+  double emojiSize = scaledFontSize;\r\n+  double emojiGap = scaledFontSize * 0.2;\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Measure total width\r\n+  double totalWidth = 0;\r\n+  for (final segment in segments) {\r\n+    if (segment.isEmoji) {\r\n+      totalWidth += emojiSize;\r\n+    } else {\r\n+      final tp = TextPainter(\r\n+        text: TextSpan(text: segment.text, style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      totalWidth += tp.width;\r\n+    }\r\n+  }\r\n \r\n-    // Prepare the text painter\r\n-    // Multiply fontSize by scale to maintain sharpness\r\n-    final TextPainter textPainter = TextPainter(\r\n-      text: TextSpan(\r\n-        text: message,\r\n-        style: textStyle.copyWith(\r\n-            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n-      ),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas\r\n-    final Offset offset = Offset(\r\n-      (width - textPainter.width) / 2,\r\n-      (height - textPainter.height) / 2,\r\n-    );\r\n-    textPainter.paint(canvas, offset);\r\n+  // Create canvas (display width + scrollable content)\r\n+  int displayWidth = cols * scale;\r\n+  int canvasWidth = (totalWidth * 2 + displayWidth).ceil(); // Allow seamless scrolling\r\n \r\n-    // End recording and get the image\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  // Setup canvas\r\n+  final recorder = ui.PictureRecorder();\r\n+  final canvas = Canvas(recorder, Rect.fromLTRB(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  canvas.drawColor(Colors.white, BlendMode.srcOver);\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+  // Draw segments\r\n+  double currentX = displayWidth.toDouble(); // Start off-screen for initial position\r\n+  for (final segment in segments) {\r\n+    if (segment.isEmoji) {\r\n+      int index = int.parse(segment.text);\r\n+      var image = await _loadEmojiImage(index, emojiSize);\r\n+      if (image != null) {\r\n+        drawImageCentered(canvas, image, currentX, canvasHeight, emojiSize);\r\n+        currentX += emojiSize;\r\n+      }\r\n+    } else {\r\n+      currentX = drawTextSegment(\r\n+        canvas,\r\n+        segment.text,\r\n+        textStyle,\r\n+        scaledFontSize,\r\n+        currentX,\r\n+        canvasHeight as double,\r\n+      );\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  }\r\n \r\n-    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              // Calculate brightness using average of R, G, B channels.\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n-          }\r\n+  // Convert to pixel matrix\r\n+  return await _convertCanvasToMatrix(\r\n+    recorder,\r\n+    canvasWidth,\r\n+    canvasHeight,\r\n+    cols,\r\n+    rows,\r\n+    scale,\r\n+  );\r\n+}\r\n+\r\n+Future<ui.Image?> _loadEmojiImage(int index, double size) async {\r\n+  // Your existing emoji loading logic\r\n+}\r\n+\r\n+void drawImageCentered(Canvas canvas, ui.Image image, double x, double canvasHeight, double size) {\r\n+  final y = (canvasHeight - size) / 2;\r\n+  canvas.drawImageRect(\r\n+    image,\r\n+    Rect.fromLTRB(0, 0, image.width.toDouble(), image.height.toDouble()),\r\n+    Rect.fromLTWH(x, y, size, size),\r\n+    Paint(),\r\n+  );\r\n+}\r\n+\r\n+double drawTextSegment(\r\n+  Canvas canvas,\r\n+  String text,\r\n+  TextStyle style,\r\n+  double fontSize,\r\n+  double x,\r\n+  double canvasHeight,\r\n+) {\r\n+  final tp = TextPainter(\r\n+    text: TextSpan(text: text, style: style.copyWith(fontSize: fontSize)),\r\n+    textDirection: TextDirection.ltr,\r\n+  );\r\n+  tp.layout();\r\n+  final y = (canvasHeight - tp.height) / 2; // Vertical center\r\n+  tp.paint(canvas, Offset(x, y));\r\n+  return x + tp.width;\r\n+}\r\n+\r\n+Future<List<List<bool>>> _convertCanvasToMatrix(\r\n+  ui.PictureRecorder recorder,\r\n+  int canvasWidth,\r\n+  int canvasHeight,\r\n+  int cols,\r\n+  int rows,\r\n+  int scale,\r\n+) async {\r\n+  final image = await recorder.endRecording().toImage(canvasWidth, canvasHeight);\r\n+  final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  final pixels = byteData!.buffer.asUint8List();\r\n+\r\n+  List<List<bool>> matrix = List.generate(rows, (_) => List.filled(cols, false));\r\n+\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int darkPixels = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          final pixelX = col * scale + x;\r\n+          final pixelY = row * scale + y;\r\n+          if (pixelX >= canvasWidth) continue;\r\n+\r\n+          final index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          final r = pixels[index];\r\n+          final g = pixels[index + 1];\r\n+          final b = pixels[index + 2];\r\n+          final a = pixels[index + 3];\r\n+\r\n+          // Blend with white background and calculate brightness\r\n+          final alpha = a / 255.0;\r\n+          final brightness = (r * alpha + 255 * (1 - alpha)) * 0.2126 +\r\n+                            (g * alpha + 255 * (1 - alpha)) * 0.7152 +\r\n+                            (b * alpha + 255 * (1 - alpha)) * 0.0722;\r\n+\r\n+          if (brightness < 150) darkPixels++;\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkPixels > (scale * scale * 0.25);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1739988463248,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,36 +1,27 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n-\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,176 +50,92 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10,\r\n-}) async {\r\n-  // Parse segments (text and emojis)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Calculate dimensions\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  scaledFontSize = min(scaledFontSize, canvasHeight * 0.9);\r\n-  double emojiSize = scaledFontSize;\r\n-  double emojiGap = scaledFontSize * 0.2;\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // Measure total width\r\n-  double totalWidth = 0;\r\n-  for (final segment in segments) {\r\n-    if (segment.isEmoji) {\r\n-      totalWidth += emojiSize;\r\n-    } else {\r\n-      final tp = TextPainter(\r\n-        text: TextSpan(text: segment.text, style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      totalWidth += tp.width;\r\n-    }\r\n-  }\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // Create canvas (display width + scrollable content)\r\n-  int displayWidth = cols * scale;\r\n-  int canvasWidth = (totalWidth * 2 + displayWidth).ceil(); // Allow seamless scrolling\r\n+    // Prepare the text painter\r\n+    // Multiply fontSize by scale to maintain sharpness\r\n+    final TextPainter textPainter = TextPainter(\r\n+      text: TextSpan(\r\n+        text: message,\r\n+        style: textStyle.copyWith(\r\n+            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n+      ),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    textPainter.layout(maxWidth: width.toDouble());\r\n+    // Center the text on the canvas\r\n+    final Offset offset = Offset(\r\n+      (width - textPainter.width) / 2,\r\n+      (height - textPainter.height) / 2,\r\n+    );\r\n+    textPainter.paint(canvas, offset);\r\n \r\n-  // Setup canvas\r\n-  final recorder = ui.PictureRecorder();\r\n-  final canvas = Canvas(recorder, Rect.fromLTRB(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-  canvas.drawColor(Colors.white, BlendMode.srcOver);\r\n+    // End recording and get the image\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Draw segments\r\n-  double currentX = displayWidth.toDouble(); // Start off-screen for initial position\r\n-  for (final segment in segments) {\r\n-    if (segment.isEmoji) {\r\n-      int index = int.parse(segment.text);\r\n-      var image = await _loadEmojiImage(index, emojiSize);\r\n-      if (image != null) {\r\n-        drawImageCentered(canvas, image, currentX, canvasHeight, emojiSize);\r\n-        currentX += emojiSize;\r\n-      }\r\n-    } else {\r\n-      currentX = drawTextSegment(\r\n-        canvas,\r\n-        segment.text,\r\n-        textStyle,\r\n-        scaledFontSize,\r\n-        currentX,\r\n-        canvasHeight as double,\r\n-      );\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n-  }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Convert to pixel matrix\r\n-  return await _convertCanvasToMatrix(\r\n-    recorder,\r\n-    canvasWidth,\r\n-    canvasHeight,\r\n-    cols,\r\n-    rows,\r\n-    scale,\r\n-  );\r\n-}\r\n-\r\n-Future<ui.Image?> _loadEmojiImage(int index, double size) async {\r\n-  // Your existing emoji loading logic\r\n-}\r\n-\r\n-void drawImageCentered(Canvas canvas, ui.Image image, double x, double canvasHeight, double size) {\r\n-  final y = (canvasHeight - size) / 2;\r\n-  canvas.drawImageRect(\r\n-    image,\r\n-    Rect.fromLTRB(0, 0, image.width.toDouble(), image.height.toDouble()),\r\n-    Rect.fromLTWH(x, y, size, size),\r\n-    Paint(),\r\n-  );\r\n-}\r\n-\r\n-double drawTextSegment(\r\n-  Canvas canvas,\r\n-  String text,\r\n-  TextStyle style,\r\n-  double fontSize,\r\n-  double x,\r\n-  double canvasHeight,\r\n-) {\r\n-  final tp = TextPainter(\r\n-    text: TextSpan(text: text, style: style.copyWith(fontSize: fontSize)),\r\n-    textDirection: TextDirection.ltr,\r\n-  );\r\n-  tp.layout();\r\n-  final y = (canvasHeight - tp.height) / 2; // Vertical center\r\n-  tp.paint(canvas, Offset(x, y));\r\n-  return x + tp.width;\r\n-}\r\n-\r\n-Future<List<List<bool>>> _convertCanvasToMatrix(\r\n-  ui.PictureRecorder recorder,\r\n-  int canvasWidth,\r\n-  int canvasHeight,\r\n-  int cols,\r\n-  int rows,\r\n-  int scale,\r\n-) async {\r\n-  final image = await recorder.endRecording().toImage(canvasWidth, canvasHeight);\r\n-  final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  final pixels = byteData!.buffer.asUint8List();\r\n-\r\n-  List<List<bool>> matrix = List.generate(rows, (_) => List.filled(cols, false));\r\n-\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int darkPixels = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          final pixelX = col * scale + x;\r\n-          final pixelY = row * scale + y;\r\n-          if (pixelX >= canvasWidth) continue;\r\n-\r\n-          final index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          final r = pixels[index];\r\n-          final g = pixels[index + 1];\r\n-          final b = pixels[index + 2];\r\n-          final a = pixels[index + 3];\r\n-\r\n-          // Blend with white background and calculate brightness\r\n-          final alpha = a / 255.0;\r\n-          final brightness = (r * alpha + 255 * (1 - alpha)) * 0.2126 +\r\n-                            (g * alpha + 255 * (1 - alpha)) * 0.7152 +\r\n-                            (b * alpha + 255 * (1 - alpha)) * 0.0722;\r\n-\r\n-          if (brightness < 150) darkPixels++;\r\n+    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              // Calculate brightness using average of R, G, B channels.\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n+          }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkPixels > (scale * scale * 0.25);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -320,5 +227,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\n\\ No newline at end of file\n+} \n\\ No newline at end of file\n"
                },
                {
                    "date": 1739988478465,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,27 +1,36 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n+\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -50,92 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n+    }\r\n+  }\r\n \r\n-    // Prepare the text painter\r\n-    // Multiply fontSize by scale to maintain sharpness\r\n-    final TextPainter textPainter = TextPainter(\r\n-      text: TextSpan(\r\n-        text: message,\r\n-        style: textStyle.copyWith(\r\n-            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n-      ),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas\r\n-    final Offset offset = Offset(\r\n-      (width - textPainter.width) / 2,\r\n-      (height - textPainter.height) / 2,\r\n-    );\r\n-    textPainter.paint(canvas, offset);\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // End recording and get the image\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+        }\r\n+      }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              // Calculate brightness using average of R, G, B channels.\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -227,5 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-} \n\\ No newline at end of file\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739988598346,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,316 +1,236 @@\n import 'dart:math';\r\n+\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n-  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-  ImageUtils imageUtils = ImageUtils();\r\n+@@ -15,7 +17,6 @@ class Converters {\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n-        int index = int.parse(message[x + 2] + message[x + 3]);\r\n-        var key = controllerData.imageCache.keys.toList()[index];\r\n-        if (key is List) {\r\n-          String filename = key[0];\r\n-          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n-          x += 5;\r\n-        } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n-          hexStrings.addAll(hs);\r\n-          x += 5;\r\n-        }\r\n-      } else {\r\n-        if (converter.charCodes.containsKey(message[x])) {\r\n-          hexStrings.add(converter.charCodes[message[x]]!);\r\n-        }\r\n-      }\r\n-    }\r\n-    if (isInverted) {\r\n-      hexStrings = invertHex(hexStrings.join()).split('');\r\n-      hexStrings = padHexString(hexStrings);\r\n-    }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n+@@ -50,108 +51,161 @@ class Converters {\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+  //function to convert the bitmap to the LED hex format\r\n+  //it takes the 2D list of pixels and converts it to the LED hex format\r\n+  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n-      }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n-    }\r\n-  }\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Prepare the text painter\r\n+    // Multiply fontSize by scale to maintain sharpness\r\n+    final TextPainter textPainter = TextPainter(\r\n+      text: TextSpan(\r\n+        text: message,\r\n+        style: textStyle.copyWith(\r\n+            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n+      ),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    textPainter.layout(maxWidth: width.toDouble());\r\n+    // Center the text on the canvas\r\n+    final Offset offset = Offset(\r\n+      (width - textPainter.width) / 2,\r\n+      (height - textPainter.height) / 2,\r\n+    );\r\n+    textPainter.paint(canvas, offset);\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // End recording and get the image\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-        }\r\n-      }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              // Calculate brightness using average of R, G, B channels.\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n+    // Determine the height and width of the image\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n+\r\n+    // Initialize variables to calculate padding and offsets\r\n     int finalSum = 0;\r\n+\r\n+    // Calculate and adjust for right-side padding\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j]; // Sum up pixel values in each column\r\n         sum += image[i][j];\r\n       }\r\n       if (sum == 0 && trim) {\r\n+        // If column sum is zero, mark all pixels in that column as -1\r\n         for (int i = 0; i < height; i++) {\r\n           image[i][j] = -1;\r\n         }\r\n       } else {\r\n+        // Otherwise, update finalSum and exit loop\r\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n+\r\n+    // Calculate and adjust for left-side padding\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n+        sum += image[i]\r\n+            [j]; // Sum up pixel values in each column (from right to left)\r\n         sum += image[i][j];\r\n       }\r\n       if (sum == 0 && trim) {\r\n+        // If column sum is zero, mark all pixels in that column as -1\r\n         for (int i = 0; i < height; i++) {\r\n           image[i][j] = -1;\r\n         }\r\n       } else {\r\n+        // Otherwise, update finalSum and exit loop\r\n         finalSum += (height - j - 1);\r\n         break;\r\n       }\r\n     }\r\n+\r\n+    // Calculate padding difference to align height to a multiple of 8\r\n     int diff = 0;\r\n     if ((height - finalSum) % 8 > 0) {\r\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n+\r\n+    // Calculate left and right offsets for padding\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n+\r\n+    // Initialize a new list to accommodate the padded image\r\n     List<List<int>> list =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+\r\n+    // Fill the new list with the padded image data\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n+        list[i][k++] = 0; // Fill right-side padding\r\n         list[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n+          list[i][k++] = image[i][j]; // Copy non-padded pixels\r\n           list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n+        list[i][k++] = 0; // Fill left-side padding\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n+\r\n     logger.d(\"Padded image: $list\");\r\n+\r\n+    // Convert each 8-bit segment into hexadecimal strings\r\n     List<String> allHexs = [];\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n+\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n+\r\n+        // Construct 8-bit segments for each row\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n           stBuilder.write(list[k][j]);\r\n         }\r\n+\r\n+        // Convert binary string to hexadecimal\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n+        lineHex.write(hex); // Append hexadecimal to line\r\n         lineHex.write(hex);\r\n       }\r\n+\r\n+      allHexs.add(lineHex.toString()); // Store completed hexadecimal line\r\n       allHexs.add(lineHex.toString());\r\n     }\r\n+    return allHexs; // Return list of hexadecimal strings\r\n     return allHexs;\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n-    StringBuffer invertedHex = StringBuffer();\r\n-    for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n-          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n-    }\r\n-    return invertedHex.toString();\r\n-  }\r\n-\r\n-  List<String> padHexString(List<String> hexString) {\r\n+@@ -168,13 +222,10 @@ class Converters {\r\n     List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n     }).toList();\r\n+\r\n+    //add 1 at the satrt and end of each row in the 2D list\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1);\r\n       hexArray[i].add(1);\r\n     }\r\n+\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739988604942,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -0,0 +1,316 @@\n+import 'dart:math';\r\n+import 'dart:typed_data';\r\n+import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n+import 'package:badgemagic/providers/imageprovider.dart';\r\n+import 'package:flutter/material.dart';\r\n+import 'package:get_it/get_it.dart';\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n+class Converters {\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n+  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+  ImageUtils imageUtils = ImageUtils();\r\n+  FileHelper fileHelper = FileHelper();\r\n+\r\n+  int controllerLength = 0;\r\n+\r\n+  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n+    List<String> hexStrings = [];\r\n+    for (int x = 0; x < message.length; x++) {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n+        int index = int.parse(message[x + 2] + message[x + 3]);\r\n+        var key = controllerData.imageCache.keys.toList()[index];\r\n+        if (key is List) {\r\n+          String filename = key[0];\r\n+          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n+          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n+          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n+          x += 5;\r\n+        } else {\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          hexStrings.addAll(hs);\r\n+          x += 5;\r\n+        }\r\n+      } else {\r\n+        if (converter.charCodes.containsKey(message[x])) {\r\n+          hexStrings.add(converter.charCodes[message[x]]!);\r\n+        }\r\n+      }\r\n+    }\r\n+    if (isInverted) {\r\n+      hexStrings = invertHex(hexStrings.join()).split('');\r\n+      hexStrings = padHexString(hexStrings);\r\n+    }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n+    return hexStrings;\r\n+  }\r\n+\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n+\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n+    }\r\n+  }\r\n+\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n+\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+        }\r\n+      }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n+    }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n+          }\r\n+        }\r\n+      }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n+    }\r\n+  }\r\n+  return matrix;\r\n+}\r\n+\r\n+\r\n+  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n+    int height = image.length;\r\n+    int width = image.isNotEmpty ? image[0].length : 0;\r\n+    int finalSum = 0;\r\n+    for (int j = 0; j < width; j++) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += j;\r\n+        break;\r\n+      }\r\n+    }\r\n+    for (int j = width - 1; j >= 0; j--) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += (height - j - 1);\r\n+        break;\r\n+      }\r\n+    }\r\n+    int diff = 0;\r\n+    if ((height - finalSum) % 8 > 0) {\r\n+      diff = 8 - (height - finalSum) % 8;\r\n+    }\r\n+    int rOff = (diff / 2).floor();\r\n+    int lOff = (diff / 2).ceil();\r\n+    List<List<int>> list =\r\n+        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+    for (int i = 0; i < height; i++) {\r\n+      int k = 0;\r\n+      for (int j = 0; j < rOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+      for (int j = 0; j < width; j++) {\r\n+        if (image[i][j] != -1) {\r\n+          list[i][k++] = image[i][j];\r\n+        }\r\n+      }\r\n+      for (int j = 0; j < lOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+    }\r\n+    logger.d(\"Padded image: $list\");\r\n+    List<String> allHexs = [];\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+      StringBuffer lineHex = StringBuffer();\r\n+      for (int k = 0; k < height; k++) {\r\n+        StringBuffer stBuilder = StringBuffer();\r\n+        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n+          stBuilder.write(list[k][j]);\r\n+        }\r\n+        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n+            .toRadixString(16)\r\n+            .padLeft(2, '0');\r\n+        lineHex.write(hex);\r\n+      }\r\n+      allHexs.add(lineHex.toString());\r\n+    }\r\n+    return allHexs;\r\n+  }\r\n+\r\n+  static String invertHex(String hex) {\r\n+    StringBuffer invertedHex = StringBuffer();\r\n+    for (int i = 0; i < hex.length; i++) {\r\n+      String invertedHexDigit =\r\n+          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+      invertedHex.write(invertedHexDigit);\r\n+    }\r\n+    return invertedHex.toString();\r\n+  }\r\n+\r\n+  List<String> padHexString(List<String> hexString) {\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n+    for (int i = 0; i < hexArray.length; i++) {\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n+    }\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n+  }\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739988831594,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,36 +1,27 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n-\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,289 +50,8 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n-\r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n-\r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n-      }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n-    }\r\n-  }\r\n-\r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n-\r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-\r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-        }\r\n-      }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n-    }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n-          }\r\n-        }\r\n-      }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n-    }\r\n-  }\r\n-  return matrix;\r\n-}\r\n-\r\n-\r\n-  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n-    int height = image.length;\r\n-    int width = image.isNotEmpty ? image[0].length : 0;\r\n-    int finalSum = 0;\r\n-    for (int j = 0; j < width; j++) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += j;\r\n-        break;\r\n-      }\r\n-    }\r\n-    for (int j = width - 1; j >= 0; j--) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += (height - j - 1);\r\n-        break;\r\n-      }\r\n-    }\r\n-    int diff = 0;\r\n-    if ((height - finalSum) % 8 > 0) {\r\n-      diff = 8 - (height - finalSum) % 8;\r\n-    }\r\n-    int rOff = (diff / 2).floor();\r\n-    int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n-        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n-    for (int i = 0; i < height; i++) {\r\n-      int k = 0;\r\n-      for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-      for (int j = 0; j < width; j++) {\r\n-        if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n-        }\r\n-      }\r\n-      for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-    }\r\n-    logger.d(\"Padded image: $list\");\r\n-    List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n-      StringBuffer lineHex = StringBuffer();\r\n-      for (int k = 0; k < height; k++) {\r\n-        StringBuffer stBuilder = StringBuffer();\r\n-        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n-        }\r\n-        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n-            .toRadixString(16)\r\n-            .padLeft(2, '0');\r\n-        lineHex.write(hex);\r\n-      }\r\n-      allHexs.add(lineHex.toString());\r\n-    }\r\n-    return allHexs;\r\n-  }\r\n-\r\n-  static String invertHex(String hex) {\r\n-    StringBuffer invertedHex = StringBuffer();\r\n-    for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n-          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n-    }\r\n-    return invertedHex.toString();\r\n-  }\r\n-\r\n-  List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n-    for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n-      hexArray[i].add(1);\r\n-    }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n-  }\r\n-}\n-import 'dart:math';\r\n-\r\n-import 'dart:typed_data';\r\n-import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-import 'package:badgemagic/providers/imageprovider.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:get_it/get_it.dart';\r\n-\r\n-class Converters {\r\n-@@ -15,7 +17,6 @@ class Converters {\r\n-  FileHelper fileHelper = FileHelper();\r\n-\r\n-  int controllerLength = 0;\r\n-\r\n-  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n-    List<String> hexStrings = [];\r\n-    for (int x = 0; x < message.length; x++) {\r\n-@@ -50,108 +51,161 @@ class Converters {\r\n-    return hexStrings;\r\n-  }\r\n-\r\n-  //function to convert the bitmap to the LED hex format\r\n-  //it takes the 2D list of pixels and converts it to the LED hex format\r\n   /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n@@ -425,128 +135,97 @@\n     return matrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n-    // Determine the height and width of the image\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n-\r\n-    // Initialize variables to calculate padding and offsets\r\n     int finalSum = 0;\r\n-\r\n-    // Calculate and adjust for right-side padding\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j]; // Sum up pixel values in each column\r\n         sum += image[i][j];\r\n       }\r\n       if (sum == 0 && trim) {\r\n-        // If column sum is zero, mark all pixels in that column as -1\r\n         for (int i = 0; i < height; i++) {\r\n           image[i][j] = -1;\r\n         }\r\n       } else {\r\n-        // Otherwise, update finalSum and exit loop\r\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n-\r\n-    // Calculate and adjust for left-side padding\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n-        sum += image[i]\r\n-            [j]; // Sum up pixel values in each column (from right to left)\r\n         sum += image[i][j];\r\n       }\r\n       if (sum == 0 && trim) {\r\n-        // If column sum is zero, mark all pixels in that column as -1\r\n         for (int i = 0; i < height; i++) {\r\n           image[i][j] = -1;\r\n         }\r\n       } else {\r\n-        // Otherwise, update finalSum and exit loop\r\n         finalSum += (height - j - 1);\r\n         break;\r\n       }\r\n     }\r\n-\r\n-    // Calculate padding difference to align height to a multiple of 8\r\n     int diff = 0;\r\n     if ((height - finalSum) % 8 > 0) {\r\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n-\r\n-    // Calculate left and right offsets for padding\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n-\r\n-    // Initialize a new list to accommodate the padded image\r\n     List<List<int>> list =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n-\r\n-    // Fill the new list with the padded image data\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0; // Fill right-side padding\r\n         list[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j]; // Copy non-padded pixels\r\n           list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0; // Fill left-side padding\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n-\r\n     logger.d(\"Padded image: $list\");\r\n-\r\n-    // Convert each 8-bit segment into hexadecimal strings\r\n     List<String> allHexs = [];\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n-\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n-\r\n-        // Construct 8-bit segments for each row\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n           stBuilder.write(list[k][j]);\r\n         }\r\n-\r\n-        // Convert binary string to hexadecimal\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n-        lineHex.write(hex); // Append hexadecimal to line\r\n         lineHex.write(hex);\r\n       }\r\n-\r\n-      allHexs.add(lineHex.toString()); // Store completed hexadecimal line\r\n       allHexs.add(lineHex.toString());\r\n     }\r\n-    return allHexs; // Return list of hexadecimal strings\r\n     return allHexs;\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n-@@ -168,13 +222,10 @@ class Converters {\r\n+    StringBuffer invertedHex = StringBuffer();\r\n+    for (int i = 0; i < hex.length; i++) {\r\n+      String invertedHexDigit =\r\n+          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+      invertedHex.write(invertedHexDigit);\r\n+    }\r\n+    return invertedHex.toString();\r\n+  }\r\n+\r\n+  List<String> padHexString(List<String> hexString) {\r\n     List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n     }).toList();\r\n-\r\n-    //add 1 at the satrt and end of each row in the 2D list\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1);\r\n       hexArray[i].add(1);\r\n     }\r\n-\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739988889217,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,27 +1,36 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n+\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -50,92 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n+    }\r\n+  }\r\n \r\n-    // Prepare the text painter\r\n-    // Multiply fontSize by scale to maintain sharpness\r\n-    final TextPainter textPainter = TextPainter(\r\n-      text: TextSpan(\r\n-        text: message,\r\n-        style: textStyle.copyWith(\r\n-            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n-      ),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas\r\n-    final Offset offset = Offset(\r\n-      (width - textPainter.width) / 2,\r\n-      (height - textPainter.height) / 2,\r\n-    );\r\n-    textPainter.paint(canvas, offset);\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // End recording and get the image\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+        }\r\n+      }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              // Calculate brightness using average of R, G, B channels.\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1739989152200,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,36 +1,27 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n-\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,168 +50,92 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-    }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n-      }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n-    }\r\n-  }\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Prepare the text painter\r\n+    // Multiply fontSize by scale to maintain sharpness\r\n+    final TextPainter textPainter = TextPainter(\r\n+      text: TextSpan(\r\n+        text: message,\r\n+        style: textStyle.copyWith(\r\n+            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n+      ),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    textPainter.layout(maxWidth: width.toDouble());\r\n+    // Center the text on the canvas\r\n+    final Offset offset = Offset(\r\n+      (width - textPainter.width) / 2,\r\n+      (height - textPainter.height) / 2,\r\n+    );\r\n+    textPainter.paint(canvas, offset);\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // End recording and get the image\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-        }\r\n-      }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              // Calculate brightness using average of R, G, B channels.\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1739989317043,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,67 +16,97 @@\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n+\r\n+  /// Converts a given [message] into a list of LED hex strings.\r\n+  ///\r\n+  /// Tokens in the message should match the format `<XNNY>` (for example,\r\n+  /// `<I12!>`), where the two digits (`NN`) are parsed as an index. Any text\r\n+  /// outside of such tokens is converted via [converter.charCodes].\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n-    for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n-        int index = int.parse(message[x + 2] + message[x + 3]);\r\n+    // Use a regex to match tokens of the format: <XNNY>\r\n+    final RegExp tokenRegExp = RegExp(r'<(.)(\\d{2})(.)>');\r\n+    int lastMatchEnd = 0;\r\n+\r\n+    // Process each token found in the message.\r\n+    for (final match in tokenRegExp.allMatches(message)) {\r\n+      // Process text before this token.\r\n+      if (match.start > lastMatchEnd) {\r\n+        final textSegment = message.substring(lastMatchEnd, match.start);\r\n+        for (int i = 0; i < textSegment.length; i++) {\r\n+          if (converter.charCodes.containsKey(textSegment[i])) {\r\n+            hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n+          }\r\n+        }\r\n+      }\r\n+\r\n+      // Extract token details.\r\n+      String? token = match.group(0);\r\n+      String? indexStr = match.group(2); // two-digit number\r\n+      if (token != null && indexStr != null) {\r\n+        int index = int.parse(indexStr);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n           List<List<int>> imageData =\r\n               image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n-          x += 5;\r\n+          hexStrings.addAll(convertBitmapToLEDHex(imageData, true));\r\n         } else {\r\n           List<String> hs =\r\n               await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n-          x += 5;\r\n         }\r\n-      } else {\r\n-        if (converter.charCodes.containsKey(message[x])) {\r\n-          hexStrings.add(converter.charCodes[message[x]]!);\r\n+      }\r\n+      lastMatchEnd = match.end;\r\n+    }\r\n+\r\n+    // Process any remaining text after the last token.\r\n+    if (lastMatchEnd < message.length) {\r\n+      final textSegment = message.substring(lastMatchEnd);\r\n+      for (int i = 0; i < textSegment.length; i++) {\r\n+        if (converter.charCodes.containsKey(textSegment[i])) {\r\n+          hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n         }\r\n       }\r\n     }\r\n+\r\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// New: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  /// Renders the given [message] using the provided [textStyle] onto an offscreen\r\n+  /// canvas and converts it to an LED matrix (11 rows x 44 columns).\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n   }) async {\r\n-    // Calculate canvas size\r\n+    // Calculate canvas size.\r\n     final int width = cols * scale;\r\n     final int height = rows * scale;\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n+    // Create a PictureRecorder and Canvas.\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n         recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-    // Fill background with white\r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-    // Prepare the text painter\r\n-    // Multiply fontSize by scale to maintain sharpness\r\n+    // Prepare the text painter.\r\n+    // Multiply fontSize by scale to maintain sharpness.\r\n     final TextPainter textPainter = TextPainter(\r\n       text: TextSpan(\r\n         text: message,\r\n         style: textStyle.copyWith(\r\n@@ -84,16 +114,16 @@\n       ),\r\n       textDirection: TextDirection.ltr,\r\n     );\r\n     textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas\r\n+    // Center the text on the canvas.\r\n     final Offset offset = Offset(\r\n       (width - textPainter.width) / 2,\r\n       (height - textPainter.height) / 2,\r\n     );\r\n     textPainter.paint(canvas, offset);\r\n \r\n-    // End recording and get the image\r\n+    // End recording and get the image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image image = await picture.toImage(width, height);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -127,9 +157,9 @@\n             }\r\n           }\r\n         }\r\n         double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false)\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false).\r\n         matrix[row][col] = avgBrightness < 128;\r\n       }\r\n     }\r\n     return matrix;\r\n@@ -227,5 +257,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\n\\ No newline at end of file\n+}\r\n"
                },
                {
                    "date": 1739989673927,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -17,245 +17,184 @@\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n-  /// Converts a given [message] into a list of LED hex strings.\r\n-  ///\r\n-  /// Tokens in the message should match the format `<XNNY>` (for example,\r\n-  /// `<I12!>`), where the two digits (`NN`) are parsed as an index. Any text\r\n-  /// outside of such tokens is converted via [converter.charCodes].\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n-    // Use a regex to match tokens of the format: <XNNY>\r\n-    final RegExp tokenRegExp = RegExp(r'<(.)(\\d{2})(.)>');\r\n-    int lastMatchEnd = 0;\r\n-\r\n-    // Process each token found in the message.\r\n-    for (final match in tokenRegExp.allMatches(message)) {\r\n-      // Process text before this token.\r\n-      if (match.start > lastMatchEnd) {\r\n-        final textSegment = message.substring(lastMatchEnd, match.start);\r\n-        for (int i = 0; i < textSegment.length; i++) {\r\n-          if (converter.charCodes.containsKey(textSegment[i])) {\r\n-            hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n-          }\r\n-        }\r\n-      }\r\n-\r\n-      // Extract token details.\r\n-      String? token = match.group(0);\r\n-      String? indexStr = match.group(2); // two-digit number\r\n-      if (token != null && indexStr != null) {\r\n-        int index = int.parse(indexStr);\r\n-        var key = controllerData.imageCache.keys.toList()[index];\r\n-        if (key is List) {\r\n-          String filename = key[0];\r\n-          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings.addAll(convertBitmapToLEDHex(imageData, true));\r\n-        } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n-          hexStrings.addAll(hs);\r\n-        }\r\n-      }\r\n-      lastMatchEnd = match.end;\r\n+    List<List<List<bool>>> allMatrices =\r\n+        await renderTextToMatrices(message, TextStyle(fontSize: 11));\r\n+    for (var matrix in allMatrices) {\r\n+      List<List<int>> intMatrix = matrix\r\n+          .map((row) => row.map((b) => b ? 1 : 0).toList())\r\n+          .toList();\r\n+      hexStrings.addAll(convertBitmapToLEDHex(intMatrix, false));\r\n     }\r\n-\r\n-    // Process any remaining text after the last token.\r\n-    if (lastMatchEnd < message.length) {\r\n-      final textSegment = message.substring(lastMatchEnd);\r\n-      for (int i = 0; i < textSegment.length; i++) {\r\n-        if (converter.charCodes.containsKey(textSegment[i])) {\r\n-          hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n-        }\r\n-      }\r\n-    }\r\n-\r\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Renders the given [message] using the provided [textStyle] onto an offscreen\r\n-  /// canvas and converts it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n+  /// Renders text into multiple 11x44 matrices, splitting as needed.\r\n+  Future<List<List<List<bool>>>> renderTextToMatrices(\r\n     String message,\r\n     TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n+    int scale = 10,\r\n   }) async {\r\n-    // Calculate canvas size.\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+    List<List<List<bool>>> allMatrices = [];\r\n+    // Render entire text into a single wide matrix\r\n+    List<List<bool>> fullMatrix = await _renderFullMatrix(message, textStyle, scale);\r\n+    // Split into 44-column chunks\r\n+    int cols = fullMatrix.isNotEmpty ? fullMatrix[0].length : 0;\r\n+    for (int startCol = 0; startCol < cols; startCol += 44) {\r\n+      int endCol = min(startCol + 44, cols);\r\n+      List<List<bool>> chunk = [];\r\n+      for (int row = 0; row < 11; row++) {\r\n+        List<bool> chunkRow = [];\r\n+        for (int col = startCol; col < endCol; col++) {\r\n+          chunkRow.add(fullMatrix[row][col]);\r\n+        }\r\n+        // Pad with false (off) if chunk is less than 44 columns\r\n+        while (chunkRow.length < 44) {\r\n+          chunkRow.add(false);\r\n+        }\r\n+        chunk.add(chunkRow);\r\n+      }\r\n+      allMatrices.add(chunk);\r\n+    }\r\n+    return allMatrices;\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas.\r\n+  Future<List<List<bool>>> _renderFullMatrix(\r\n+      String message, TextStyle textStyle, int scale) async {\r\n+    final textPainter = TextPainter(\r\n+      text: TextSpan(text: message, style: textStyle),\r\n+      textDirection: TextDirection.ltr,\r\n+    );\r\n+    textPainter.layout();\r\n+    double textWidth = textPainter.width;\r\n+    double textHeight = textPainter.height;\r\n+    int cols = (textWidth / scale).ceil();\r\n+    int rows = 11;\r\n+\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n-\r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    final Canvas canvas = Canvas(recorder,\r\n+        Rect.fromLTWH(0, 0, cols * scale.toDouble(), rows * scale.toDouble()));\r\n+    // Draw background\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, cols * scale.toDouble(), rows * scale.toDouble()),\r\n+        Paint()..color = Colors.white);\r\n+    // Draw text centered vertically\r\n+    textPainter.paint(\r\n+        canvas,\r\n+        Offset(\r\n+            0, (rows * scale - textHeight) / 2)); // Left-aligned horizontally\r\n \r\n-    // Prepare the text painter.\r\n-    // Multiply fontSize by scale to maintain sharpness.\r\n-    final TextPainter textPainter = TextPainter(\r\n-      text: TextSpan(\r\n-        text: message,\r\n-        style: textStyle.copyWith(\r\n-            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n-      ),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas.\r\n-    final Offset offset = Offset(\r\n-      (width - textPainter.width) / 2,\r\n-      (height - textPainter.height) / 2,\r\n-    );\r\n-    textPainter.paint(canvas, offset);\r\n-\r\n-    // End recording and get the image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n+    final ui.Image image = await recorder.endRecording().toImage(\r\n+        cols * scale, rows * scale);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) return List.generate(rows, (_) => List.filled(cols, false));\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    Uint8List data = byteData.buffer.asUint8List();\r\n+    List<List<bool>> matrix = [];\r\n     for (int row = 0; row < rows; row++) {\r\n+      List<bool> matrixRow = [];\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n-        int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            int index = (pixelY * cols * scale + pixelX) * 4;\r\n             if (index + 3 < data.length) {\r\n-              // Calculate brightness using average of R, G, B channels.\r\n               int r = data[index];\r\n               int g = data[index + 1];\r\n               int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n+              sum += (r + g + b) ~/ 3;\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false).\r\n-        matrix[row][col] = avgBrightness < 128;\r\n+        matrixRow.add((sum / (scale * scale)) < 128);\r\n       }\r\n+      matrix.add(matrixRow);\r\n     }\r\n     return matrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n-    int finalSum = 0;\r\n-    for (int j = 0; j < width; j++) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n+    int firstNonEmpty = -1;\r\n+    int lastNonEmpty = -1;\r\n+\r\n+    // Find first and last non-empty columns if trimming\r\n+    if (trim) {\r\n+      for (int j = 0; j < width; j++) {\r\n         for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n+          if (image[i][j] == 1) {\r\n+            if (firstNonEmpty == -1) firstNonEmpty = j;\r\n+            lastNonEmpty = j;\r\n+          }\r\n         }\r\n-      } else {\r\n-        finalSum += j;\r\n-        break;\r\n       }\r\n+      if (firstNonEmpty == -1) return []; // All empty\r\n+    } else {\r\n+      firstNonEmpty = 0;\r\n+      lastNonEmpty = width - 1;\r\n     }\r\n-    for (int j = width - 1; j >= 0; j--) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += (height - j - 1);\r\n-        break;\r\n-      }\r\n-    }\r\n-    int diff = 0;\r\n-    if ((height - finalSum) % 8 > 0) {\r\n-      diff = 8 - (height - finalSum) % 8;\r\n-    }\r\n+\r\n+    int contentWidth = lastNonEmpty - firstNonEmpty + 1;\r\n+    int diff = (8 - (contentWidth % 8)) % 8;\r\n     int rOff = (diff / 2).floor();\r\n-    int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n-        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+    int lOff = diff - rOff;\r\n+\r\n+    List<List<int>> paddedImage = [];\r\n     for (int i = 0; i < height; i++) {\r\n-      int k = 0;\r\n-      for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n+      List<int> paddedRow = [];\r\n+      // Add left padding\r\n+      paddedRow.addAll(List.filled(lOff, 0));\r\n+      // Add content\r\n+      for (int j = firstNonEmpty; j <= lastNonEmpty; j++) {\r\n+        paddedRow.add(image[i][j]);\r\n       }\r\n-      for (int j = 0; j < width; j++) {\r\n-        if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n-        }\r\n-      }\r\n-      for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n+      // Add right padding\r\n+      paddedRow.addAll(List.filled(rOff, 0));\r\n+      paddedImage.add(paddedRow);\r\n     }\r\n-    logger.d(\"Padded image: $list\");\r\n-    List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n-      StringBuffer lineHex = StringBuffer();\r\n-      for (int k = 0; k < height; k++) {\r\n-        StringBuffer stBuilder = StringBuffer();\r\n-        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n+\r\n+    int totalBlocks = (paddedImage[0].length + 7) ~/ 8;\r\n+    List<String> hexStrings = [];\r\n+    for (int block = 0; block < totalBlocks; block++) {\r\n+      StringBuffer blockHex = StringBuffer();\r\n+      for (int row = 0; row < height; row++) {\r\n+        StringBuffer rowBits = StringBuffer();\r\n+        for (int col = block * 8; col < (block + 1) * 8; col++) {\r\n+          if (col < paddedImage[row].length) {\r\n+            rowBits.write(paddedImage[row][col]);\r\n+          } else {\r\n+            rowBits.write(0);\r\n+          }\r\n         }\r\n-        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n+        String hex = int.parse(rowBits.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n-        lineHex.write(hex);\r\n+        blockHex.write(hex);\r\n       }\r\n-      allHexs.add(lineHex.toString());\r\n+      hexStrings.add(blockHex.toString());\r\n     }\r\n-    return allHexs;\r\n+    return hexStrings;\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n-    StringBuffer invertedHex = StringBuffer();\r\n-    for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n-          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n-    }\r\n-    return invertedHex.toString();\r\n+    return hex.split('').map((c) {\r\n+      int value = int.parse(c, radix: 16);\r\n+      return (15 - value).toRadixString(16).toUpperCase();\r\n+    }).join();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n-    for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n-      hexArray[i].add(1);\r\n-    }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n+    // Existing padding logic if needed\r\n+    return hexString;\r\n   }\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739989693421,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -17,184 +17,245 @@\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n+  /// Converts a given [message] into a list of LED hex strings.\r\n+  ///\r\n+  /// Tokens in the message should match the format `<XNNY>` (for example,\r\n+  /// `<I12!>`), where the two digits (`NN`) are parsed as an index. Any text\r\n+  /// outside of such tokens is converted via [converter.charCodes].\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n-    List<List<List<bool>>> allMatrices =\r\n-        await renderTextToMatrices(message, TextStyle(fontSize: 11));\r\n-    for (var matrix in allMatrices) {\r\n-      List<List<int>> intMatrix = matrix\r\n-          .map((row) => row.map((b) => b ? 1 : 0).toList())\r\n-          .toList();\r\n-      hexStrings.addAll(convertBitmapToLEDHex(intMatrix, false));\r\n+    // Use a regex to match tokens of the format: <XNNY>\r\n+    final RegExp tokenRegExp = RegExp(r'<(.)(\\d{2})(.)>');\r\n+    int lastMatchEnd = 0;\r\n+\r\n+    // Process each token found in the message.\r\n+    for (final match in tokenRegExp.allMatches(message)) {\r\n+      // Process text before this token.\r\n+      if (match.start > lastMatchEnd) {\r\n+        final textSegment = message.substring(lastMatchEnd, match.start);\r\n+        for (int i = 0; i < textSegment.length; i++) {\r\n+          if (converter.charCodes.containsKey(textSegment[i])) {\r\n+            hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n+          }\r\n+        }\r\n+      }\r\n+\r\n+      // Extract token details.\r\n+      String? token = match.group(0);\r\n+      String? indexStr = match.group(2); // two-digit number\r\n+      if (token != null && indexStr != null) {\r\n+        int index = int.parse(indexStr);\r\n+        var key = controllerData.imageCache.keys.toList()[index];\r\n+        if (key is List) {\r\n+          String filename = key[0];\r\n+          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n+          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n+          hexStrings.addAll(convertBitmapToLEDHex(imageData, true));\r\n+        } else {\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          hexStrings.addAll(hs);\r\n+        }\r\n+      }\r\n+      lastMatchEnd = match.end;\r\n     }\r\n+\r\n+    // Process any remaining text after the last token.\r\n+    if (lastMatchEnd < message.length) {\r\n+      final textSegment = message.substring(lastMatchEnd);\r\n+      for (int i = 0; i < textSegment.length; i++) {\r\n+        if (converter.charCodes.containsKey(textSegment[i])) {\r\n+          hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n+        }\r\n+      }\r\n+    }\r\n+\r\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Renders text into multiple 11x44 matrices, splitting as needed.\r\n-  Future<List<List<List<bool>>>> renderTextToMatrices(\r\n+  /// Renders the given [message] using the provided [textStyle] onto an offscreen\r\n+  /// canvas and converts it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n-    int scale = 10,\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n   }) async {\r\n-    List<List<List<bool>>> allMatrices = [];\r\n-    // Render entire text into a single wide matrix\r\n-    List<List<bool>> fullMatrix = await _renderFullMatrix(message, textStyle, scale);\r\n-    // Split into 44-column chunks\r\n-    int cols = fullMatrix.isNotEmpty ? fullMatrix[0].length : 0;\r\n-    for (int startCol = 0; startCol < cols; startCol += 44) {\r\n-      int endCol = min(startCol + 44, cols);\r\n-      List<List<bool>> chunk = [];\r\n-      for (int row = 0; row < 11; row++) {\r\n-        List<bool> chunkRow = [];\r\n-        for (int col = startCol; col < endCol; col++) {\r\n-          chunkRow.add(fullMatrix[row][col]);\r\n-        }\r\n-        // Pad with false (off) if chunk is less than 44 columns\r\n-        while (chunkRow.length < 44) {\r\n-          chunkRow.add(false);\r\n-        }\r\n-        chunk.add(chunkRow);\r\n-      }\r\n-      allMatrices.add(chunk);\r\n-    }\r\n-    return allMatrices;\r\n-  }\r\n+    // Calculate canvas size.\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  Future<List<List<bool>>> _renderFullMatrix(\r\n-      String message, TextStyle textStyle, int scale) async {\r\n-    final textPainter = TextPainter(\r\n-      text: TextSpan(text: message, style: textStyle),\r\n+    // Create a PictureRecorder and Canvas.\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+\r\n+    // Fill background with white.\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+\r\n+    // Prepare the text painter.\r\n+    // Multiply fontSize by scale to maintain sharpness.\r\n+    final TextPainter textPainter = TextPainter(\r\n+      text: TextSpan(\r\n+        text: message,\r\n+        style: textStyle.copyWith(\r\n+            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n+      ),\r\n       textDirection: TextDirection.ltr,\r\n     );\r\n-    textPainter.layout();\r\n-    double textWidth = textPainter.width;\r\n-    double textHeight = textPainter.height;\r\n-    int cols = (textWidth / scale).ceil();\r\n-    int rows = 11;\r\n+    textPainter.layout(maxWidth: width.toDouble());\r\n+    // Center the text on the canvas.\r\n+    final Offset offset = Offset(\r\n+      (width - textPainter.width) / 2,\r\n+      (height - textPainter.height) / 2,\r\n+    );\r\n+    textPainter.paint(canvas, offset);\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(recorder,\r\n-        Rect.fromLTWH(0, 0, cols * scale.toDouble(), rows * scale.toDouble()));\r\n-    // Draw background\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, cols * scale.toDouble(), rows * scale.toDouble()),\r\n-        Paint()..color = Colors.white);\r\n-    // Draw text centered vertically\r\n-    textPainter.paint(\r\n-        canvas,\r\n-        Offset(\r\n-            0, (rows * scale - textHeight) / 2)); // Left-aligned horizontally\r\n-\r\n-    final ui.Image image = await recorder.endRecording().toImage(\r\n-        cols * scale, rows * scale);\r\n+    // End recording and get the image.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) return List.generate(rows, (_) => List.filled(cols, false));\r\n \r\n-    Uint8List data = byteData.buffer.asUint8List();\r\n-    List<List<bool>> matrix = [];\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n     for (int row = 0; row < rows; row++) {\r\n-      List<bool> matrixRow = [];\r\n\\ No newline at end of file\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n+        int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * cols * scale + pixelX) * 4;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n             if (index + 3 < data.length) {\r\n+              // Calculate brightness using average of R, G, B channels.\r\n               int r = data[index];\r\n               int g = data[index + 1];\r\n               int b = data[index + 2];\r\n-              sum += (r + g + b) ~/ 3;\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n             }\r\n           }\r\n         }\r\n-        matrixRow.add((sum / (scale * scale)) < 128);\r\n+        double avgBrightness = sum / count;\r\n+        // Use a threshold of 128 to decide if the LED is on (true) or off (false).\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix.add(matrixRow);\r\n     }\r\n     return matrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n-    int firstNonEmpty = -1;\r\n-    int lastNonEmpty = -1;\r\n-\r\n-    // Find first and last non-empty columns if trimming\r\n-    if (trim) {\r\n-      for (int j = 0; j < width; j++) {\r\n+    int finalSum = 0;\r\n+    for (int j = 0; j < width; j++) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n         for (int i = 0; i < height; i++) {\r\n-          if (image[i][j] == 1) {\r\n-            if (firstNonEmpty == -1) firstNonEmpty = j;\r\n-            lastNonEmpty = j;\r\n-          }\r\n+          image[i][j] = -1;\r\n         }\r\n+      } else {\r\n+        finalSum += j;\r\n+        break;\r\n       }\r\n-      if (firstNonEmpty == -1) return []; // All empty\r\n-    } else {\r\n-      firstNonEmpty = 0;\r\n-      lastNonEmpty = width - 1;\r\n     }\r\n-\r\n-    int contentWidth = lastNonEmpty - firstNonEmpty + 1;\r\n-    int diff = (8 - (contentWidth % 8)) % 8;\r\n+    for (int j = width - 1; j >= 0; j--) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += (height - j - 1);\r\n+        break;\r\n+      }\r\n+    }\r\n+    int diff = 0;\r\n+    if ((height - finalSum) % 8 > 0) {\r\n+      diff = 8 - (height - finalSum) % 8;\r\n+    }\r\n     int rOff = (diff / 2).floor();\r\n-    int lOff = diff - rOff;\r\n-\r\n-    List<List<int>> paddedImage = [];\r\n+    int lOff = (diff / 2).ceil();\r\n+    List<List<int>> list =\r\n+        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n     for (int i = 0; i < height; i++) {\r\n-      List<int> paddedRow = [];\r\n-      // Add left padding\r\n-      paddedRow.addAll(List.filled(lOff, 0));\r\n-      // Add content\r\n-      for (int j = firstNonEmpty; j <= lastNonEmpty; j++) {\r\n-        paddedRow.add(image[i][j]);\r\n+      int k = 0;\r\n+      for (int j = 0; j < rOff; j++) {\r\n+        list[i][k++] = 0;\r\n       }\r\n-      // Add right padding\r\n-      paddedRow.addAll(List.filled(rOff, 0));\r\n-      paddedImage.add(paddedRow);\r\n+      for (int j = 0; j < width; j++) {\r\n+        if (image[i][j] != -1) {\r\n+          list[i][k++] = image[i][j];\r\n+        }\r\n+      }\r\n+      for (int j = 0; j < lOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n     }\r\n-\r\n-    int totalBlocks = (paddedImage[0].length + 7) ~/ 8;\r\n-    List<String> hexStrings = [];\r\n-    for (int block = 0; block < totalBlocks; block++) {\r\n-      StringBuffer blockHex = StringBuffer();\r\n-      for (int row = 0; row < height; row++) {\r\n-        StringBuffer rowBits = StringBuffer();\r\n-        for (int col = block * 8; col < (block + 1) * 8; col++) {\r\n-          if (col < paddedImage[row].length) {\r\n-            rowBits.write(paddedImage[row][col]);\r\n-          } else {\r\n-            rowBits.write(0);\r\n-          }\r\n+    logger.d(\"Padded image: $list\");\r\n+    List<String> allHexs = [];\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+      StringBuffer lineHex = StringBuffer();\r\n+      for (int k = 0; k < height; k++) {\r\n+        StringBuffer stBuilder = StringBuffer();\r\n+        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n+          stBuilder.write(list[k][j]);\r\n         }\r\n-        String hex = int.parse(rowBits.toString(), radix: 2)\r\n+        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n-        blockHex.write(hex);\r\n+        lineHex.write(hex);\r\n       }\r\n-      hexStrings.add(blockHex.toString());\r\n+      allHexs.add(lineHex.toString());\r\n     }\r\n-    return hexStrings;\r\n+    return allHexs;\r\n   }\r\n \r\n   static String invertHex(String hex) {\r\n-    return hex.split('').map((c) {\r\n-      int value = int.parse(c, radix: 16);\r\n-      return (15 - value).toRadixString(16).toUpperCase();\r\n-    }).join();\r\n+    StringBuffer invertedHex = StringBuffer();\r\n+    for (int i = 0; i < hex.length; i++) {\r\n+      String invertedHexDigit =\r\n+          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+      invertedHex.write(invertedHexDigit);\r\n+    }\r\n+    return invertedHex.toString();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    // Existing padding logic if needed\r\n-    return hexString;\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n+    for (int i = 0; i < hexArray.length; i++) {\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n+    }\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\n+}\r\n"
                },
                {
                    "date": 1739989794364,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -17,155 +24,203 @@\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n-  /// Converts a given [message] into a list of LED hex strings.\r\n-  ///\r\n-  /// Tokens in the message should match the format `<XNNY>` (for example,\r\n-  /// `<I12!>`), where the two digits (`NN`) are parsed as an index. Any text\r\n-  /// outside of such tokens is converted via [converter.charCodes].\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n-    // Use a regex to match tokens of the format: <XNNY>\r\n-    final RegExp tokenRegExp = RegExp(r'<(.)(\\d{2})(.)>');\r\n-    int lastMatchEnd = 0;\r\n-\r\n-    // Process each token found in the message.\r\n-    for (final match in tokenRegExp.allMatches(message)) {\r\n-      // Process text before this token.\r\n-      if (match.start > lastMatchEnd) {\r\n-        final textSegment = message.substring(lastMatchEnd, match.start);\r\n-        for (int i = 0; i < textSegment.length; i++) {\r\n-          if (converter.charCodes.containsKey(textSegment[i])) {\r\n-            hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n-          }\r\n-        }\r\n-      }\r\n-\r\n-      // Extract token details.\r\n-      String? token = match.group(0);\r\n-      String? indexStr = match.group(2); // two-digit number\r\n-      if (token != null && indexStr != null) {\r\n-        int index = int.parse(indexStr);\r\n+    for (int x = 0; x < message.length; x++) {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n+        int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n           List<List<int>> imageData =\r\n               image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings.addAll(convertBitmapToLEDHex(imageData, true));\r\n+          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n+          x += 5;\r\n         } else {\r\n           List<String> hs =\r\n               await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n+          x += 5;\r\n         }\r\n-      }\r\n-      lastMatchEnd = match.end;\r\n-    }\r\n-\r\n-    // Process any remaining text after the last token.\r\n-    if (lastMatchEnd < message.length) {\r\n-      final textSegment = message.substring(lastMatchEnd);\r\n-      for (int i = 0; i < textSegment.length; i++) {\r\n-        if (converter.charCodes.containsKey(textSegment[i])) {\r\n-          hexStrings.add(converter.charCodes[textSegment[i]]!);\r\n+      } else {\r\n+        if (converter.charCodes.containsKey(message[x])) {\r\n+          hexStrings.add(converter.charCodes[message[x]]!);\r\n         }\r\n       }\r\n     }\r\n-\r\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Renders the given [message] using the provided [textStyle] onto an offscreen\r\n-  /// canvas and converts it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Calculate canvas size.\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas.\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize*0.78; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n+    }\r\n+  }\r\n \r\n-    // Prepare the text painter.\r\n-    // Multiply fontSize by scale to maintain sharpness.\r\n-    final TextPainter textPainter = TextPainter(\r\n-      text: TextSpan(\r\n-        text: message,\r\n-        style: textStyle.copyWith(\r\n-            color: Colors.black, fontSize: (textStyle.fontSize ?? 16) * scale),\r\n-      ),\r\n-      textDirection: TextDirection.ltr,\r\n-    );\r\n-    textPainter.layout(maxWidth: width.toDouble());\r\n-    // Center the text on the canvas.\r\n-    final Offset offset = Offset(\r\n-      (width - textPainter.width) / 2,\r\n-      (height - textPainter.height) / 2,\r\n-    );\r\n-    textPainter.paint(canvas, offset);\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // End recording and get the image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+        }\r\n+      }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample: For each cell (scale x scale block) compute average brightness.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              // Calculate brightness using average of R, G, B channels.\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        // Use a threshold of 128 to decide if the LED is on (true) or off (false).\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -257,5 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739989822576,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -94,9 +94,9 @@\n   // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n   if (scaledFontSize > canvasHeight * 0.9) {\r\n     scaledFontSize = canvasHeight * 0.9;\r\n   }\r\n-  double emojiWidth = scaledFontSize*0.78; // emojis are square, matching text height\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n   double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n   // Measure one copy of the message (in scaled pixels)\r\n   double singleCopyWidth = 0;\r\n"
                },
                {
                    "date": 1739990179233,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,22 +1,15 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -27,10 +20,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,168 +51,130 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), \r\n+          isEmoji: false\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n-      }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n-    }\r\n-  }\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize*0.734; \r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n+        double offsetY = 0 - textPainter.height / 3.8;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Convert canvas to image for downsampling\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -312,5 +266,10 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+}\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1739990441658,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -52,54 +52,78 @@\n     return hexStrings;\r\n   }\r\n \r\n   /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n+  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n+  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n+    int xOffset = 0, // new parameter: horizontal offset (in pixels) for animation\r\n   }) async {\r\n-    // Parse the message for emoji placeholders\r\n+    // Parse the message for emoji placeholders in the format <<00>>\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), \r\n-          isEmoji: false\r\n-        ));\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+    // Calculate total width required to draw the entire message.\r\n+    // We assume emoji width is proportional to the font size.\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout();\r\n+        totalWidth += textPainter.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n+      }\r\n+    }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n+    // The LED display is defined by (cols * scale). We create a canvas that is wide enough\r\n+    // to hold the entire message.\r\n+    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+    final int canvasHeight = rows * scale;\r\n+\r\n+    // Create a PictureRecorder and Canvas with the full width.\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n     // Fill background with white\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n \r\n     double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize*0.734; \r\n+    double centerY = canvasHeight / 2.0;\r\n \r\n+    // Draw each segment onto the canvas.\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         // Paint text\r\n         final TextPainter textPainter = TextPainter(\r\n@@ -109,10 +133,11 @@\n                 color: Colors.black, fontSize: fontSize),\r\n           ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = 0 - textPainter.height / 3.8;\r\n+        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+        // Adjust Y offset so text is vertically centered.\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n         textPainter.paint(canvas, Offset(currentX, offsetY));\r\n         currentX += textPainter.width;\r\n       } else {\r\n         // Paint emoji\r\n@@ -134,37 +159,42 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Convert canvas to image for downsampling\r\n+    // End recording and convert the picture to an image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n     if (byteData == null) {\r\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample to LED matrix\r\n+    // Downsample to LED matrix.\r\n+    // Instead of always taking the left-most part (which clipped extra text),\r\n+    // we now sample starting from the given [xOffset].\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n+            int pixelX = xOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n+            // If we're outside the drawn image, assume a white pixel.\r\n+            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+              int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n               int r = data[index], g = data[index + 1], b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n               sum += brightness;\r\n               count++;\r\n+            } else {\r\n+              sum += 255;\r\n+              count++;\r\n             }\r\n           }\r\n         }\r\n         double avgBrightness = sum / count;\r\n@@ -267,9 +297,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n\\ No newline at end of file\n-}\n+}\r\n"
                },
                {
                    "date": 1739990606382,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -55,156 +55,151 @@\n   /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n   /// message is longer than the LED display, the extra text is not clipped – instead,\r\n   /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    int xOffset = 0, // new parameter: horizontal offset (in pixels) for animation\r\n-  }) async {\r\n-    // Parse the message for emoji placeholders in the format <<00>>\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n-    }\r\n-    if (lastMatchEnd < message.length) {\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n+}) async {\r\n+  // Parse the message for emoji placeholders in the format <<00>>\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n       segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd), isEmoji: false));\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(\r\n+        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Calculate total width required to draw the entire message.\r\n-    // We assume emoji width is proportional to the font size.\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout();\r\n-        totalWidth += textPainter.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n-      }\r\n+  // Capture the emoji keys once to ensure consistent ordering.\r\n+  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n+\r\n+  // Calculate total width required to draw the entire message.\r\n+  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double emojiWidth = fontSize * 0.734;\r\n+  double totalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout();\r\n+      totalWidth += textPainter.width;\r\n+    } else {\r\n+      totalWidth += emojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // The LED display is defined by (cols * scale). We create a canvas that is wide enough\r\n-    // to hold the entire message.\r\n-    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-    final int canvasHeight = rows * scale;\r\n+  // Create a canvas wide enough to hold the entire message.\r\n+  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+  final int canvasHeight = rows * scale;\r\n \r\n-    // Create a PictureRecorder and Canvas with the full width.\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n+  // Fill background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    double currentX = 0;\r\n-    double centerY = canvasHeight / 2.0;\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n \r\n-    // Draw each segment onto the canvas.\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-        // Adjust Y offset so text is vertically centered.\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+  // Draw each segment onto the canvas.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      // Paint text.\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+      double offsetY = centerY - textPainter.height / 2;\r\n+      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += textPainter.width;\r\n+    } else {\r\n+      // Paint emoji.\r\n+      int index = int.parse(segment.text);\r\n+      if (index < emojiKeys.length) {\r\n+        var key = emojiKeys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // End recording and convert the picture to an image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  // Convert canvas to image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample to LED matrix.\r\n-    // Instead of always taking the left-most part (which clipped extra text),\r\n-    // we now sample starting from the given [xOffset].\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = xOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            // If we're outside the drawn image, assume a white pixel.\r\n-            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-              int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            } else {\r\n-              sum += 255;\r\n-              count++;\r\n-            }\r\n+  // Downsample to LED matrix using the provided xOffset.\r\n+  List<List<bool>> matrix =\r\n+      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = xOffset + col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n+            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n+          } else {\r\n+            sum += 255;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -302,5 +297,5 @@\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n-}\n\\ No newline at end of file\n+}\r\n"
                },
                {
                    "date": 1740042138604,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -55,151 +55,151 @@\n   /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n   /// message is longer than the LED display, the extra text is not clipped – instead,\r\n   /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n-}) async {\r\n-  // Parse the message for emoji placeholders in the format <<00>>\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    int xOffset = 0, // horizontal offset (in pixels) for animation\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders in the format <<00>>\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(\r\n-        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Capture the emoji keys once to ensure consistent ordering.\r\n-  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n+    // Capture the emoji keys once to ensure consistent ordering.\r\n+    final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n \r\n-  // Calculate total width required to draw the entire message.\r\n-  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double emojiWidth = fontSize * 0.734;\r\n-  double totalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter textPainter = TextPainter(\r\n-        text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      textPainter.layout();\r\n-      totalWidth += textPainter.width;\r\n-    } else {\r\n-      totalWidth += emojiWidth;\r\n+    // Calculate total width required to draw the entire message.\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+          maxLines: 1, // Ensure text is rendered in a single line\r\n+        );\r\n+        textPainter.layout();\r\n+        totalWidth += textPainter.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n+      }\r\n     }\r\n-  }\r\n \r\n-  // Create a canvas wide enough to hold the entire message.\r\n-  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-  final int canvasHeight = rows * scale;\r\n+    // Create a canvas wide enough to hold the entire message.\r\n+    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+    final int canvasHeight = rows * scale;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(recorder,\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Fill background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    // Fill background with white.\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n+    double currentX = 0;\r\n+    double centerY = canvasHeight / 2.0;\r\n \r\n-  // Draw each segment onto the canvas.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      // Paint text.\r\n-      final TextPainter textPainter = TextPainter(\r\n-        text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-      double offsetY = centerY - textPainter.height / 2;\r\n-      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += textPainter.width;\r\n-    } else {\r\n-      // Paint emoji.\r\n-      int index = int.parse(segment.text);\r\n-      if (index < emojiKeys.length) {\r\n-        var key = emojiKeys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n+    // Draw each segment onto the canvas.\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text.\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji.\r\n+        int index = int.parse(segment.text);\r\n+        if (index < emojiKeys.length) {\r\n+          var key = emojiKeys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n     }\r\n-  }\r\n \r\n-  // Convert canvas to image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    // Convert canvas to image.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample to LED matrix using the provided xOffset.\r\n-  List<List<bool>> matrix =\r\n-      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = xOffset + col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n-            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n-          } else {\r\n-            sum += 255;\r\n-            count++;\r\n+    // Downsample to LED matrix using the provided xOffset.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = xOffset + col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+              int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            } else {\r\n+              sum += 255;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740042374898,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,29 +63,27 @@\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n     int xOffset = 0, // horizontal offset (in pixels) for animation\r\n   }) async {\r\n-    // Parse the message for emoji placeholders in the format <<00>>\r\n+    // Parse the message for emoji placeholders\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false\r\n+        ));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Capture the emoji keys once to ensure consistent ordering.\r\n-    final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n-\r\n     // Calculate total width required to draw the entire message.\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n     double totalWidth = 0;\r\n@@ -140,10 +138,11 @@\n         currentX += textPainter.width;\r\n       } else {\r\n         // Paint emoji.\r\n         int index = int.parse(segment.text);\r\n-        if (index < emojiKeys.length) {\r\n-          var key = emojiKeys[index];\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n           Uint8List? emojiBytes = controllerData.imageCache[key];\r\n           if (emojiBytes != null) {\r\n             ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n@@ -157,9 +156,9 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Convert canvas to image.\r\n+    // Convert canvas to image for downsampling\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -180,9 +179,9 @@\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = xOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n             if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-              int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n+              int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n               int r = data[index], g = data[index + 1], b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n               sum += brightness;\r\n               count++;\r\n"
                },
                {
                    "date": 1740042989318,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -51,19 +51,16 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n-  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n-  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n+  /// Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n-    int xOffset = 0, // horizontal offset (in pixels) for animation\r\n   }) async {\r\n     // Parse the message for emoji placeholders\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n@@ -82,63 +79,81 @@\n     if (lastMatchEnd < message.length) {\r\n       segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate total width required to draw the entire message.\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-          maxLines: 1, // Ensure text is rendered in a single line\r\n-        );\r\n-        textPainter.layout();\r\n-        totalWidth += textPainter.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n-      }\r\n-    }\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-    // Create a canvas wide enough to hold the entire message.\r\n-    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-    final int canvasHeight = rows * scale;\r\n-\r\n+    // Create a PictureRecorder and Canvas\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(recorder,\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-    // Fill background with white.\r\n+    // Fill background with white\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n     double currentX = 0;\r\n-    double centerY = canvasHeight / 2.0;\r\n+    double centerY = height / 2.0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Draw each segment onto the canvas.\r\n+    List<List<bool>> completeMatrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n-        // Paint text.\r\n+        // Paint text\r\n         final TextPainter textPainter = TextPainter(\r\n           text: TextSpan(\r\n             text: segment.text,\r\n             style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n           ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n-        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n+        textPainter.layout(maxWidth: width.toDouble());\r\n+\r\n+        // Check if the text fits within the display width\r\n+        if (textPainter.width > width) {\r\n+          // Split the text into smaller segments that fit within the display width\r\n+          String text = segment.text;\r\n+          while (text.isNotEmpty) {\r\n+            textPainter.text = TextSpan(\r\n+              text: text,\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+            );\r\n+            textPainter.layout(maxWidth: width.toDouble());\r\n+\r\n+            if (textPainter.width <= width) {\r\n+              double offsetY = centerY - textPainter.height / 2;\r\n+              textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+              currentX += textPainter.width;\r\n+              text = text.substring(textPainter.text!.toPlainText().length);\r\n+            } else {\r\n+              // Find the last word that fits within the display width\r\n+              String remainingText = text;\r\n+              while (textPainter.width > width && remainingText.isNotEmpty) {\r\n+                remainingText = remainingText.substring(0, remainingText.length - 1);\r\n+                textPainter.text = TextSpan(\r\n+                  text: remainingText,\r\n+                  style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+                );\r\n+                textPainter.layout(maxWidth: width.toDouble());\r\n+              }\r\n+              double offsetY = centerY - textPainter.height / 2;\r\n+              textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+              currentX += textPainter.width;\r\n+              text = text.substring(remainingText.length);\r\n+            }\r\n+          }\r\n+        } else {\r\n+          double offsetY = centerY - textPainter.height / 2;\r\n+          textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += textPainter.width;\r\n+        }\r\n       } else {\r\n-        // Paint emoji.\r\n+        // Paint emoji\r\n         int index = int.parse(segment.text);\r\n         List keys = controllerData.imageCache.keys.toList();\r\n         if (index < keys.length) {\r\n           var key = keys[index];\r\n@@ -154,49 +169,54 @@\n             currentX += emojiWidth;\r\n           }\r\n         }\r\n       }\r\n-    }\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+      // Convert canvas to image for downsampling\r\n+      final ui.Picture picture = recorder.endRecording();\r\n+      final ui.Image image = await picture.toImage(width, height);\r\n+      final ByteData? byteData =\r\n+          await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+      if (byteData == null) {\r\n+        throw Exception(\"Failed to convert image to byte data.\");\r\n+      }\r\n+      final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample to LED matrix using the provided xOffset.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = xOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-              int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            } else {\r\n-              sum += 255;\r\n-              count++;\r\n+      // Downsample to LED matrix\r\n+      List<List<bool>> matrix =\r\n+          List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+      for (int row = 0; row < rows; row++) {\r\n+        for (int col = 0; col < cols; col++) {\r\n+          int sum = 0;\r\n+          int count = 0;\r\n+          for (int y = 0; y < scale; y++) {\r\n+            for (int x = 0; x < scale; x++) {\r\n+              int pixelX = col * scale + x;\r\n+              int pixelY = row * scale + y;\r\n+              int index = (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+              if (index + 3 < data.length) {\r\n+                int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+                int brightness = ((r + g + b) / 3).round();\r\n+                sum += brightness;\r\n+                count++;\r\n+              }\r\n             }\r\n           }\r\n+          double avgBrightness = sum / count;\r\n+          matrix[row][col] = avgBrightness < 128;\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+\r\n+      // Combine the matrices\r\n+      for (int row = 0; row < rows; row++) {\r\n+        for (int col = 0; col < cols; col++) {\r\n+          completeMatrix[row][col] |= matrix[row][col];\r\n+        }\r\n+      }\r\n     }\r\n-    return matrix;\r\n+\r\n+    return completeMatrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1740043245321,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -20,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -51,27 +59,27 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the width of one copy of the rendered message plus a gap,\r\n+  /// and the message is repeated so that there is no empty padding at the borders.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n   }) async {\r\n-    // Parse the message for emoji placeholders\r\n+    // Parse the message into segments (plain text and emoji)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false\r\n-        ));\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n@@ -79,144 +87,142 @@\n     if (lastMatchEnd < message.length) {\r\n       segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+    // Determine the scaled font size and set emoji dimensions.\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n+    // Ensure the text fits vertically within the canvas (leaving a tiny margin)\r\n+    if (scaledFontSize > canvasHeight * 0.9) {\r\n+      scaledFontSize = canvasHeight * 0.9;\r\n+    }\r\n+    double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+    double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n+    // Measure one copy of the message (in scaled pixels)\r\n+    double singleCopyWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        singleCopyWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segments[i].text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        singleCopyWidth += tp.width;\r\n+      }\r\n+    }\r\n+\r\n+    // fixedWidth is the display width (in scaled pixels)\r\n+    int fixedWidth = cols * scale;\r\n+    // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+    int gapWidth = fixedWidth;\r\n+    // Determine the number of copies to repeat.\r\n+    // Here we use 2 copies so that the canvas covers at least one full cycle.\r\n+    int numCopies = 2;\r\n+    // Canvas width is the total width of all copies plus the gap between each copy.\r\n+    int canvasWidth = ((singleCopyWidth + gapWidth) * numCopies).ceil();\r\n+\r\n+    double centerY = canvasHeight / 2;\r\n+\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill background with white\r\n+    // Fill the background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    List<List<bool>> completeMatrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: width.toDouble());\r\n-\r\n-        // Check if the text fits within the display width\r\n-        if (textPainter.width > width) {\r\n-          // Split the text into smaller segments that fit within the display width\r\n-          String text = segment.text;\r\n-          while (text.isNotEmpty) {\r\n-            textPainter.text = TextSpan(\r\n-              text: text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-            );\r\n-            textPainter.layout(maxWidth: width.toDouble());\r\n-\r\n-            if (textPainter.width <= width) {\r\n-              double offsetY = centerY - textPainter.height / 2;\r\n-              textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-              currentX += textPainter.width;\r\n-              text = text.substring(textPainter.text!.toPlainText().length);\r\n-            } else {\r\n-              // Find the last word that fits within the display width\r\n-              String remainingText = text;\r\n-              while (textPainter.width > width && remainingText.isNotEmpty) {\r\n-                remainingText = remainingText.substring(0, remainingText.length - 1);\r\n-                textPainter.text = TextSpan(\r\n-                  text: remainingText,\r\n-                  style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-                );\r\n-                textPainter.layout(maxWidth: width.toDouble());\r\n-              }\r\n-              double offsetY = centerY - textPainter.height / 2;\r\n-              textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-              currentX += textPainter.width;\r\n-              text = text.substring(remainingText.length);\r\n+    // Draw repeated copies of the message.\r\n+    for (int copy = 0; copy < numCopies; copy++) {\r\n+      double currentX = copy * (singleCopyWidth + gapWidth);\r\n+      // Draw each segment of the message.\r\n+      for (int i = 0; i < segments.length; i++) {\r\n+        final _Segment segment = segments[i];\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+                text: segment.text,\r\n+                style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          // Compute descent so that letters with descenders are fully visible.\r\n+          var metrics = tp.computeLineMetrics();\r\n+          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+          // Offset the text so that the bottom of the glyphs is centered.\r\n+          double offsetY = centerY - (tp.height - descent) / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                  targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n             }\r\n           }\r\n-        } else {\r\n-          double offsetY = centerY - textPainter.height / 2;\r\n-          textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += textPainter.width;\r\n-        }\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n+          currentX += emojiWidth;\r\n+          if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+            currentX += emojiGap;\r\n           }\r\n         }\r\n       }\r\n+      // A gap is automatically left after each copy because of our canvas width calculation.\r\n+    }\r\n \r\n-      // Convert canvas to image for downsampling\r\n-      final ui.Picture picture = recorder.endRecording();\r\n-      final ui.Image image = await picture.toImage(width, height);\r\n-      final ByteData? byteData =\r\n-          await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    // Finalize the drawing and convert the result to an image.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-      if (byteData == null) {\r\n-        throw Exception(\"Failed to convert image to byte data.\");\r\n-      }\r\n-      final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-      // Downsample to LED matrix\r\n-      List<List<bool>> matrix =\r\n-          List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-      for (int row = 0; row < rows; row++) {\r\n-        for (int col = 0; col < cols; col++) {\r\n-          int sum = 0;\r\n-          int count = 0;\r\n-          for (int y = 0; y < scale; y++) {\r\n-            for (int x = 0; x < scale; x++) {\r\n-              int pixelX = col * scale + x;\r\n-              int pixelY = row * scale + y;\r\n-              int index = (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-              if (index + 3 < data.length) {\r\n-                int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-                int brightness = ((r + g + b) / 3).round();\r\n-                sum += brightness;\r\n-                count++;\r\n-              }\r\n+    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n             }\r\n           }\r\n-          double avgBrightness = sum / count;\r\n-          matrix[row][col] = avgBrightness < 128;\r\n         }\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n-\r\n-      // Combine the matrices\r\n-      for (int row = 0; row < rows; row++) {\r\n-        for (int col = 0; col < cols; col++) {\r\n-          completeMatrix[row][col] |= matrix[row][col];\r\n-        }\r\n-      }\r\n     }\r\n-\r\n-    return completeMatrix;\r\n+    return matrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n@@ -311,10 +317,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740043362301,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,22 +1,15 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -59,167 +52,129 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the width of one copy of the rendered message plus a gap,\r\n-  /// and the message is repeated so that there is no empty padding at the borders.\r\n+  /// Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n   }) async {\r\n-    // Parse the message into segments (plain text and emoji)\r\n+    // Parse the message for emoji placeholders\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Determine the scaled font size and set emoji dimensions.\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n-    // Ensure the text fits vertically within the canvas (leaving a tiny margin)\r\n-    if (scaledFontSize > canvasHeight * 0.9) {\r\n-      scaledFontSize = canvasHeight * 0.9;\r\n-    }\r\n-    double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-    double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-    // Measure one copy of the message (in scaled pixels)\r\n-    double singleCopyWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        singleCopyWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segments[i].text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        singleCopyWidth += tp.width;\r\n-      }\r\n-    }\r\n-\r\n-    // fixedWidth is the display width (in scaled pixels)\r\n-    int fixedWidth = cols * scale;\r\n-    // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-    int gapWidth = fixedWidth;\r\n-    // Determine the number of copies to repeat.\r\n-    // Here we use 2 copies so that the canvas covers at least one full cycle.\r\n-    int numCopies = 2;\r\n-    // Canvas width is the total width of all copies plus the gap between each copy.\r\n-    int canvasWidth = ((singleCopyWidth + gapWidth) * numCopies).ceil();\r\n-\r\n-    double centerY = canvasHeight / 2;\r\n-\r\n+    // Create a PictureRecorder and Canvas\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-    // Fill the background with white.\r\n+    // Fill background with white\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-    // Draw repeated copies of the message.\r\n-    for (int copy = 0; copy < numCopies; copy++) {\r\n-      double currentX = copy * (singleCopyWidth + gapWidth);\r\n-      // Draw each segment of the message.\r\n-      for (int i = 0; i < segments.length; i++) {\r\n-        final _Segment segment = segments[i];\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-                text: segment.text,\r\n-                style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          // Compute descent so that letters with descenders are fully visible.\r\n-          var metrics = tp.computeLineMetrics();\r\n-          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-          // Offset the text so that the bottom of the glyphs is centered.\r\n-          double offsetY = centerY - (tp.height - descent) / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                  targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            }\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n           }\r\n-          currentX += emojiWidth;\r\n-          if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-            currentX += emojiGap;\r\n-          }\r\n         }\r\n       }\r\n-      // A gap is automatically left after each copy because of our canvas width calculation.\r\n     }\r\n \r\n-    // Finalize the drawing and convert the result to an image.\r\n+    // Convert canvas to image for downsampling\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+\r\n     if (byteData == null) {\r\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n     for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n+              sum += brightness;\r\n+              count++;\r\n             }\r\n           }\r\n         }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n@@ -317,4 +272,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740043549766,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,133 +53,154 @@\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message for emoji placeholders\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message for emoji placeholders.\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+          isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(\r\n+        text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n+\r\n+  final int canvasWidth = cols * scale;\r\n+  final int canvasHeight = rows * scale;\r\n+\r\n+  // First, measure the total width needed with the base font size.\r\n+  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double baseEmojiWidth = baseFontSize * 0.734;\r\n+  double unscaledTotalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      unscaledTotalWidth += tp.width;\r\n+    } else {\r\n+      unscaledTotalWidth += baseEmojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  // If the text is wider than the canvas, calculate a scaling factor.\r\n+  double scaleFactor = 1.0;\r\n+  if (unscaledTotalWidth > canvasWidth) {\r\n+    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Adjust font and emoji sizes.\r\n+  double fontSize = baseFontSize * scaleFactor;\r\n+  double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Create an offscreen canvas.\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+  // Fill the background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n+\r\n+  // Render each segment.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      double offsetY = centerY - tp.height / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(\r\n+              emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(),\r\n+              targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  // Convert canvas to image and downsample.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample to an LED matrix.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index],\r\n+                g = data[index + 1],\r\n+                b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n-\r\n+  return matrix;\r\n+}\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740043566091,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,154 +53,133 @@\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message for emoji placeholders.\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false));\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(\r\n-        text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n-\r\n-  final int canvasWidth = cols * scale;\r\n-  final int canvasHeight = rows * scale;\r\n-\r\n-  // First, measure the total width needed with the base font size.\r\n-  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double baseEmojiWidth = baseFontSize * 0.734;\r\n-  double unscaledTotalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      unscaledTotalWidth += tp.width;\r\n-    } else {\r\n-      unscaledTotalWidth += baseEmojiWidth;\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n-  }\r\n \r\n-  // If the text is wider than the canvas, calculate a scaling factor.\r\n-  double scaleFactor = 1.0;\r\n-  if (unscaledTotalWidth > canvasWidth) {\r\n-    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n-  }\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // Adjust font and emoji sizes.\r\n-  double fontSize = baseFontSize * scaleFactor;\r\n-  double emojiWidth = fontSize * 0.734;\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n-  // Create an offscreen canvas.\r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // Fill the background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n-\r\n-  // Render each segment.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      double offsetY = centerY - tp.height / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(\r\n-              emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(),\r\n-              targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n     }\r\n-  }\r\n \r\n-  // Convert canvas to image and downsample.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    // Convert canvas to image for downsampling\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Downsample to an LED matrix.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index],\r\n-                g = data[index + 1],\r\n-                b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740043589215,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,132 +54,156 @@\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message for emoji placeholders\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message for emoji placeholders.\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+          isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(\r\n+        text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n+\r\n+  final int canvasWidth = cols * scale;\r\n+  final int canvasHeight = rows * scale;\r\n+\r\n+  // First, measure the total width needed with the base font size.\r\n+  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double baseEmojiWidth = baseFontSize * 0.734;\r\n+  double unscaledTotalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      unscaledTotalWidth += tp.width;\r\n+    } else {\r\n+      unscaledTotalWidth += baseEmojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  // If the text is wider than the canvas, calculate a scaling factor.\r\n+  double scaleFactor = 1.0;\r\n+  if (unscaledTotalWidth > canvasWidth) {\r\n+    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+  // Adjust font and emoji sizes.\r\n+  double fontSize = baseFontSize * scaleFactor;\r\n+  double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+  // Create an offscreen canvas.\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+  // Fill the background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n+\r\n+  // Render each segment.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      double offsetY = centerY - tp.height / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(\r\n+              emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(),\r\n+              targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  // Convert canvas to image and downsample.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample to an LED matrix.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index],\r\n+                g = data[index + 1],\r\n+                b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740043752136,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,8 +8,14 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -20,10 +26,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -52,158 +57,152 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message for emoji placeholders.\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false));\r\n+          isEmoji: false\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(\r\n-        text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  final int canvasWidth = cols * scale;\r\n-  final int canvasHeight = rows * scale;\r\n+    // Calculate canvas size\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n \r\n-  // First, measure the total width needed with the base font size.\r\n-  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double baseEmojiWidth = baseFontSize * 0.734;\r\n-  double unscaledTotalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    // Measure the total width of the message\r\n+    double totalWidth = 0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      unscaledTotalWidth += tp.width;\r\n-    } else {\r\n-      unscaledTotalWidth += baseEmojiWidth;\r\n+            style: textStyle.copyWith(fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout();\r\n+        totalWidth += textPainter.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n+      }\r\n     }\r\n-  }\r\n \r\n-  // If the text is wider than the canvas, calculate a scaling factor.\r\n-  double scaleFactor = 1.0;\r\n-  if (unscaledTotalWidth > canvasWidth) {\r\n-    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n-  }\r\n+    // Adjust canvas width to accommodate the entire message\r\n+    int canvasWidth = max(width, totalWidth.toInt());\r\n \r\n-  // Adjust font and emoji sizes.\r\n-  double fontSize = baseFontSize * scaleFactor;\r\n-  double emojiWidth = fontSize * 0.734;\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()));\r\n \r\n-  // Create an offscreen canvas.\r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // Fill the background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n \r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n-\r\n-  // Render each segment.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      double offsetY = centerY - tp.height / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(\r\n-              emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(),\r\n-              targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: canvasWidth.toDouble() - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n     }\r\n-  }\r\n \r\n-  // Convert canvas to image and downsample.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    // Convert canvas to image for downsampling\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Downsample to an LED matrix.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index],\r\n-                g = data[index + 1],\r\n-                b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -296,10 +295,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740043791181,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,14 +8,8 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -26,9 +20,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -57,152 +52,158 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message for emoji placeholders\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message for emoji placeholders.\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+          isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(\r\n+        text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+  final int canvasWidth = cols * scale;\r\n+  final int canvasHeight = rows * scale;\r\n \r\n-    // Measure the total width of the message\r\n-    double totalWidth = 0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  // First, measure the total width needed with the base font size.\r\n+  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double baseEmojiWidth = baseFontSize * 0.734;\r\n+  double unscaledTotalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout();\r\n-        totalWidth += textPainter.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n-      }\r\n+            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      unscaledTotalWidth += tp.width;\r\n+    } else {\r\n+      unscaledTotalWidth += baseEmojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // Adjust canvas width to accommodate the entire message\r\n-    int canvasWidth = max(width, totalWidth.toInt());\r\n+  // If the text is wider than the canvas, calculate a scaling factor.\r\n+  double scaleFactor = 1.0;\r\n+  if (unscaledTotalWidth > canvasWidth) {\r\n+    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n+  }\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()));\r\n+  // Adjust font and emoji sizes.\r\n+  double fontSize = baseFontSize * scaleFactor;\r\n+  double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()), bgPaint);\r\n+  // Create an offscreen canvas.\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n+  // Fill the background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n+\r\n+  // Render each segment.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: canvasWidth.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      double offsetY = centerY - tp.height / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(\r\n+              emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(),\r\n+              targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // Convert canvas to image for downsampling\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, height);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  // Convert canvas to image and downsample.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample to an LED matrix.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index],\r\n+                g = data[index + 1],\r\n+                b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -295,4 +296,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740043800509,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,156 +54,12 @@\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message for emoji placeholders.\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false));\r\n-    }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(\r\n-        text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n \r\n-  final int canvasWidth = cols * scale;\r\n-  final int canvasHeight = rows * scale;\r\n \r\n-  // First, measure the total width needed with the base font size.\r\n-  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double baseEmojiWidth = baseFontSize * 0.734;\r\n-  double unscaledTotalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      unscaledTotalWidth += tp.width;\r\n-    } else {\r\n-      unscaledTotalWidth += baseEmojiWidth;\r\n-    }\r\n-  }\r\n \r\n-  // If the text is wider than the canvas, calculate a scaling factor.\r\n-  double scaleFactor = 1.0;\r\n-  if (unscaledTotalWidth > canvasWidth) {\r\n-    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n-  }\r\n \r\n-  // Adjust font and emoji sizes.\r\n-  double fontSize = baseFontSize * scaleFactor;\r\n-  double emojiWidth = fontSize * 0.734;\r\n-\r\n-  // Create an offscreen canvas.\r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-\r\n-  // Fill the background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n-\r\n-  // Render each segment.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      double offsetY = centerY - tp.height / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(\r\n-              emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(),\r\n-              targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n-        }\r\n-      }\r\n-    }\r\n-  }\r\n-\r\n-  // Convert canvas to image and downsample.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-  // Downsample to an LED matrix.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index],\r\n-                g = data[index + 1],\r\n-                b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n-          }\r\n-        }\r\n-      }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n-    }\r\n-  }\r\n-  return matrix;\r\n-}\r\n-\r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740043845675,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,12 +54,156 @@\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message for emoji placeholders.\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false));\r\n+    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(\r\n+        text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n+  final int canvasWidth = cols * scale;\r\n+  final int canvasHeight = rows * scale;\r\n \r\n+  // First, measure the total width needed with the base font size.\r\n+  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double baseEmojiWidth = baseFontSize * 0.734;\r\n+  double unscaledTotalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      unscaledTotalWidth += tp.width;\r\n+    } else {\r\n+      unscaledTotalWidth += baseEmojiWidth;\r\n+    }\r\n+  }\r\n \r\n+  // If the text is wider than the canvas, calculate a scaling factor.\r\n+  double scaleFactor = 1.0;\r\n+  if (unscaledTotalWidth > canvasWidth) {\r\n+    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n+  }\r\n \r\n+  // Adjust font and emoji sizes.\r\n+  double fontSize = baseFontSize * scaleFactor;\r\n+  double emojiWidth = fontSize * 0.734;\r\n+\r\n+  // Create an offscreen canvas.\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+\r\n+  // Fill the background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n+\r\n+  // Render each segment.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      double offsetY = centerY - tp.height / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(\r\n+              emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(),\r\n+              targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n+        }\r\n+      }\r\n+    }\r\n+  }\r\n+\r\n+  // Convert canvas to image and downsample.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+  // Downsample to an LED matrix.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index],\r\n+                g = data[index + 1],\r\n+                b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n+          }\r\n+        }\r\n+      }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n+    }\r\n+  }\r\n+  return matrix;\r\n+}\r\n+\r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740043915534,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,8 +8,14 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -20,10 +26,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -54,156 +59,148 @@\n   }\r\n \r\n   /// Render the given text using the provided [textStyle] onto an offscreen\r\n   /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message for emoji placeholders.\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false));\r\n+          isEmoji: false\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(\r\n-        text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  final int canvasWidth = cols * scale;\r\n-  final int canvasHeight = rows * scale;\r\n+    // Calculate canvas size\r\n+    final int height = rows * scale;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-  // First, measure the total width needed with the base font size.\r\n-  double baseFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double baseEmojiWidth = baseFontSize * 0.734;\r\n-  double unscaledTotalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    // Measure the total width of the message\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: baseFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      unscaledTotalWidth += tp.width;\r\n-    } else {\r\n-      unscaledTotalWidth += baseEmojiWidth;\r\n+            style: textStyle.copyWith(fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout();\r\n+        totalWidth += textPainter.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n+      }\r\n     }\r\n-  }\r\n \r\n-  // If the text is wider than the canvas, calculate a scaling factor.\r\n-  double scaleFactor = 1.0;\r\n-  if (unscaledTotalWidth > canvasWidth) {\r\n-    scaleFactor = canvasWidth / unscaledTotalWidth;\r\n-  }\r\n+    // Adjust canvas width to accommodate the entire message\r\n+    int canvasWidth = max(cols * scale, totalWidth.toInt());\r\n \r\n-  // Adjust font and emoji sizes.\r\n-  double fontSize = baseFontSize * scaleFactor;\r\n-  double emojiWidth = fontSize * 0.734;\r\n+    // Create a PictureRecorder and Canvas\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()));\r\n \r\n-  // Create an offscreen canvas.\r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()), bgPaint);\r\n \r\n-  // Fill the background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n \r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n-\r\n-  // Render each segment.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        // Paint text\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      double offsetY = centerY - tp.height / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(\r\n-              emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(),\r\n-              targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n+            style: textStyle.copyWith(\r\n+                color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: canvasWidth.toDouble() - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        // Paint emoji\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n     }\r\n-  }\r\n \r\n-  // Convert canvas to image and downsample.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    // Convert canvas to image for downsampling\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-  // Downsample to an LED matrix.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index],\r\n-                g = data[index + 1],\r\n-                b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index =\r\n+                (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -296,10 +293,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740044013929,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,14 +8,8 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -26,9 +20,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -74,57 +69,38 @@\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false\r\n+          isEmoji: false,\r\n         ));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n     // Calculate canvas size\r\n+    final int width = cols * scale;\r\n     final int height = rows * scale;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure the total width of the message\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout();\r\n-        totalWidth += textPainter.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n-      }\r\n-    }\r\n-\r\n-    // Adjust canvas width to accommodate the entire message\r\n-    int canvasWidth = max(cols * scale, totalWidth.toInt());\r\n-\r\n     // Create a PictureRecorder and Canvas\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()));\r\n+        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n \r\n     // Fill background with white\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), height.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n \r\n     double currentX = 0;\r\n     double centerY = height / 2.0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         // Paint text\r\n@@ -135,9 +111,9 @@\n                 color: Colors.black, fontSize: fontSize),\r\n           ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n-        textPainter.layout(maxWidth: canvasWidth.toDouble() - currentX);\r\n+        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n         double offsetY = centerY - textPainter.height / 2;\r\n         textPainter.paint(canvas, Offset(currentX, offsetY));\r\n         currentX += textPainter.width;\r\n       } else {\r\n@@ -148,9 +124,10 @@\n           var key = keys[index];\r\n           Uint8List? emojiBytes = controllerData.imageCache[key];\r\n           if (emojiBytes != null) {\r\n             ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt());\r\n             ui.FrameInfo fi = await codec.getNextFrame();\r\n             ui.Image emojiImage = fi.image;\r\n             double offsetY = centerY - emojiWidth / 2;\r\n             Paint imagePaint = Paint();\r\n@@ -162,9 +139,9 @@\n     }\r\n \r\n     // Convert canvas to image for downsampling\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, height);\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n     if (byteData == null) {\r\n@@ -183,11 +160,13 @@\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = col * scale + x;\r\n             int pixelY = row * scale + y;\r\n             int index =\r\n-                (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n             if (index + 3 < data.length) {\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n               sum += brightness;\r\n               count++;\r\n             }\r\n@@ -293,4 +272,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740044026372,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -52,129 +59,167 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n+  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the width of one copy of the rendered message plus a gap,\r\n+  /// and the message is repeated so that there is no empty padding at the borders.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n   }) async {\r\n-    // Parse the message for emoji placeholders\r\n+    // Parse the message into segments (plain text and emoji)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate canvas size\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+    // Determine the scaled font size and set emoji dimensions.\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n+    // Ensure the text fits vertically within the canvas (leaving a tiny margin)\r\n+    if (scaledFontSize > canvasHeight * 0.9) {\r\n+      scaledFontSize = canvasHeight * 0.9;\r\n+    }\r\n+    double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+    double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Create a PictureRecorder and Canvas\r\n+    // Measure one copy of the message (in scaled pixels)\r\n+    double singleCopyWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        singleCopyWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segments[i].text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        singleCopyWidth += tp.width;\r\n+      }\r\n+    }\r\n+\r\n+    // fixedWidth is the display width (in scaled pixels)\r\n+    int fixedWidth = cols * scale;\r\n+    // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+    int gapWidth = fixedWidth;\r\n+    // Determine the number of copies to repeat.\r\n+    // Here we use 2 copies so that the canvas covers at least one full cycle.\r\n+    int numCopies = 2;\r\n+    // Canvas width is the total width of all copies plus the gap between each copy.\r\n+    int canvasWidth = ((singleCopyWidth + gapWidth) * numCopies).ceil();\r\n+\r\n+    double centerY = canvasHeight / 2;\r\n+\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill background with white\r\n+    // Fill the background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n \r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        // Paint text\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(\r\n-                color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        // Paint emoji\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n+    // Draw repeated copies of the message.\r\n+    for (int copy = 0; copy < numCopies; copy++) {\r\n+      double currentX = copy * (singleCopyWidth + gapWidth);\r\n+      // Draw each segment of the message.\r\n+      for (int i = 0; i < segments.length; i++) {\r\n+        final _Segment segment = segments[i];\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+                text: segment.text,\r\n+                style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          // Compute descent so that letters with descenders are fully visible.\r\n+          var metrics = tp.computeLineMetrics();\r\n+          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+          // Offset the text so that the bottom of the glyphs is centered.\r\n+          double offsetY = centerY - (tp.height - descent) / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                  targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            }\r\n           }\r\n+          currentX += emojiWidth;\r\n+          if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+            currentX += emojiGap;\r\n+          }\r\n         }\r\n       }\r\n+      // A gap is automatically left after each copy because of our canvas width calculation.\r\n     }\r\n \r\n-    // Convert canvas to image for downsampling\r\n+    // Finalize the drawing and convert the result to an image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-\r\n     if (byteData == null) {\r\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample to LED matrix\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n     for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index =\r\n-                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n@@ -272,10 +317,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740044032892,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,320 +1,1 @@\n-import 'dart:math';\r\n-import 'dart:typed_data';\r\n-import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-import 'package:badgemagic/providers/imageprovider.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:get_it/get_it.dart';\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n-class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n-  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-  ImageUtils imageUtils = ImageUtils();\r\n-  FileHelper fileHelper = FileHelper();\r\n-\r\n-  int controllerLength = 0;\r\n-\r\n-  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n-    List<String> hexStrings = [];\r\n-    for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n-        int index = int.parse(message[x + 2] + message[x + 3]);\r\n-        var key = controllerData.imageCache.keys.toList()[index];\r\n-        if (key is List) {\r\n-          String filename = key[0];\r\n-          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n-          x += 5;\r\n-        } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n-          hexStrings.addAll(hs);\r\n-          x += 5;\r\n-        }\r\n-      } else {\r\n-        if (converter.charCodes.containsKey(message[x])) {\r\n-          hexStrings.add(converter.charCodes[message[x]]!);\r\n-        }\r\n-      }\r\n-    }\r\n-    if (isInverted) {\r\n-      hexStrings = invertHex(hexStrings.join()).split('');\r\n-      hexStrings = padHexString(hexStrings);\r\n-    }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n-    return hexStrings;\r\n-  }\r\n-\r\n-  /// Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the width of one copy of the rendered message plus a gap,\r\n-  /// and the message is repeated so that there is no empty padding at the borders.\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-  }) async {\r\n-    // Parse the message into segments (plain text and emoji)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n-    }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n-\r\n-    // Determine the scaled font size and set emoji dimensions.\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n-    // Ensure the text fits vertically within the canvas (leaving a tiny margin)\r\n-    if (scaledFontSize > canvasHeight * 0.9) {\r\n-      scaledFontSize = canvasHeight * 0.9;\r\n-    }\r\n-    double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-    double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n-\r\n-    // Measure one copy of the message (in scaled pixels)\r\n-    double singleCopyWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        singleCopyWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segments[i].text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        singleCopyWidth += tp.width;\r\n-      }\r\n-    }\r\n-\r\n-    // fixedWidth is the display width (in scaled pixels)\r\n-    int fixedWidth = cols * scale;\r\n-    // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-    int gapWidth = fixedWidth;\r\n-    // Determine the number of copies to repeat.\r\n-    // Here we use 2 copies so that the canvas covers at least one full cycle.\r\n-    int numCopies = 2;\r\n-    // Canvas width is the total width of all copies plus the gap between each copy.\r\n-    int canvasWidth = ((singleCopyWidth + gapWidth) * numCopies).ceil();\r\n-\r\n-    double centerY = canvasHeight / 2;\r\n-\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-\r\n-    // Fill the background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()), bgPaint);\r\n-\r\n-    // Draw repeated copies of the message.\r\n-    for (int copy = 0; copy < numCopies; copy++) {\r\n-      double currentX = copy * (singleCopyWidth + gapWidth);\r\n-      // Draw each segment of the message.\r\n-      for (int i = 0; i < segments.length; i++) {\r\n-        final _Segment segment = segments[i];\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-                text: segment.text,\r\n-                style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          // Compute descent so that letters with descenders are fully visible.\r\n-          var metrics = tp.computeLineMetrics();\r\n-          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-          // Offset the text so that the bottom of the glyphs is centered.\r\n-          double offsetY = centerY - (tp.height - descent) / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                  targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            }\r\n-          }\r\n-          currentX += emojiWidth;\r\n-          if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-            currentX += emojiGap;\r\n-          }\r\n-        }\r\n-      }\r\n-      // A gap is automatically left after each copy because of our canvas width calculation.\r\n-    }\r\n-\r\n-    // Finalize the drawing and convert the result to an image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n-          }\r\n-        }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n-      }\r\n-    }\r\n-    return matrix;\r\n-  }\r\n-\r\n-  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n-    int height = image.length;\r\n-    int width = image.isNotEmpty ? image[0].length : 0;\r\n-    int finalSum = 0;\r\n-    for (int j = 0; j < width; j++) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += j;\r\n-        break;\r\n-      }\r\n-    }\r\n-    for (int j = width - 1; j >= 0; j--) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += (height - j - 1);\r\n-        break;\r\n-      }\r\n-    }\r\n-    int diff = 0;\r\n-    if ((height - finalSum) % 8 > 0) {\r\n-      diff = 8 - (height - finalSum) % 8;\r\n-    }\r\n-    int rOff = (diff / 2).floor();\r\n-    int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n-        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n-    for (int i = 0; i < height; i++) {\r\n-      int k = 0;\r\n-      for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-      for (int j = 0; j < width; j++) {\r\n-        if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n-        }\r\n-      }\r\n-      for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-    }\r\n-    logger.d(\"Padded image: $list\");\r\n-    List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n-      StringBuffer lineHex = StringBuffer();\r\n-      for (int k = 0; k < height; k++) {\r\n-        StringBuffer stBuilder = StringBuffer();\r\n-        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n-        }\r\n-        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n-            .toRadixString(16)\r\n-            .padLeft(2, '0');\r\n-        lineHex.write(hex);\r\n-      }\r\n-      allHexs.add(lineHex.toString());\r\n-    }\r\n-    return allHexs;\r\n-  }\r\n-\r\n-  static String invertHex(String hex) {\r\n-    StringBuffer invertedHex = StringBuffer();\r\n-    for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n-          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n-    }\r\n-    return invertedHex.toString();\r\n-  }\r\n-\r\n-  List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n-    for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n-      hexArray[i].add(1);\r\n-    }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n-  }\r\n-}\r\n+z\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740044113841,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,1 +1,279 @@\n-z\n\\ No newline at end of file\n+import 'dart:math';\r\n+import 'dart:typed_data';\r\n+import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n+import 'package:badgemagic/providers/imageprovider.dart';\r\n+import 'package:flutter/material.dart';\r\n+import 'package:get_it/get_it.dart';\r\n+\r\n+class Converters {\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n+  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+  ImageUtils imageUtils = ImageUtils();\r\n+  FileHelper fileHelper = FileHelper();\r\n+\r\n+  int controllerLength = 0;\r\n+\r\n+  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n+    List<String> hexStrings = [];\r\n+    for (int x = 0; x < message.length; x++) {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n+        int index = int.parse(message[x + 2] + message[x + 3]);\r\n+        var key = controllerData.imageCache.keys.toList()[index];\r\n+        if (key is List) {\r\n+          String filename = key[0];\r\n+          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n+          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n+          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n+          x += 5;\r\n+        } else {\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          hexStrings.addAll(hs);\r\n+          x += 5;\r\n+        }\r\n+      } else {\r\n+        if (converter.charCodes.containsKey(message[x])) {\r\n+          hexStrings.add(converter.charCodes[message[x]]!);\r\n+        }\r\n+      }\r\n+    }\r\n+    if (isInverted) {\r\n+      hexStrings = invertHex(hexStrings.join()).split('');\r\n+      hexStrings = padHexString(hexStrings);\r\n+    }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n+    return hexStrings;\r\n+  }\r\n+\r\n+  /// Render the given text (with possible emoji placeholders) on a fixed-size canvas\r\n+  /// and convert it to an LED matrix (11 rows × 44 columns). A constant font size is used;\r\n+  /// any text beyond the fixed width is clipped.\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+  }) async {\r\n+    // Parse the message for emoji placeholders.\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start),\r\n+            isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n+    }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n+\r\n+    // Fixed canvas dimensions.\r\n+    final int width = cols * scale;\r\n+    final int height = rows * scale;\r\n+\r\n+    // Create an offscreen canvas.\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas =\r\n+        Canvas(recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n+\r\n+    // Fill the background with white.\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n+\r\n+    // Use a constant font size based on scale.\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+    double currentX = 0;\r\n+    double centerY = height / 2.0;\r\n+\r\n+    // Render each segment at a constant size.\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style:\r\n+                textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(); // natural width; no maxWidth constraint\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n+        }\r\n+      }\r\n+    }\r\n+\r\n+    // The canvas is fixed; extra text beyond the width will be clipped.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(width, height);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    // Downsample to LED matrix.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n+          }\r\n+        }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n+      }\r\n+    }\r\n+    return matrix;\r\n+  }\r\n+\r\n+  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n+    int height = image.length;\r\n+    int width = image.isNotEmpty ? image[0].length : 0;\r\n+    int finalSum = 0;\r\n+    for (int j = 0; j < width; j++) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += j;\r\n+        break;\r\n+      }\r\n+    }\r\n+    for (int j = width - 1; j >= 0; j--) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += (height - j - 1);\r\n+        break;\r\n+      }\r\n+    }\r\n+    int diff = 0;\r\n+    if ((height - finalSum) % 8 > 0) {\r\n+      diff = 8 - (height - finalSum) % 8;\r\n+    }\r\n+    int rOff = (diff / 2).floor();\r\n+    int lOff = (diff / 2).ceil();\r\n+    List<List<int>> list =\r\n+        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+    for (int i = 0; i < height; i++) {\r\n+      int k = 0;\r\n+      for (int j = 0; j < rOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+      for (int j = 0; j < width; j++) {\r\n+        if (image[i][j] != -1) {\r\n+          list[i][k++] = image[i][j];\r\n+        }\r\n+      }\r\n+      for (int j = 0; j < lOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+    }\r\n+    logger.d(\"Padded image: $list\");\r\n+    List<String> allHexs = [];\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+      StringBuffer lineHex = StringBuffer();\r\n+      for (int k = 0; k < height; k++) {\r\n+        StringBuffer stBuilder = StringBuffer();\r\n+        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n+          stBuilder.write(list[k][j]);\r\n+        }\r\n+        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n+            .toRadixString(16)\r\n+            .padLeft(2, '0');\r\n+        lineHex.write(hex);\r\n+      }\r\n+      allHexs.add(lineHex.toString());\r\n+    }\r\n+    return allHexs;\r\n+  }\r\n+\r\n+  static String invertHex(String hex) {\r\n+    StringBuffer invertedHex = StringBuffer();\r\n+    for (int i = 0; i < hex.length; i++) {\r\n+      String invertedHexDigit =\r\n+          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+      invertedHex.write(invertedHexDigit);\r\n+    }\r\n+    return invertedHex.toString();\r\n+  }\r\n+\r\n+  List<String> padHexString(List<String> hexString) {\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n+    for (int i = 0; i < hexArray.length; i++) {\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n+    }\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n+  }\r\n+}\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740044348623,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -52,27 +52,34 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with possible emoji placeholders) on a fixed-size canvas\r\n-  /// and convert it to an LED matrix (11 rows × 44 columns). A constant font size is used;\r\n-  /// any text beyond the fixed width is clipped.\r\n+  /// Render the given text (with possible emoji placeholders) to an LED matrix.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale), and the text is rendered\r\n+  /// on a wider \"marquee\" canvas if needed. Two copies of the message (separated by a gap)\r\n+  /// are drawn to allow seamless scrolling. The [scrollOffset] (in pixels) selects the horizontal\r\n+  /// window from the marquee that is downsampled to the LED matrix.\r\n+  ///\r\n+  /// Update [scrollOffset] over time to animate the marquee.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n     // Parse the message for emoji placeholders.\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start),\r\n-            isEmoji: false));\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n       }\r\n       String emojiIndex = match.group(1)!;\r\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n@@ -81,86 +88,124 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed canvas dimensions.\r\n-    final int width = cols * scale;\r\n-    final int height = rows * scale;\r\n+    // Fixed display dimensions.\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n \r\n-    // Create an offscreen canvas.\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas =\r\n-        Canvas(recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n-\r\n-    // Fill the background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n-\r\n-    // Use a constant font size based on scale.\r\n+    // Use a constant font size.\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n-    double currentX = 0;\r\n-    double centerY = height / 2.0;\r\n \r\n-    // Render each segment at a constant size.\r\n+    // Measure the total width of one copy of the message.\r\n+    double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n+        final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n-            text: segment.text,\r\n-            style:\r\n-                textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n+              text: segment.text,\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n-        textPainter.layout(); // natural width; no maxWidth constraint\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n+        tp.layout();\r\n+        messageWidth += tp.width;\r\n       } else {\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n+        messageWidth += emojiWidth;\r\n+      }\r\n+    }\r\n+\r\n+    // Define a gap between copies. For a smooth marquee, you can adjust this value.\r\n+    double gapWidth = displayWidth; // e.g., one full display width as gap\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+\r\n+    // The canvas will include two copies for seamless scrolling.\r\n+    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    int canvasHeight = displayHeight;\r\n+\r\n+    // Create an offscreen canvas with the wide marquee dimensions.\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas =\r\n+        Canvas(recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+\r\n+    // Fill the background with white.\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n+\r\n+    double centerY = canvasHeight / 2.0;\r\n+\r\n+    // Helper function to draw one copy of the message at a given horizontal offset.\r\n+    Future<void> drawMessage(double offsetX) async {\r\n+      double currentX = offsetX;\r\n+      for (var segment in segments) {\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+                text: segment.text,\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          double offsetY = centerY - tp.height / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                  targetWidth: emojiWidth.toInt(),\r\n+                  targetHeight: emojiWidth.toInt());\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              currentX += emojiWidth;\r\n+            }\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // The canvas is fixed; extra text beyond the width will be clipped.\r\n+    // Draw the first copy starting at x = 0.\r\n+    await drawMessage(0);\r\n+    // Draw the second copy starting at x = marqueeWidth.\r\n+    await drawMessage(marqueeWidth);\r\n+\r\n+    // Finalize the drawing and create the wide marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(width, height);\r\n+    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n     if (byteData == null) {\r\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Downsample to LED matrix.\r\n+    // Compute the effective horizontal offset for the scrolling window.\r\n+    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+\r\n+    // Downsample the display window (of width displayWidth and height canvasHeight)\r\n+    // from the wide marquee image into the LED matrix.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n"
                },
                {
                    "date": 1740044356389,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -115,9 +115,9 @@\n       }\r\n     }\r\n \r\n     // Define a gap between copies. For a smooth marquee, you can adjust this value.\r\n-    double gapWidth = displayWidth; // e.g., one full display width as gap\r\n+    double gapWidth = displayWidth as double; // e.g., one full display width as gap\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n \r\n     // The canvas will include two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n"
                },
                {
                    "date": 1740044453862,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -52,25 +52,23 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with possible emoji placeholders) to an LED matrix.\r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n   ///\r\n-  /// The display area is fixed (cols×scale by rows×scale), and the text is rendered\r\n-  /// on a wider \"marquee\" canvas if needed. Two copies of the message (separated by a gap)\r\n-  /// are drawn to allow seamless scrolling. The [scrollOffset] (in pixels) selects the horizontal\r\n-  /// window from the marquee that is downsampled to the LED matrix.\r\n-  ///\r\n-  /// Update [scrollOffset] over time to animate the marquee.\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10, // scale factor for better resolution\r\n     double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n-    // Parse the message for emoji placeholders.\r\n+    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -88,17 +86,17 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions.\r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Use a constant font size.\r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure the total width of one copy of the message.\r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n@@ -114,30 +112,31 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies. For a smooth marquee, you can adjust this value.\r\n-    double gapWidth = displayWidth as double; // e.g., one full display width as gap\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n \r\n-    // The canvas will include two copies for seamless scrolling.\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n-    // Create an offscreen canvas with the wide marquee dimensions.\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas =\r\n-        Canvas(recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+    );\r\n \r\n-    // Fill the background with white.\r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper function to draw one copy of the message at a given horizontal offset.\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -158,11 +157,13 @@\n           if (index < keys.length) {\r\n             var key = keys[index];\r\n             Uint8List? emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                  targetWidth: emojiWidth.toInt(),\r\n-                  targetHeight: emojiWidth.toInt());\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n               ui.FrameInfo fi = await codec.getNextFrame();\r\n               ui.Image emojiImage = fi.image;\r\n               double offsetY = centerY - emojiWidth / 2;\r\n               Paint imagePaint = Paint();\r\n@@ -173,14 +174,13 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw the first copy starting at x = 0.\r\n+    // Draw two copies of the message.\r\n     await drawMessage(0);\r\n-    // Draw the second copy starting at x = marqueeWidth.\r\n     await drawMessage(marqueeWidth);\r\n \r\n-    // Finalize the drawing and create the wide marquee image.\r\n+    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -188,24 +188,25 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute the effective horizontal offset for the scrolling window.\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample the display window (of width displayWidth and height canvasHeight)\r\n-    // from the wide marquee image into the LED matrix.\r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -215,9 +216,9 @@\n             }\r\n           }\r\n         }\r\n         double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n"
                },
                {
                    "date": 1740044552011,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -188,25 +188,28 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    // Compute effective horizontal offset (looping within marqueeWidth).\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n+    // Downsample only the display window (of size displayWidth x canvasHeight)\r\n+    // from the wide marquee image into the LED matrix.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    // Adjusted threshold to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n+            // Compute sampleX with wrap-around.\r\n+            int sampleX = effectiveOffset + col * scale + x;\r\n+            sampleX = sampleX % canvasWidth;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n"
                },
                {
                    "date": 1740044585453,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -188,28 +188,25 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth).\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window (of size displayWidth x canvasHeight)\r\n-    // from the wide marquee image into the LED matrix.\r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold to capture anti-aliased text.\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            // Compute sampleX with wrap-around.\r\n-            int sampleX = effectiveOffset + col * scale + x;\r\n-            sampleX = sampleX % canvasWidth;\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n"
                },
                {
                    "date": 1740044710149,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -54,12 +54,14 @@\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n   ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message\r\n+  /// exceeds this area, a marquee canvas is created by drawing two copies of the message\r\n+  /// back-to-back (without any gap) so that the text repeats seamlessly. The\r\n+  /// [scrollOffset] (in pixels) determines which horizontal window is downsampled\r\n+  /// to produce the LED matrix. Update [scrollOffset] over time (e.g. via an animation)\r\n+  /// to scroll the full message into view.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -86,25 +88,24 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n+    // Fixed display dimensions.\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Constant font settings\r\n+    // Use a constant font size.\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure one copy of the message's width\r\n+    // Measure the width of one copy of the message.\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -112,11 +113,11 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+    // If the message is shorter than the display, no scrolling is needed.\r\n+    // Otherwise, the marquee width equals the message width.\r\n+    double marqueeWidth = max(messageWidth, displayWidth.toDouble());\r\n \r\n     // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n@@ -134,18 +135,17 @@\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    // Helper function to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -174,9 +174,9 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n+    // Draw two copies of the message back-to-back.\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n     // Finalize the marquee image.\r\n@@ -188,25 +188,31 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    // Compute the effective horizontal offset.\r\n+    // We take the scroll offset modulo marqueeWidth (not canvasWidth)\r\n+    // so that the pattern repeats seamlessly.\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n+    // Downsample the display window (of size displayWidth x canvasHeight)\r\n+    // from the wide marquee image into the LED matrix.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    // Using a threshold that works for anti-aliased fonts.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n+            // Calculate the sample X coordinate with wrap-around.\r\n+            int sampleX = effectiveOffset + col * scale + x;\r\n+            // Wrap within the marqueeWidth, then offset appropriately if needed.\r\n+            sampleX = sampleX % canvasWidth;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n"
                },
                {
                    "date": 1740044856672,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,8 +8,14 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -53,15 +59,8 @@\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message\r\n-  /// exceeds this area, a marquee canvas is created by drawing two copies of the message\r\n-  /// back-to-back (without any gap) so that the text repeats seamlessly. The\r\n-  /// [scrollOffset] (in pixels) determines which horizontal window is downsampled\r\n-  /// to produce the LED matrix. Update [scrollOffset] over time (e.g. via an animation)\r\n-  /// to scroll the full message into view.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -88,24 +87,25 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions.\r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Use a constant font size.\r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure the width of one copy of the message.\r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -113,11 +113,11 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // If the message is shorter than the display, no scrolling is needed.\r\n-    // Otherwise, the marquee width equals the message width.\r\n-    double marqueeWidth = max(messageWidth, displayWidth.toDouble());\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n \r\n     // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n@@ -135,17 +135,18 @@\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper function to draw one copy of the message starting at a given x-offset.\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -174,9 +175,9 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message back-to-back.\r\n+    // Draw two copies of the message.\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n     // Finalize the marquee image.\r\n@@ -188,31 +189,25 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute the effective horizontal offset.\r\n-    // We take the scroll offset modulo marqueeWidth (not canvasWidth)\r\n-    // so that the pattern repeats seamlessly.\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample the display window (of size displayWidth x canvasHeight)\r\n-    // from the wide marquee image into the LED matrix.\r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Using a threshold that works for anti-aliased fonts.\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            // Calculate the sample X coordinate with wrap-around.\r\n-            int sampleX = effectiveOffset + col * scale + x;\r\n-            // Wrap within the marqueeWidth, then offset appropriately if needed.\r\n-            sampleX = sampleX % canvasWidth;\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -322,10 +317,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740044910192,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,14 +8,8 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -59,8 +53,15 @@\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message\r\n+  /// exceeds this area, a marquee canvas is created by drawing two copies of the message\r\n+  /// back-to-back (without any gap) so that the text repeats seamlessly. The\r\n+  /// [scrollOffset] (in pixels) determines which horizontal window is downsampled\r\n+  /// to produce the LED matrix. Update [scrollOffset] over time (e.g. via an animation)\r\n+  /// to scroll the full message into view.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -87,25 +88,24 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n+    // Fixed display dimensions.\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Constant font settings\r\n+    // Use a constant font size.\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure one copy of the message's width\r\n+    // Measure the width of one copy of the message.\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -113,11 +113,11 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+    // If the message is shorter than the display, no scrolling is needed.\r\n+    // Otherwise, the marquee width equals the message width.\r\n+    double marqueeWidth = max(messageWidth, displayWidth.toDouble());\r\n \r\n     // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n@@ -135,18 +135,17 @@\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    // Helper function to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -175,9 +174,9 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n+    // Draw two copies of the message back-to-back.\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n     // Finalize the marquee image.\r\n@@ -189,25 +188,31 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    // Compute the effective horizontal offset.\r\n+    // We take the scroll offset modulo marqueeWidth (not canvasWidth)\r\n+    // so that the pattern repeats seamlessly.\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n+    // Downsample the display window (of size displayWidth x canvasHeight)\r\n+    // from the wide marquee image into the LED matrix.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    // Using a threshold that works for anti-aliased fonts.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n+            // Calculate the sample X coordinate with wrap-around.\r\n+            int sampleX = effectiveOffset + col * scale + x;\r\n+            // Wrap within the marqueeWidth, then offset appropriately if needed.\r\n+            sampleX = sampleX % canvasWidth;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -317,4 +322,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740044977165,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,331 +1,170 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n-import 'package:get_it/get_it.dart';\r\n \r\n-class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n-  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-  ImageUtils imageUtils = ImageUtils();\r\n-  FileHelper fileHelper = FileHelper();\r\n+class ScrollingTextController extends ValueNotifier<double> {\r\n+  final double speed; // pixels per second\r\n+  final double messageWidth;\r\n+  final double displayWidth;\r\n+  \r\n+  ScrollingTextController({\r\n+    required this.messageWidth,\r\n+    required this.displayWidth,\r\n+    this.speed = 50.0,\r\n+  }) : super(0.0);\r\n \r\n-  int controllerLength = 0;\r\n+  void startScrolling(TickerProvider vsync) {\r\n+    final controller = AnimationController(\r\n+      vsync: vsync,\r\n+      duration: Duration(seconds: (messageWidth / speed).ceil()),\r\n+    );\r\n \r\n-  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n-    List<String> hexStrings = [];\r\n-    for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n-        int index = int.parse(message[x + 2] + message[x + 3]);\r\n-        var key = controllerData.imageCache.keys.toList()[index];\r\n-        if (key is List) {\r\n-          String filename = key[0];\r\n-          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n-          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n-          x += 5;\r\n-        } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n-          hexStrings.addAll(hs);\r\n-          x += 5;\r\n-        }\r\n-      } else {\r\n-        if (converter.charCodes.containsKey(message[x])) {\r\n-          hexStrings.add(converter.charCodes[message[x]]!);\r\n-        }\r\n-      }\r\n-    }\r\n-    if (isInverted) {\r\n-      hexStrings = invertHex(hexStrings.join()).split('');\r\n-      hexStrings = padHexString(hexStrings);\r\n-    }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n-    return hexStrings;\r\n+    controller.addListener(() {\r\n+      value = (controller.value * messageWidth) % messageWidth;\r\n+    });\r\n+\r\n+    controller.repeat();\r\n   }\r\n+}\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message\r\n-  /// exceeds this area, a marquee canvas is created by drawing two copies of the message\r\n-  /// back-to-back (without any gap) so that the text repeats seamlessly. The\r\n-  /// [scrollOffset] (in pixels) determines which horizontal window is downsampled\r\n-  /// to produce the LED matrix. Update [scrollOffset] over time (e.g. via an animation)\r\n-  /// to scroll the full message into view.\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n-  }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n-    }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+class ScrollingText extends StatefulWidget {\r\n+  final String message;\r\n+  final TextStyle textStyle;\r\n+  final int cols;\r\n+  final int rows;\r\n+  final int scale;\r\n+  final double speed;\r\n \r\n-    // Fixed display dimensions.\r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n+  const ScrollingText({\r\n+    Key? key,\r\n+    required this.message,\r\n+    required this.textStyle,\r\n+    this.cols = 44,\r\n+    this.rows = 11,\r\n+    this.scale = 10,\r\n+    this.speed = 50.0,\r\n+  }) : super(key: key);\r\n \r\n-    // Use a constant font size.\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+  @override\r\n+  State<ScrollingText> createState() => _ScrollingTextState();\r\n+}\r\n \r\n-    // Measure the width of one copy of the message.\r\n-    double messageWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        messageWidth += tp.width;\r\n-      } else {\r\n-        messageWidth += emojiWidth;\r\n-      }\r\n-    }\r\n+class _ScrollingTextState extends State<ScrollingText> with SingleTickerProviderStateMixin {\r\n+  late ScrollingTextController scrollController;\r\n+  late Converters converters;\r\n \r\n-    // If the message is shorter than the display, no scrolling is needed.\r\n-    // Otherwise, the marquee width equals the message width.\r\n-    double marqueeWidth = max(messageWidth, displayWidth.toDouble());\r\n+  @override\r\n+  void initState() {\r\n+    super.initState();\r\n+    converters = Converters();\r\n+    // Initialize with a temporary value, will be updated after first layout\r\n+    scrollController = ScrollingTextController(\r\n+      messageWidth: 1000, // temporary value\r\n+      displayWidth: widget.cols * widget.scale.toDouble(),\r\n+      speed: widget.speed,\r\n+    );\r\n+    \r\n+    WidgetsBinding.instance.addPostFrameCallback((_) async {\r\n+      // Measure actual message width\r\n+      double messageWidth = await _measureMessageWidth();\r\n+      \r\n+      // Recreate controller with actual width\r\n+      scrollController = ScrollingTextController(\r\n+        messageWidth: messageWidth,\r\n+        displayWidth: widget.cols * widget.scale.toDouble(),\r\n+        speed: widget.speed,\r\n+      );\r\n+      \r\n+      // Start the scrolling animation\r\n+      scrollController.startScrolling(this);\r\n+    });\r\n+  }\r\n \r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n-    int canvasWidth = (marqueeWidth * 2).ceil();\r\n-    int canvasHeight = displayHeight;\r\n-\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+  Future<double> _measureMessageWidth() async {\r\n+    // Use the existing renderTextToMatrix logic to measure the text\r\n+    final matrix = await converters.renderTextToMatrix(\r\n+      widget.message,\r\n+      widget.textStyle,\r\n+      cols: widget.cols,\r\n+      rows: widget.rows,\r\n+      scale: widget.scale,\r\n+      scrollOffset: 0,\r\n     );\r\n+    \r\n+    // Return the actual width needed\r\n+    return matrix[0].length * widget.scale.toDouble();\r\n+  }\r\n \r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+  @override\r\n+  Widget build(BuildContext context) {\r\n+    return ValueListenableBuilder<double>(\r\n+      valueListenable: scrollController,\r\n+      builder: (context, scrollOffset, child) {\r\n+        return FutureBuilder<List<List<bool>>>(\r\n+          future: converters.renderTextToMatrix(\r\n+            widget.message,\r\n+            widget.textStyle,\r\n+            cols: widget.cols,\r\n+            rows: widget.rows,\r\n+            scale: widget.scale,\r\n+            scrollOffset: scrollOffset,\r\n+          ),\r\n+          builder: (context, snapshot) {\r\n+            if (!snapshot.hasData) {\r\n+              return const SizedBox(); // Loading placeholder\r\n+            }\r\n \r\n-    double centerY = canvasHeight / 2.0;\r\n+            final matrix = snapshot.data!;\r\n+            return CustomPaint(\r\n+              size: Size(\r\n+                widget.cols * widget.scale.toDouble(),\r\n+                widget.rows * widget.scale.toDouble(),\r\n+              ),\r\n+              painter: MatrixPainter(matrix: matrix, scale: widget.scale),\r\n+            );\r\n+          },\r\n+        );\r\n+      },\r\n+    );\r\n+  }\r\n \r\n-    // Helper function to draw one copy of the message starting at a given x-offset.\r\n-    Future<void> drawMessage(double offsetX) async {\r\n-      double currentX = offsetX;\r\n-      for (var segment in segments) {\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-                text: segment.text,\r\n-                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          double offsetY = centerY - tp.height / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-              currentX += emojiWidth;\r\n-            }\r\n-          }\r\n-        }\r\n-      }\r\n-    }\r\n+  @override\r\n+  void dispose() {\r\n+    scrollController.dispose();\r\n+    super.dispose();\r\n+  }\r\n+}\r\n \r\n-    // Draw two copies of the message back-to-back.\r\n-    await drawMessage(0);\r\n-    await drawMessage(marqueeWidth);\r\n+class MatrixPainter extends CustomPainter {\r\n+  final List<List<bool>> matrix;\r\n+  final int scale;\r\n \r\n-    // Finalize the marquee image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  MatrixPainter({required this.matrix, required this.scale});\r\n \r\n-    // Compute the effective horizontal offset.\r\n-    // We take the scroll offset modulo marqueeWidth (not canvasWidth)\r\n-    // so that the pattern repeats seamlessly.\r\n-    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+  @override\r\n+  void paint(Canvas canvas, Size size) {\r\n+    final paint = Paint()\r\n+      ..color = Colors.black\r\n+      ..style = PaintingStyle.fill;\r\n \r\n-    // Downsample the display window (of size displayWidth x canvasHeight)\r\n-    // from the wide marquee image into the LED matrix.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Using a threshold that works for anti-aliased fonts.\r\n-    const int threshold = 200;\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            // Calculate the sample X coordinate with wrap-around.\r\n-            int sampleX = effectiveOffset + col * scale + x;\r\n-            // Wrap within the marqueeWidth, then offset appropriately if needed.\r\n-            sampleX = sampleX % canvasWidth;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n-          }\r\n+    for (int row = 0; row < matrix.length; row++) {\r\n+      for (int col = 0; col < matrix[row].length; col++) {\r\n+        if (matrix[row][col]) {\r\n+          canvas.drawRect(\r\n+            Rect.fromLTWH(\r\n+              col * scale.toDouble(),\r\n+              row * scale.toDouble(),\r\n+              scale.toDouble(),\r\n+              scale.toDouble(),\r\n+            ),\r\n+            paint,\r\n+          );\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n-    return matrix;\r\n   }\r\n \r\n-  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n-    int height = image.length;\r\n-    int width = image.isNotEmpty ? image[0].length : 0;\r\n-    int finalSum = 0;\r\n-    for (int j = 0; j < width; j++) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += j;\r\n-        break;\r\n-      }\r\n-    }\r\n-    for (int j = width - 1; j >= 0; j--) {\r\n-      int sum = 0;\r\n-      for (int i = 0; i < height; i++) {\r\n-        sum += image[i][j];\r\n-      }\r\n-      if (sum == 0 && trim) {\r\n-        for (int i = 0; i < height; i++) {\r\n-          image[i][j] = -1;\r\n-        }\r\n-      } else {\r\n-        finalSum += (height - j - 1);\r\n-        break;\r\n-      }\r\n-    }\r\n-    int diff = 0;\r\n-    if ((height - finalSum) % 8 > 0) {\r\n-      diff = 8 - (height - finalSum) % 8;\r\n-    }\r\n-    int rOff = (diff / 2).floor();\r\n-    int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n-        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n-    for (int i = 0; i < height; i++) {\r\n-      int k = 0;\r\n-      for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-      for (int j = 0; j < width; j++) {\r\n-        if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n-        }\r\n-      }\r\n-      for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n-      }\r\n-    }\r\n-    logger.d(\"Padded image: $list\");\r\n-    List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n-      StringBuffer lineHex = StringBuffer();\r\n-      for (int k = 0; k < height; k++) {\r\n-        StringBuffer stBuilder = StringBuffer();\r\n-        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n-        }\r\n-        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n-            .toRadixString(16)\r\n-            .padLeft(2, '0');\r\n-        lineHex.write(hex);\r\n-      }\r\n-      allHexs.add(lineHex.toString());\r\n-    }\r\n-    return allHexs;\r\n-  }\r\n-\r\n-  static String invertHex(String hex) {\r\n-    StringBuffer invertedHex = StringBuffer();\r\n-    for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n-          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n-    }\r\n-    return invertedHex.toString();\r\n-  }\r\n-\r\n-  List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n-    for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n-      hexArray[i].add(1);\r\n-    }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n-  }\r\n-}\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+  @override\r\n+  bool shouldRepaint(MatrixPainter oldDelegate) => true;\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740045077036,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,170 +1,303 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n+import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n+import 'package:get_it/get_it.dart';\r\n \r\n-class ScrollingTextController extends ValueNotifier<double> {\r\n-  final double speed; // pixels per second\r\n-  final double messageWidth;\r\n-  final double displayWidth;\r\n-  \r\n-  ScrollingTextController({\r\n-    required this.messageWidth,\r\n-    required this.displayWidth,\r\n-    this.speed = 50.0,\r\n-  }) : super(0.0);\r\n+class Converters {\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+  ImageUtils imageUtils = ImageUtils();\r\n+  FileHelper fileHelper = FileHelper();\r\n \r\n-  void startScrolling(TickerProvider vsync) {\r\n-    final controller = AnimationController(\r\n-      vsync: vsync,\r\n-      duration: Duration(seconds: (messageWidth / speed).ceil()),\r\n-    );\r\n+  int controllerLength = 0;\r\n \r\n-    controller.addListener(() {\r\n-      value = (controller.value * messageWidth) % messageWidth;\r\n-    });\r\n-\r\n-    controller.repeat();\r\n+  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n+    List<String> hexStrings = [];\r\n+    for (int x = 0; x < message.length; x++) {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n+        int index = int.parse(message[x + 2] + message[x + 3]);\r\n+        var key = controllerData.imageCache.keys.toList()[index];\r\n+        if (key is List) {\r\n+          String filename = key[0];\r\n+          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n+          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n+          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n+          x += 5;\r\n+        } else {\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          hexStrings.addAll(hs);\r\n+          x += 5;\r\n+        }\r\n+      } else {\r\n+        if (converter.charCodes.containsKey(message[x])) {\r\n+          hexStrings.add(converter.charCodes[message[x]]!);\r\n+        }\r\n+      }\r\n+    }\r\n+    if (isInverted) {\r\n+      hexStrings = invertHex(hexStrings.join()).split('');\r\n+      hexStrings = padHexString(hexStrings);\r\n+    }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n+    return hexStrings;\r\n   }\r\n-}\r\n \r\n-class ScrollingText extends StatefulWidget {\r\n-  final String message;\r\n-  final TextStyle textStyle;\r\n-  final int cols;\r\n-  final int rows;\r\n-  final int scale;\r\n-  final double speed;\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    \r\n+    // Parse message into segments (text and emoji placeholders)\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n+    }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  const ScrollingText({\r\n-    Key? key,\r\n-    required this.message,\r\n-    required this.textStyle,\r\n-    this.cols = 44,\r\n-    this.rows = 11,\r\n-    this.scale = 10,\r\n-    this.speed = 50.0,\r\n-  }) : super(key: key);\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-  @override\r\n-  State<ScrollingText> createState() => _ScrollingTextState();\r\n-}\r\n+    // First pass: measure total width\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        totalWidth += tp.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n+      }\r\n+    }\r\n \r\n-class _ScrollingTextState extends State<ScrollingText> with SingleTickerProviderStateMixin {\r\n-  late ScrollingTextController scrollController;\r\n-  late Converters converters;\r\n-\r\n-  @override\r\n-  void initState() {\r\n-    super.initState();\r\n-    converters = Converters();\r\n-    // Initialize with a temporary value, will be updated after first layout\r\n-    scrollController = ScrollingTextController(\r\n-      messageWidth: 1000, // temporary value\r\n-      displayWidth: widget.cols * widget.scale.toDouble(),\r\n-      speed: widget.speed,\r\n+    // Create canvas wide enough for scrolling\r\n+    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n     );\r\n-    \r\n-    WidgetsBinding.instance.addPostFrameCallback((_) async {\r\n-      // Measure actual message width\r\n-      double messageWidth = await _measureMessageWidth();\r\n-      \r\n\\ No newline at end of file\n-      // Recreate controller with actual width\r\n-      scrollController = ScrollingTextController(\r\n-        messageWidth: messageWidth,\r\n-        displayWidth: widget.cols * widget.scale.toDouble(),\r\n-        speed: widget.speed,\r\n-      );\r\n-      \r\n-      // Start the scrolling animation\r\n-      scrollController.startScrolling(this);\r\n-    });\r\n-  }\r\n \r\n-  Future<double> _measureMessageWidth() async {\r\n-    // Use the existing renderTextToMatrix logic to measure the text\r\n-    final matrix = await converters.renderTextToMatrix(\r\n-      widget.message,\r\n-      widget.textStyle,\r\n-      cols: widget.cols,\r\n-      rows: widget.rows,\r\n-      scale: widget.scale,\r\n-      scrollOffset: 0,\r\n+    // Fill background\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      bgPaint\r\n     );\r\n-    \r\n-    // Return the actual width needed\r\n-    return matrix[0].length * widget.scale.toDouble();\r\n-  }\r\n \r\n-  @override\r\n-  Widget build(BuildContext context) {\r\n-    return ValueListenableBuilder<double>(\r\n-      valueListenable: scrollController,\r\n-      builder: (context, scrollOffset, child) {\r\n-        return FutureBuilder<List<List<bool>>>(\r\n-          future: converters.renderTextToMatrix(\r\n-            widget.message,\r\n-            widget.textStyle,\r\n-            cols: widget.cols,\r\n-            rows: widget.rows,\r\n-            scale: widget.scale,\r\n-            scrollOffset: scrollOffset,\r\n-          ),\r\n-          builder: (context, snapshot) {\r\n-            if (!snapshot.hasData) {\r\n-              return const SizedBox(); // Loading placeholder\r\n+    // Draw two copies of the content for seamless scrolling\r\n+    double centerY = displayHeight / 2;\r\n+    for (int copy = 0; copy < 2; copy++) {\r\n+      double currentX = copy * totalWidth;\r\n+      for (var segment in segments) {\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+            ),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          var key = controllerData.imageCache.keys.toList()[index];\r\n+          if (key != null) {\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              canvas.drawImage(\r\n+                fi.image,\r\n+                Offset(currentX, centerY - emojiWidth / 2),\r\n+                Paint()\r\n+              );\r\n             }\r\n+          }\r\n+          currentX += emojiWidth;\r\n+        }\r\n+      }\r\n+    }\r\n \r\n-            final matrix = snapshot.data!;\r\n-            return CustomPaint(\r\n-              size: Size(\r\n-                widget.cols * widget.scale.toDouble(),\r\n-                widget.rows * widget.scale.toDouble(),\r\n-              ),\r\n-              painter: MatrixPainter(matrix: matrix, scale: widget.scale),\r\n-            );\r\n-          },\r\n-        );\r\n-      },\r\n+    // Convert to image and sample the visible portion\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n+    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n+\r\n+    // Create the output matrix\r\n+    List<List<bool>> matrix = List.generate(\r\n+      rows,\r\n+      (_) => List.generate(cols, (_) => false)\r\n     );\r\n-  }\r\n \r\n-  @override\r\n-  void dispose() {\r\n-    scrollController.dispose();\r\n-    super.dispose();\r\n-  }\r\n-}\r\n+    // Sample the visible portion with scrolling\r\n+    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n+    final Uint8List pixels = byteData.buffer.asUint8List();\r\n+    const int threshold = 200;\r\n \r\n-class MatrixPainter extends CustomPainter {\r\n-  final List<List<bool>> matrix;\r\n-  final int scale;\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        \r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n+            \r\n+            if (index + 3 < pixels.length) {\r\n+              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n+          }\r\n+        }\r\n+        \r\n+        matrix[row][col] = (sum / count) < threshold;\r\n+      }\r\n+    }\r\n \r\n-  MatrixPainter({required this.matrix, required this.scale});\r\n+    return matrix;\r\n+  }\r\n \r\n-  @override\r\n-  void paint(Canvas canvas, Size size) {\r\n-    final paint = Paint()\r\n-      ..color = Colors.black\r\n-      ..style = PaintingStyle.fill;\r\n-\r\n-    for (int row = 0; row < matrix.length; row++) {\r\n-      for (int col = 0; col < matrix[row].length; col++) {\r\n-        if (matrix[row][col]) {\r\n-          canvas.drawRect(\r\n-            Rect.fromLTWH(\r\n-              col * scale.toDouble(),\r\n-              row * scale.toDouble(),\r\n-              scale.toDouble(),\r\n-              scale.toDouble(),\r\n-            ),\r\n-            paint,\r\n-          );\r\n+  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n+    int height = image.length;\r\n+    int width = image.isNotEmpty ? image[0].length : 0;\r\n+    int finalSum = 0;\r\n+    for (int j = 0; j < width; j++) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n         }\r\n+      } else {\r\n+        finalSum += j;\r\n+        break;\r\n       }\r\n     }\r\n+    for (int j = width - 1; j >= 0; j--) {\r\n+      int sum = 0;\r\n+      for (int i = 0; i < height; i++) {\r\n+        sum += image[i][j];\r\n+      }\r\n+      if (sum == 0 && trim) {\r\n+        for (int i = 0; i < height; i++) {\r\n+          image[i][j] = -1;\r\n+        }\r\n+      } else {\r\n+        finalSum += (height - j - 1);\r\n+        break;\r\n+      }\r\n+    }\r\n+    int diff = 0;\r\n+    if ((height - finalSum) % 8 > 0) {\r\n+      diff = 8 - (height - finalSum) % 8;\r\n+    }\r\n+    int rOff = (diff / 2).floor();\r\n+    int lOff = (diff / 2).ceil();\r\n+    List<List<int>> list =\r\n+        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n+    for (int i = 0; i < height; i++) {\r\n+      int k = 0;\r\n+      for (int j = 0; j < rOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+      for (int j = 0; j < width; j++) {\r\n+        if (image[i][j] != -1) {\r\n+          list[i][k++] = image[i][j];\r\n+        }\r\n+      }\r\n+      for (int j = 0; j < lOff; j++) {\r\n+        list[i][k++] = 0;\r\n+      }\r\n+    }\r\n+    logger.d(\"Padded image: $list\");\r\n+    List<String> allHexs = [];\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+      StringBuffer lineHex = StringBuffer();\r\n+      for (int k = 0; k < height; k++) {\r\n+        StringBuffer stBuilder = StringBuffer();\r\n+        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n+          stBuilder.write(list[k][j]);\r\n+        }\r\n+        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n+            .toRadixString(16)\r\n+            .padLeft(2, '0');\r\n+        lineHex.write(hex);\r\n+      }\r\n+      allHexs.add(lineHex.toString());\r\n+    }\r\n+    return allHexs;\r\n   }\r\n \r\n-  @override\r\n-  bool shouldRepaint(MatrixPainter oldDelegate) => true;\r\n-}\n+  static String invertHex(String hex) {\r\n+    StringBuffer invertedHex = StringBuffer();\r\n+    for (int i = 0; i < hex.length; i++) {\r\n+      String invertedHexDigit =\r\n+          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n+      invertedHex.write(invertedHexDigit);\r\n+    }\r\n+    return invertedHex.toString();\r\n+  }\r\n+\r\n+  List<String> padHexString(List<String> hexString) {\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n+    for (int i = 0; i < hexArray.length; i++) {\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n+    }\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n+  }\r\n+}\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740045201567,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,17 +1,25 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -51,157 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    \r\n-    // Parse message into segments (text and emoji placeholders)\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // First pass: measure total width\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        totalWidth += tp.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    // Create canvas wide enough for scrolling\r\n-    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-    );\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // Fill background\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-      bgPaint\r\n-    );\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Draw two copies of the content for seamless scrolling\r\n-    double centerY = displayHeight / 2;\r\n-    for (int copy = 0; copy < 2; copy++) {\r\n-      double currentX = copy * totalWidth;\r\n-      for (var segment in segments) {\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-            ),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          var key = controllerData.imageCache.keys.toList()[index];\r\n-          if (key != null) {\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              canvas.drawImage(\r\n-                fi.image,\r\n-                Offset(currentX, centerY - emojiWidth / 2),\r\n-                Paint()\r\n-              );\r\n-            }\r\n-          }\r\n-          currentX += emojiWidth;\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Convert to image and sample the visible portion\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n-    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n-\r\n-    // Create the output matrix\r\n-    List<List<bool>> matrix = List.generate(\r\n-      rows,\r\n-      (_) => List.generate(cols, (_) => false)\r\n-    );\r\n-\r\n-    // Sample the visible portion with scrolling\r\n-    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n-    final Uint8List pixels = byteData.buffer.asUint8List();\r\n-    const int threshold = 200;\r\n-\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        \r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n-            \r\n-            if (index + 3 < pixels.length) {\r\n-              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        \r\n-        matrix[row][col] = (sum / count) < threshold;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -293,11 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740045253967,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,162 +63,156 @@\n   /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n   /// The canvas width is set to the total rendered message width plus extra (display width),\r\n   /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    \r\n+    // Parse message into segments (text and emoji placeholders)\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    // First pass: measure total width\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        totalWidth += tp.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Create canvas wide enough for scrolling\r\n+    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+    );\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Fill background\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      bgPaint\r\n+    );\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    // Draw two copies of the content for seamless scrolling\r\n+    double centerY = displayHeight / 2;\r\n+    for (int copy = 0; copy < 2; copy++) {\r\n+      double currentX = copy * totalWidth;\r\n+      for (var segment in segments) {\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+            ),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          var key = controllerData.imageCache.keys.toList()[index];\r\n+          if (key != null) {\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              canvas.drawImage(\r\n+                fi.image,\r\n+                Offset(currentX, centerY - emojiWidth / 2),\r\n+                Paint()\r\n+              );\r\n+            }\r\n+          }\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Convert to image and sample the visible portion\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n+    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n+\r\n+    // Create the output matrix\r\n+    List<List<bool>> matrix = List.generate(\r\n+      rows,\r\n+      (_) => List.generate(cols, (_) => false)\r\n+    );\r\n+\r\n+    // Sample the visible portion with scrolling\r\n+    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n+    final Uint8List pixels = byteData.buffer.asUint8List();\r\n+    const int threshold = 200;\r\n+\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        \r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n+            \r\n+            if (index + 3 < pixels.length) {\r\n+              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        \r\n+        matrix[row][col] = (sum / count) < threshold;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1740045281461,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -63,156 +63,162 @@\n   /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n   /// onto an offscreen canvas and convert it to an LED matrix.\r\n   /// The canvas width is set to the total rendered message width plus extra (display width),\r\n   /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    \r\n-    // Parse message into segments (text and emoji placeholders)\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // First pass: measure total width\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        totalWidth += tp.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    // Create canvas wide enough for scrolling\r\n-    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-    );\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // Fill background\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-      bgPaint\r\n-    );\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Draw two copies of the content for seamless scrolling\r\n-    double centerY = displayHeight / 2;\r\n-    for (int copy = 0; copy < 2; copy++) {\r\n-      double currentX = copy * totalWidth;\r\n-      for (var segment in segments) {\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-            ),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          var key = controllerData.imageCache.keys.toList()[index];\r\n-          if (key != null) {\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              canvas.drawImage(\r\n-                fi.image,\r\n-                Offset(currentX, centerY - emojiWidth / 2),\r\n-                Paint()\r\n-              );\r\n-            }\r\n-          }\r\n-          currentX += emojiWidth;\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n+\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Convert to image and sample the visible portion\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n-    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n-\r\n-    // Create the output matrix\r\n-    List<List<bool>> matrix = List.generate(\r\n-      rows,\r\n-      (_) => List.generate(cols, (_) => false)\r\n-    );\r\n-\r\n-    // Sample the visible portion with scrolling\r\n-    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n-    final Uint8List pixels = byteData.buffer.asUint8List();\r\n-    const int threshold = 200;\r\n-\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        \r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n-            \r\n-            if (index + 3 < pixels.length) {\r\n-              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        \r\n-        matrix[row][col] = (sum / count) < threshold;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n"
                },
                {
                    "date": 1740045469877,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,25 +1,17 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -59,168 +51,157 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    \r\n+    // Parse message into segments (text and emoji placeholders)\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    // First pass: measure total width\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        totalWidth += tp.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Create canvas wide enough for scrolling\r\n+    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+    );\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Fill background\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      bgPaint\r\n+    );\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n-\r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    // Draw two copies of the content for seamless scrolling\r\n+    double centerY = displayHeight / 2;\r\n+    for (int copy = 0; copy < 2; copy++) {\r\n+      double currentX = copy * totalWidth;\r\n+      for (var segment in segments) {\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n+            ),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          var key = controllerData.imageCache.keys.toList()[index];\r\n+          if (key != null) {\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              canvas.drawImage(\r\n+                fi.image,\r\n+                Offset(currentX, centerY - emojiWidth / 2),\r\n+                Paint()\r\n+              );\r\n+            }\r\n+          }\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Convert to image and sample the visible portion\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n+    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n+\r\n+    // Create the output matrix\r\n+    List<List<bool>> matrix = List.generate(\r\n+      rows,\r\n+      (_) => List.generate(cols, (_) => false)\r\n+    );\r\n+\r\n+    // Sample the visible portion with scrolling\r\n+    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n+    final Uint8List pixels = byteData.buffer.asUint8List();\r\n+    const int threshold = 200;\r\n+\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        \r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n+            \r\n+            if (index + 3 < pixels.length) {\r\n+              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        \r\n+        matrix[row][col] = (sum / count) < threshold;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -312,5 +293,11 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\n\\ No newline at end of file\n+}\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740045477469,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -9,9 +9,10 @@\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -51,21 +52,26 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n+    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n-    \r\n-    // Parse message into segments (text and emoji placeholders)\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n@@ -76,129 +82,145 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n+\r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // First pass: measure total width\r\n-    double totalWidth = 0;\r\n+    // Measure one copy of the message's width\r\n+    double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-          ),\r\n+              text: segment.text,\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n-        totalWidth += tp.width;\r\n+        messageWidth += tp.width;\r\n       } else {\r\n-        totalWidth += emojiWidth;\r\n+        messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Create canvas wide enough for scrolling\r\n-    int canvasWidth = max(displayWidth, (totalWidth * 2).ceil());\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n+    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    int canvasHeight = displayHeight;\r\n+\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n       recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background\r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-      bgPaint\r\n-    );\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-    // Draw two copies of the content for seamless scrolling\r\n-    double centerY = displayHeight / 2;\r\n-    for (int copy = 0; copy < 2; copy++) {\r\n-      double currentX = copy * totalWidth;\r\n+    double centerY = canvasHeight / 2.0;\r\n+\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    Future<void> drawMessage(double offsetX) async {\r\n+      double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)\r\n-            ),\r\n+                text: segment.text,\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n-          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n+          double offsetY = centerY - tp.height / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n           currentX += tp.width;\r\n         } else {\r\n           int index = int.parse(segment.text);\r\n-          var key = controllerData.imageCache.keys.toList()[index];\r\n-          if (key != null) {\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n             Uint8List? emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n               ui.Codec codec = await ui.instantiateImageCodec(\r\n                 emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(),\r\n                 targetHeight: emojiWidth.toInt(),\r\n               );\r\n               ui.FrameInfo fi = await codec.getNextFrame();\r\n-              canvas.drawImage(\r\n-                fi.image,\r\n-                Offset(currentX, centerY - emojiWidth / 2),\r\n-                Paint()\r\n-              );\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              currentX += emojiWidth;\r\n             }\r\n           }\r\n-          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Convert to image and sample the visible portion\r\n+    // Draw two copies of the message.\r\n+    await drawMessage(0);\r\n+    await drawMessage(marqueeWidth);\r\n+\r\n+    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, displayHeight);\r\n-    final ByteData? byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) throw Exception(\"Failed to convert image to byte data\");\r\n+    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Create the output matrix\r\n-    List<List<bool>> matrix = List.generate(\r\n-      rows,\r\n-      (_) => List.generate(cols, (_) => false)\r\n-    );\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Sample the visible portion with scrolling\r\n-    int effectiveOffset = scrollOffset.floor() % totalWidth.floor();\r\n-    final Uint8List pixels = byteData.buffer.asUint8List();\r\n+    // Downsample only the display window from the wide marquee image.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n-\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n-        \r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int sampleX = (effectiveOffset + col * scale + x).floor() % canvasWidth;\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + sampleX) * 4;\r\n-            \r\n-            if (index + 3 < pixels.length) {\r\n-              int brightness = ((pixels[index] + pixels[index + 1] + pixels[index + 2]) / 3).round();\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n               sum += brightness;\r\n               count++;\r\n             }\r\n           }\r\n         }\r\n-        \r\n-        matrix[row][col] = (sum / count) < threshold;\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n-\r\n     return matrix;\r\n   }\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n"
                },
                {
                    "date": 1740045598140,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,11 +8,16 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -28,10 +33,9 @@\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n           List<String> hs =\r\n@@ -82,29 +86,27 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n+    // Fixed display dimensions.\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Constant font settings\r\n+    // Constant font settings.\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n+    double emojiWidth = fontSize * 0.734; // width factor for emojis\r\n \r\n-    // Measure one copy of the message's width\r\n+    // Measure one copy of the message's width.\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -129,23 +131,23 @@\n \r\n     // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint,\r\n+    );\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    // Helper function to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -194,9 +196,9 @@\n \r\n     // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    // Using an adjusted brightness threshold to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n@@ -316,10 +318,4 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n"
                },
                {
                    "date": 1740045642691,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,16 +8,11 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -33,9 +28,10 @@\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n           List<String> hs =\r\n@@ -86,27 +82,29 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions.\r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Constant font settings.\r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734; // width factor for emojis\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure one copy of the message's width.\r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -131,23 +129,23 @@\n \r\n     // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint,\r\n-    );\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper function to draw one copy of the message starting at a given x-offset.\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style: textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -196,9 +194,9 @@\n \r\n     // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Using an adjusted brightness threshold to capture anti-aliased text.\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n@@ -318,4 +316,10 @@\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740046104864,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,8 +8,13 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+// Assuming logger is defined elsewhere, e.g., via a logging package\r\n+// If not, replace logger.d with print or remove it\r\n+import 'package:logging/logging.dart';\r\n+final Logger logger = Logger('Converters');\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -54,12 +59,12 @@\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n   ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n+  /// The display area is fixed (cols×scale by rows×scale). For seamless scrolling,\r\n+  /// the marquee canvas contains two copies of the message with no gap (gapWidth = 0).\r\n+  /// The [scrollOffset] (in pixels) determines which part of the marquee is shown.\r\n+  /// To animate, update [scrollOffset] over time (e.g., via AnimationController).\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -112,31 +117,39 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n+    // **Modified**: Set gapWidth to 0 for seamless scrolling\r\n+    double gapWidth = 0; // No gap between copies for continuous looping\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n \r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n-    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    // Handle short messages: if message fits, center it without scrolling\r\n+    if (messageWidth <= displayWidth) {\r\n+      marqueeWidth = messageWidth; // Only one copy needed\r\n+    } else {\r\n+      // For long messages, create a canvas with two copies for seamless scrolling\r\n+      marqueeWidth = messageWidth * 2; // Two copies, no gap\r\n+    }\r\n+\r\n+    // Create a marquee canvas\r\n+    int canvasWidth = marqueeWidth.ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background with white.\r\n+    // Fill background with white\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    // Helper to draw one copy of the message starting at a given x-offset\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -166,21 +179,28 @@\n               ui.FrameInfo fi = await codec.getNextFrame();\r\n               ui.Image emojiImage = fi.image;\r\n               double offsetY = centerY - emojiWidth / 2;\r\n               Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              canvas.drawImage(\r\n+                  emojiImage, Offset(currentX, offsetY), imagePaint);\r\n               currentX += emojiWidth;\r\n             }\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n-    await drawMessage(0);\r\n-    await drawMessage(marqueeWidth);\r\n+    // Draw the message based on message length\r\n+    if (messageWidth <= displayWidth) {\r\n+      // Center short messages\r\n+      await drawMessage((displayWidth - messageWidth) / 2);\r\n+    } else {\r\n+      // Draw two copies for long messages\r\n+      await drawMessage(0);\r\n+      await drawMessage(messageWidth);\r\n+    }\r\n \r\n-    // Finalize the marquee image.\r\n+    // Finalize the marquee image\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -189,15 +209,16 @@\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n     // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+    int effectiveOffset = messageWidth <= displayWidth\r\n+        ? 0 // No scrolling for short messages\r\n+        : (scrollOffset % messageWidth).floor(); // Seamless loop for long messages\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n+    // Downsample only the display window from the wide marquee image\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n-    const int threshold = 200;\r\n+    const int threshold = 200; // Adjusted for anti-aliased text\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n@@ -315,11 +336,17 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+\r\n+  // Placeholder for hexStringToBool, assuming it exists elsewhere\r\n+  List<List<bool>> hexStringToBool(String hex) {\r\n+    // Implement if not already defined in your codebase\r\n+    throw UnimplementedError(\"hexStringToBool not implemented\");\r\n+  }\r\n }\r\n \r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740046121335,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,9 +53,9 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n+    \r\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n"
                },
                {
                    "date": 1740046133375,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -53,9 +53,8 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    \r\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n@@ -297,9 +296,9 @@\n       for (int j = 0; j < lOff; j++) {\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n-    logger.d(\"Padded image: $list\");\r\n+\r\n     List<String> allHexs = [];\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n"
                },
                {
                    "date": 1740046238579,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -8,13 +8,8 @@\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-// Assuming logger is defined elsewhere, e.g., via a logging package\r\n-// If not, replace logger.d with print or remove it\r\n-import 'package:logging/logging.dart';\r\n-final Logger logger = Logger('Converters');\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -25,10 +20,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -53,197 +47,159 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). For seamless scrolling,\r\n-  /// the marquee canvas contains two copies of the message with no gap (gapWidth = 0).\r\n-  /// The [scrollOffset] (in pixels) determines which part of the marquee is shown.\r\n-  /// To animate, update [scrollOffset] over time (e.g., via AnimationController).\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n-  }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n+  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n+  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n+}) async {\r\n+  // Parse the message for emoji placeholders in the format <<00>>\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(\r\n+        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Fixed display dimensions\r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n+  // Capture the emoji keys once to ensure consistent ordering.\r\n+  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n \r\n-    // Constant font settings\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    // Measure one copy of the message's width\r\n-    double messageWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        messageWidth += tp.width;\r\n-      } else {\r\n-        messageWidth += emojiWidth;\r\n-      }\r\n+  // Calculate total width required to draw the entire message.\r\n+  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double emojiWidth = fontSize * 0.734;\r\n+  double totalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout();\r\n+      totalWidth += textPainter.width;\r\n+    } else {\r\n+      totalWidth += emojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // **Modified**: Set gapWidth to 0 for seamless scrolling\r\n-    double gapWidth = 0; // No gap between copies for continuous looping\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+  // Create a canvas wide enough to hold the entire message.\r\n+  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+  final int canvasHeight = rows * scale;\r\n \r\n-    // Handle short messages: if message fits, center it without scrolling\r\n-    if (messageWidth <= displayWidth) {\r\n-      marqueeWidth = messageWidth; // Only one copy needed\r\n-    } else {\r\n-      // For long messages, create a canvas with two copies for seamless scrolling\r\n-      marqueeWidth = messageWidth * 2; // Two copies, no gap\r\n-    }\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Create a marquee canvas\r\n-    int canvasWidth = marqueeWidth.ceil();\r\n-    int canvasHeight = displayHeight;\r\n-\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-      recorder,\r\n+  // Fill background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-    );\r\n+      bgPaint);\r\n \r\n-    // Fill background with white\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n \r\n-    double centerY = canvasHeight / 2.0;\r\n-\r\n-    // Helper to draw one copy of the message starting at a given x-offset\r\n-    Future<void> drawMessage(double offsetX) async {\r\n-      double currentX = offsetX;\r\n-      for (var segment in segments) {\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-                text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          double offsetY = centerY - tp.height / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(\r\n-                  emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-              currentX += emojiWidth;\r\n-            }\r\n-          }\r\n+  // Draw each segment onto the canvas.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      // Paint text.\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+      double offsetY = centerY - textPainter.height / 2;\r\n+      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += textPainter.width;\r\n+    } else {\r\n+      // Paint emoji.\r\n+      int index = int.parse(segment.text);\r\n+      if (index < emojiKeys.length) {\r\n+        var key = emojiKeys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // Draw the message based on message length\r\n-    if (messageWidth <= displayWidth) {\r\n-      // Center short messages\r\n-      await drawMessage((displayWidth - messageWidth) / 2);\r\n-    } else {\r\n-      // Draw two copies for long messages\r\n-      await drawMessage(0);\r\n-      await drawMessage(messageWidth);\r\n-    }\r\n+  // Convert canvas to image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-    // Finalize the marquee image\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = messageWidth <= displayWidth\r\n-        ? 0 // No scrolling for short messages\r\n-        : (scrollOffset % messageWidth).floor(); // Seamless loop for long messages\r\n-\r\n-    // Downsample only the display window from the wide marquee image\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    const int threshold = 200; // Adjusted for anti-aliased text\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample to LED matrix using the provided xOffset.\r\n+  List<List<bool>> matrix =\r\n+      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = xOffset + col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n+            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n+          } else {\r\n+            sum += 255;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -296,9 +252,9 @@\n       for (int j = 0; j < lOff; j++) {\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n-\r\n+    logger.d(\"Padded image: $list\");\r\n     List<String> allHexs = [];\r\n     for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n@@ -335,17 +291,11 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-\r\n-  // Placeholder for hexStringToBool, assuming it exists elsewhere\r\n-  List<List<bool>> hexStringToBool(String hex) {\r\n-    // Implement if not already defined in your codebase\r\n-    throw UnimplementedError(\"hexStringToBool not implemented\");\r\n-  }\r\n }\r\n \r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n-}\n\\ No newline at end of file\n+}\r\n"
                },
                {
                    "date": 1740046446999,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,14 +1,15 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n+import 'package:badgemagic/bademagic_module_utils/byte_array_utils.dart' as byteUtils;\r\n+import 'package:badgemagic/bademagic_module_utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module_utils/file_helper.dart';\r\n+import 'package:badgemagic/bademagic_module_utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n+import 'package:logging/logging.dart';\r\n \r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n@@ -17,12 +18,16 @@\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n+  // Define logger explicitly for this file\r\n+  static final logger = logging.getLogger('Converters');\r\n+\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -47,159 +52,197 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n+    logger.d(\"Hex strings: $hexStrings\"); // Use local logger\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n-  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n-  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n-}) async {\r\n-  // Parse the message for emoji placeholders in the format <<00>>\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). For seamless scrolling,\r\n+  /// the marquee canvas contains two copies of the message with no gap (gapWidth = 0).\r\n+  /// The [scrollOffset] (in pixels) determines which part of the marquee is shown.\r\n+  /// To animate, update [scrollOffset] over time (e.g., via AnimationController).\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n+  }) async {\r\n+    // Parse message into segments (text and emoji placeholders)\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(\r\n-        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Capture the emoji keys once to ensure consistent ordering.\r\n-  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n+    // Fixed display dimensions\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n \r\n-  // Calculate total width required to draw the entire message.\r\n-  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double emojiWidth = fontSize * 0.734;\r\n-  double totalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter textPainter = TextPainter(\r\n-        text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      textPainter.layout();\r\n-      totalWidth += textPainter.width;\r\n+    // Constant font settings\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+\r\n+    // Measure one copy of the message's width\r\n+    double messageWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        messageWidth += tp.width;\r\n+      } else {\r\n+        messageWidth += emojiWidth;\r\n+      }\r\n+    }\r\n+\r\n+    // **Modified**: Set gapWidth to 0 for seamless scrolling\r\n+    double gapWidth = 0; // No gap between copies for continuous looping\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+\r\n+    // Handle short messages: if message fits, center it without scrolling\r\n+    if (messageWidth <= displayWidth) {\r\n+      marqueeWidth = messageWidth; // Only one copy needed\r\n     } else {\r\n-      totalWidth += emojiWidth;\r\n+      // For long messages, create a canvas with two copies for seamless scrolling\r\n+      marqueeWidth = messageWidth * 2; // Two copies, no gap\r\n     }\r\n-  }\r\n \r\n-  // Create a canvas wide enough to hold the entire message.\r\n-  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-  final int canvasHeight = rows * scale;\r\n+    // Create a marquee canvas\r\n+    int canvasWidth = marqueeWidth.ceil();\r\n+    int canvasHeight = displayHeight;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n-\r\n-  // Fill background with white.\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+      recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    );\r\n \r\n-  double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n+    // Fill background with white\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-  // Draw each segment onto the canvas.\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      // Paint text.\r\n-      final TextPainter textPainter = TextPainter(\r\n-        text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-      double offsetY = centerY - textPainter.height / 2;\r\n-      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += textPainter.width;\r\n-    } else {\r\n-      // Paint emoji.\r\n-      int index = int.parse(segment.text);\r\n-      if (index < emojiKeys.length) {\r\n-        var key = emojiKeys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n+    double centerY = canvasHeight / 2.0;\r\n+\r\n+    // Helper to draw one copy of the message starting at a given x-offset\r\n+    Future<void> drawMessage(double offsetX) async {\r\n+      double currentX = offsetX;\r\n+      for (var segment in segments) {\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+                text: segment.text,\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          double offsetY = centerY - tp.height / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              currentX += emojiWidth;\r\n+            }\r\n+          }\r\n         }\r\n       }\r\n     }\r\n-  }\r\n \r\n-  // Convert canvas to image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    // Draw the message based on message length\r\n+    if (messageWidth <= displayWidth) {\r\n+      // Center short messages\r\n+      await drawMessage((displayWidth - messageWidth) / 2);\r\n+    } else {\r\n+      // Draw two copies for long messages\r\n+      await drawMessage(0);\r\n+      await drawMessage(messageWidth);\r\n+    }\r\n \r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n+    // Finalize the marquee image\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample to LED matrix using the provided xOffset.\r\n-  List<List<bool>> matrix =\r\n-      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = xOffset + col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n-            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n-          } else {\r\n-            sum += 255;\r\n-            count++;\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    int effectiveOffset = messageWidth <= displayWidth\r\n+        ? 0 // No scrolling for short messages\r\n+        : (scrollOffset % messageWidth).floor(); // Seamless loop for long messages\r\n+\r\n+    // Downsample only the display window from the wide marquee image\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    const int threshold = 200; // Adjusted for anti-aliased text\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -291,11 +334,17 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+\r\n+  // Placeholder for hexStringToBool, assuming it exists elsewhere\r\n+  List<List<bool>> hexStringToBool(String hex) {\r\n+    // Implement if not already defined in your codebase\r\n+    throw UnimplementedError(\"hexStringToBool not implemented\");\r\n+  }\r\n }\r\n \r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740046461116,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -31,9 +31,9 @@\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n+          final List<List>? image = decodedData.cast<List<dynamic>>();\r\n           List<List<int>> imageData =\r\n               image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n"
                },
                {
                    "date": 1740046470907,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,7 +1,8 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module_utils/byte_array_utils.dart' as byteUtils;\r\n import 'package:badgemagic/bademagic_module_utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module_utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module_utils/image_utils.dart';\r\n"
                },
                {
                    "date": 1740046479175,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,8 +1,10 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/bademagic_module_utils/byte_array_utils.dart' as byteUtils;\r\n import 'package:badgemagic/bademagic_module_utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module_utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module_utils/image_utils.dart';\r\n"
                },
                {
                    "date": 1740046499071,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,18 +1,14 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-import 'package:badgemagic/bademagic_module_utils/byte_array_utils.dart' as byteUtils;\r\n-import 'package:badgemagic/bademagic_module_utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module_utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module_utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n-import 'package:logging/logging.dart';\r\n \r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n@@ -21,11 +17,8 @@\n   FileHelper fileHelper = FileHelper();\r\n \r\n   int controllerLength = 0;\r\n \r\n-  // Define logger explicitly for this file\r\n-  static final logger = logging.getLogger('Converters');\r\n-\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n       if (message[x] == '<' &&\r\n@@ -34,9 +27,9 @@\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n-          final List<List>? image = decodedData.cast<List<dynamic>>();\r\n+          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n           List<List<int>> imageData =\r\n               image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n@@ -55,18 +48,18 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    logger.d(\"Hex strings: $hexStrings\"); // Use local logger\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n   /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n   ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). For seamless scrolling,\r\n-  /// the marquee canvas contains two copies of the message with no gap (gapWidth = 0).\r\n-  /// The [scrollOffset] (in pixels) determines which part of the marquee is shown.\r\n-  /// To animate, update [scrollOffset] over time (e.g., via AnimationController).\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -119,39 +112,31 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // **Modified**: Set gapWidth to 0 for seamless scrolling\r\n-    double gapWidth = 0; // No gap between copies for continuous looping\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n \r\n-    // Handle short messages: if message fits, center it without scrolling\r\n-    if (messageWidth <= displayWidth) {\r\n-      marqueeWidth = messageWidth; // Only one copy needed\r\n-    } else {\r\n-      // For long messages, create a canvas with two copies for seamless scrolling\r\n-      marqueeWidth = messageWidth * 2; // Two copies, no gap\r\n-    }\r\n-\r\n-    // Create a marquee canvas\r\n-    int canvasWidth = marqueeWidth.ceil();\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n+    int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background with white\r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -189,19 +174,13 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw the message based on message length\r\n-    if (messageWidth <= displayWidth) {\r\n-      // Center short messages\r\n-      await drawMessage((displayWidth - messageWidth) / 2);\r\n-    } else {\r\n-      // Draw two copies for long messages\r\n-      await drawMessage(0);\r\n-      await drawMessage(messageWidth);\r\n-    }\r\n+    // Draw two copies of the message.\r\n+    await drawMessage(0);\r\n+    await drawMessage(marqueeWidth);\r\n \r\n-    // Finalize the marquee image\r\n+    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -210,16 +189,15 @@\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n     // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = messageWidth <= displayWidth\r\n-        ? 0 // No scrolling for short messages\r\n-        : (scrollOffset % messageWidth).floor(); // Seamless loop for long messages\r\n+    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image\r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    const int threshold = 200; // Adjusted for anti-aliased text\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n         int count = 0;\r\n@@ -337,17 +315,11 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-\r\n-  // Placeholder for hexStringToBool, assuming it exists elsewhere\r\n-  List<List<bool>> hexStringToBool(String hex) {\r\n-    // Implement if not already defined in your codebase\r\n-    throw UnimplementedError(\"hexStringToBool not implemented\");\r\n-  }\r\n }\r\n \r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n-}\n\\ No newline at end of file\n+}\r\n"
                },
                {
                    "date": 1740046745163,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -9,10 +9,9 @@\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -20,23 +19,20 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -48,30 +44,24 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n-    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n   }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n+\r\n+    // Parse message into segments\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n@@ -82,148 +72,150 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n+    // Calculate dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n+    final double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    final double emojiWidth = fontSize * 0.8; // Adjusted emoji width ratio\r\n \r\n-    // Constant font settings\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    // Measure one copy of the message's width\r\n-    double messageWidth = 0;\r\n+    // Pre-measure total width\r\n+    double totalWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n+        final tp = TextPainter(\r\n           text: TextSpan(\r\n-              text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n-        messageWidth += tp.width;\r\n+        totalWidth += tp.width;\r\n       } else {\r\n-        messageWidth += emojiWidth;\r\n+        totalWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+    // Calculate marquee parameters\r\n+    final double gapWidth = displayWidth * 0.5; // Reduced gap for smoother transition\r\n+    final double marqueeWidth = totalWidth + gapWidth;\r\n+    final int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    final int canvasHeight = displayHeight;\r\n \r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n-    int canvasWidth = (marqueeWidth * 2).ceil();\r\n-    int canvasHeight = displayHeight;\r\n-\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n+    // Create canvas\r\n+    final recorder = ui.PictureRecorder();\r\n+    final canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    // Fill background\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      Paint()..color = Colors.white,\r\n+    );\r\n \r\n-    double centerY = canvasHeight / 2.0;\r\n+    // Draw message copies\r\n+    Future<void> drawMessageCopy(double startX) async {\r\n+      double currentX = startX;\r\n+      double centerY = canvasHeight / 2;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n-    Future<void> drawMessage(double offsetX) async {\r\n-      double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n+          final tp = TextPainter(\r\n             text: TextSpan(\r\n-                text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+            ),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n-          double offsetY = centerY - tp.height / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n           currentX += tp.width;\r\n         } else {\r\n           int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n+          var keys = controllerData.imageCache.keys.toList();\r\n           if (index < keys.length) {\r\n             var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            var emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n+              final codec = await ui.instantiateImageCodec(\r\n                 emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(),\r\n                 targetHeight: emojiWidth.toInt(),\r\n               );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              final frame = await codec.getNextFrame();\r\n+              canvas.drawImage(\r\n+                frame.image,\r\n+                Offset(currentX, centerY - emojiWidth / 2),\r\n+                Paint(),\r\n+              );\r\n               currentX += emojiWidth;\r\n             }\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n-    await drawMessage(0);\r\n-    await drawMessage(marqueeWidth);\r\n+    // Draw two copies for seamless scrolling\r\n+    await drawMessageCopy(0);\r\n+    await drawMessageCopy(marqueeWidth);\r\n \r\n-    // Finalize the marquee image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    // Convert to matrix\r\n+    final picture = recorder.endRecording();\r\n+    final image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    \r\n     if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+      throw Exception(\"Failed to convert image to byte data\");\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+    final pixels = byteData.buffer.asUint8List();\r\n+    final effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+    \r\n+    // Create LED matrix\r\n+    List<List<bool>> matrix = List.generate(\r\n+      rows,\r\n+      (_) => List.generate(cols, (_) => false),\r\n+    );\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n-    const int threshold = 200;\r\n+    // Improved downsampling with better threshold handling\r\n+    const threshold = 180; // Adjusted threshold for better text clarity\r\n+    final scaleSq = scale * scale;\r\n+\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n+        int sumBrightness = 0;\r\n+        \r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n+            final pixelX = (effectiveOffset + col * scale + x) % canvasWidth;\r\n+            final pixelY = row * scale + y;\r\n+            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n+            \r\n+            if (idx + 3 < pixels.length) {\r\n+              final r = pixels[idx];\r\n+              final g = pixels[idx + 1];\r\n+              final b = pixels[idx + 2];\r\n+              sumBrightness += ((r + g + b) / 3).round();\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n+        \r\n+        final avgBrightness = sumBrightness / scaleSq;\r\n         matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n+\r\n     return matrix;\r\n   }\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740047718270,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -9,9 +9,10 @@\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -19,20 +20,23 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -44,11 +48,13 @@\n     if (isInverted) {\r\n       hexStrings = invertHex(hexStrings.join()).split('');\r\n       hexStrings = padHexString(hexStrings);\r\n     }\r\n+    logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n+  /// Enhanced marquee implementation with proper gap calculation\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n@@ -58,10 +64,8 @@\n   }) async {\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n-\r\n-    // Parse message into segments\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n@@ -72,146 +76,137 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n-    final double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    final double emojiWidth = fontSize * 0.8; // Adjusted emoji width ratio\r\n \r\n-    // Pre-measure total width\r\n-    double totalWidth = 0;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+\r\n+    double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n-        final tp = TextPainter(\r\n+        final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n+              text: segment.text,\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n-        totalWidth += tp.width;\r\n+        messageWidth += tp.width;\r\n       } else {\r\n-        totalWidth += emojiWidth;\r\n+        messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Calculate marquee parameters\r\n-    final double gapWidth = displayWidth * 0.5; // Reduced gap for smoother transition\r\n-    final double marqueeWidth = totalWidth + gapWidth;\r\n-    final int canvasWidth = (marqueeWidth * 2).ceil();\r\n-    final int canvasHeight = displayHeight;\r\n+    // Dynamic gap calculation based on message length\r\n+    double gapWidth = messageWidth < displayWidth ? displayWidth.toDouble() : 0;\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    int canvasHeight = displayHeight;\r\n \r\n-    // Create canvas\r\n-    final recorder = ui.PictureRecorder();\r\n-    final canvas = Canvas(\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      Paint()..color = Colors.white,\r\n-    );\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-    // Draw message copies\r\n-    Future<void> drawMessageCopy(double startX) async {\r\n-      double currentX = startX;\r\n-      double centerY = canvasHeight / 2;\r\n+    double centerY = canvasHeight / 2.0;\r\n \r\n+    Future<void> drawMessage(double offsetX) async {\r\n+      double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n-          final tp = TextPainter(\r\n+          final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-            ),\r\n+                text: segment.text,\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n-          tp.paint(canvas, Offset(currentX, centerY - tp.height / 2));\r\n+          double offsetY = centerY - tp.height / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n           currentX += tp.width;\r\n         } else {\r\n           int index = int.parse(segment.text);\r\n-          var keys = controllerData.imageCache.keys.toList();\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n           if (index < keys.length) {\r\n             var key = keys[index];\r\n-            var emojiBytes = controllerData.imageCache[key];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n-              final codec = await ui.instantiateImageCodec(\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n                 emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(),\r\n                 targetHeight: emojiWidth.toInt(),\r\n               );\r\n-              final frame = await codec.getNextFrame();\r\n-              canvas.drawImage(\r\n-                frame.image,\r\n-                Offset(currentX, centerY - emojiWidth / 2),\r\n-                Paint(),\r\n-              );\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n               currentX += emojiWidth;\r\n             }\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies for seamless scrolling\r\n-    await drawMessageCopy(0);\r\n-    await drawMessageCopy(marqueeWidth);\r\n+    // Draw two copies for seamless looping\r\n+    await drawMessage(0);\r\n+    await drawMessage(marqueeWidth);\r\n \r\n-    // Convert to matrix\r\n-    final picture = recorder.endRecording();\r\n-    final image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    \r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n     if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data\");\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    final pixels = byteData.buffer.asUint8List();\r\n-    final effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n-    \r\n-    // Create LED matrix\r\n-    List<List<bool>> matrix = List.generate(\r\n-      rows,\r\n-      (_) => List.generate(cols, (_) => false),\r\n-    );\r\n+    // Calculate effective scroll position\r\n+    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Improved downsampling with better threshold handling\r\n-    const threshold = 180; // Adjusted threshold for better text clarity\r\n-    final scaleSq = scale * scale;\r\n-\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n-        int sumBrightness = 0;\r\n-        \r\n+        int sum = 0;\r\n+        int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            final pixelX = (effectiveOffset + col * scale + x) % canvasWidth;\r\n-            final pixelY = row * scale + y;\r\n-            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n-            \r\n-            if (idx + 3 < pixels.length) {\r\n-              final r = pixels[idx];\r\n-              final g = pixels[idx + 1];\r\n-              final b = pixels[idx + 2];\r\n-              sumBrightness += ((r + g + b) / 3).round();\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            if (pixelX >= canvasWidth) pixelX -= canvasWidth;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n             }\r\n           }\r\n         }\r\n-        \r\n-        final avgBrightness = sumBrightness / scaleSq;\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n+        if (count > 0) {\r\n+          matrix[row][col] = (sum / count) < threshold;\r\n+        }\r\n       }\r\n     }\r\n-\r\n     return matrix;\r\n   }\r\n \r\n \r\n"
                },
                {
                    "date": 1740048153223,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -52,17 +59,23 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Enhanced marquee implementation with proper gap calculation\r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n+    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -80,14 +93,17 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n@@ -103,11 +119,13 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Dynamic gap calculation based on message length\r\n-    double gapWidth = messageWidth < displayWidth ? displayWidth.toDouble() : 0;\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n+\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n@@ -115,15 +133,17 @@\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -161,12 +181,13 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies for seamless looping\r\n+    // Draw two copies of the message.\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n+    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -174,13 +195,15 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Calculate effective scroll position\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n@@ -188,10 +211,9 @@\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            if (pixelX >= canvasWidth) pixelX -= canvasWidth;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -200,17 +222,15 @@\n               count++;\r\n             }\r\n           }\r\n         }\r\n-        if (count > 0) {\r\n-          matrix[row][col] = (sum / count) < threshold;\r\n-        }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -302,11 +322,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740048285691,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,22 +1,15 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -59,23 +52,17 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n+  /// Enhanced marquee implementation with proper gap calculation\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n   }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -93,17 +80,14 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n-    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n-    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n@@ -119,13 +103,11 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n+    // Dynamic gap calculation based on message length\r\n+    double gapWidth = messageWidth < displayWidth ? displayWidth.toDouble() : 0;\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n-\r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n@@ -133,17 +115,15 @@\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -181,13 +161,12 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n+    // Draw two copies for seamless looping\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n-    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -195,15 +174,13 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    // Calculate effective scroll position\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n@@ -211,9 +188,10 @@\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            if (pixelX >= canvasWidth) pixelX -= canvasWidth;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -222,15 +200,17 @@\n               count++;\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n+        if (count > 0) {\r\n+          matrix[row][col] = (sum / count) < threshold;\r\n+        }\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -322,5 +302,11 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\n\\ No newline at end of file\n+}\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740048294160,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -52,17 +52,23 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Enhanced marquee implementation with proper gap calculation\r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n+    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -80,14 +86,17 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n \r\n+    // Constant font settings\r\n     double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n     double emojiWidth = fontSize * 0.734;\r\n \r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n@@ -103,11 +112,13 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Dynamic gap calculation based on message length\r\n-    double gapWidth = messageWidth < displayWidth ? displayWidth.toDouble() : 0;\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n+\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n     int canvasWidth = (marqueeWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n \r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n@@ -115,15 +126,17 @@\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n+    // Fill background with white.\r\n     final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         bgPaint);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessage(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -161,12 +174,13 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies for seamless looping\r\n+    // Draw two copies of the message.\r\n     await drawMessage(0);\r\n     await drawMessage(marqueeWidth);\r\n \r\n+    // Finalize the marquee image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -174,13 +188,15 @@\n       throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n     final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Calculate effective scroll position\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n     int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n+    // Downsample only the display window from the wide marquee image.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n     const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sum = 0;\r\n@@ -188,10 +204,9 @@\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            if (pixelX >= canvasWidth) pixelX -= canvasWidth;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n             if (index + 3 < data.length) {\r\n               int r = data[index],\r\n                   g = data[index + 1],\r\n                   b = data[index + 2];\r\n@@ -200,17 +215,15 @@\n               count++;\r\n             }\r\n           }\r\n         }\r\n-        if (count > 0) {\r\n-          matrix[row][col] = (sum / count) < threshold;\r\n-        }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n     return matrix;\r\n   }\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740048421482,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,24 +1,23 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:flutter/material.dart';\r\n+import 'package:get_it/get_it.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n-import 'package:flutter/material.dart';\r\n-import 'package:get_it/get_it.dart';\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n+  final InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n-  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-  ImageUtils imageUtils = ImageUtils();\r\n-  FileHelper fileHelper = FileHelper();\r\n+  final DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+  final ImageUtils imageUtils = ImageUtils();\r\n+  final FileHelper fileHelper = FileHelper();\r\n \r\n-  int controllerLength = 0;\r\n-\r\n+  /// Converts a message (with optional image/emoji placeholders) to LED hex.\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n       if (message[x] == '<' &&\r\n@@ -45,30 +44,28 @@\n         }\r\n       }\r\n     }\r\n     if (isInverted) {\r\n-      hexStrings = invertHex(hexStrings.join()).split('');\r\n-      hexStrings = padHexString(hexStrings);\r\n+      String joinedHex = hexStrings.join();\r\n+      String inverted = invertHex(joinedHex);\r\n+      hexStrings = padHexString(inverted.split(''));\r\n     }\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n+  /// Renders the provided message as a scrolling marquee on an LED matrix.\r\n+  /// The [cols]×[rows] display is scaled by [scale] for better resolution.\r\n+  /// [scrollOffset] (in pixels) determines the current horizontal position.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n   }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n+    // 1. Parse the message into segments (plain text and emoji placeholders).\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -77,34 +74,32 @@\n           text: message.substring(lastMatchEnd, match.start),\r\n           isEmoji: false,\r\n         ));\r\n       }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Fixed display dimensions\r\n+    // 2. Determine display and font dimensions.\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n+    double baseFontSize = textStyle.fontSize ?? 16;\r\n+    double fontSize = baseFontSize * scale;\r\n+    double emojiWidth = fontSize * 0.734; // estimated emoji width\r\n \r\n-    // Constant font settings\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    // Measure one copy of the message's width\r\n+    // 3. Measure the width of one copy of the message.\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n       if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n               text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+              style: textStyle.copyWith(\r\n+                  color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n@@ -112,40 +107,38 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n+    // 4. Define a gap between message copies (here, one full display width).\r\n     double gapWidth = displayWidth.toDouble();\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+    double marqueeCycleWidth = messageWidth + gapWidth;\r\n \r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n-    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    // 5. Create an offscreen canvas wide enough for two cycles (for seamless looping).\r\n+    int canvasWidth = (marqueeCycleWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n-\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    // Fill the background (white) so that anti-aliasing is handled properly.\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+        Paint()..color = Colors.white);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n-    Future<void> drawMessage(double offsetX) async {\r\n+    // 6. Helper function to draw one copy of the message starting at a given x-offset.\r\n+    Future<void> drawMessageAtOffset(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n                 text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n+                style: textStyle.copyWith(\r\n+                    color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n           double offsetY = centerY - tp.height / 2;\r\n@@ -165,65 +158,73 @@\n               );\r\n               ui.FrameInfo fi = await codec.getNextFrame();\r\n               ui.Image emojiImage = fi.image;\r\n               double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), Paint());\r\n               currentX += emojiWidth;\r\n+            } else {\r\n+              currentX += emojiWidth;\r\n             }\r\n+          } else {\r\n+            currentX += emojiWidth;\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies of the message.\r\n-    await drawMessage(0);\r\n-    await drawMessage(marqueeWidth);\r\n+    // 7. Draw two copies: one at x=0 and one at x=marqueeCycleWidth.\r\n+    await drawMessageAtOffset(0);\r\n+    await drawMessageAtOffset(marqueeCycleWidth);\r\n \r\n-    // Finalize the marquee image.\r\n+    // 8. Finalize the canvas drawing and retrieve the full image.\r\n     final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ui.Image fullImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n-        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+        await fullImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n     if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n+      throw Exception(\"Failed to convert rendered image to byte data.\");\r\n     }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+    final Uint8List imageData = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n+    // 9. Compute the effective offset (within one cycle) based on scrollOffset.\r\n+    int effectiveOffset = (scrollOffset % marqueeCycleWidth).floor();\r\n \r\n-    // Downsample only the display window from the wide marquee image.\r\n+    // 10. Downsample the portion of the image corresponding to the display area.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n-    const int threshold = 200;\r\n+    const int threshold = 200; // brightness threshold\r\n+\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n+        int sumBrightness = 0;\r\n+        int pixelCount = 0;\r\n+        // Each LED matrix cell corresponds to a block of size [scale]×[scale] pixels.\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < imageData.length) {\r\n+              int r = imageData[index];\r\n+              int g = imageData[index + 1];\r\n+              int b = imageData[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n+              sumBrightness += brightness;\r\n+              pixelCount++;\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n+        double avgBrightness = sumBrightness / pixelCount;\r\n+        // A lower average brightness means the pixel was drawn (black text) so we light it up.\r\n         matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n+\r\n     return matrix;\r\n   }\r\n \r\n+  // --- Helper methods for converting bitmaps to LED hex strings ---\r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -260,32 +261,32 @@\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n+    List<List<int>> padded =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n+        padded[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n+          padded[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n+        padded[i][k++] = 0;\r\n       }\r\n     }\r\n-    logger.d(\"Padded image: $list\");\r\n+    logger.d(\"Padded image: $padded\");\r\n     List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+    for (int i = 0; i < padded[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n+          stBuilder.write(padded[k][j]);\r\n         }\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n@@ -298,17 +299,17 @@\n \r\n   static String invertHex(String hex) {\r\n     StringBuffer invertedHex = StringBuffer();\r\n     for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n+      String invertedDigit =\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n+      invertedHex.write(invertedDigit);\r\n     }\r\n     return invertedHex.toString();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+    List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n     }).toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1);\r\n@@ -317,8 +318,9 @@\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n \r\n+/// Simple helper class to hold a segment of text (or an emoji placeholder).\r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n"
                },
                {
                    "date": 1740048427950,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -309,9 +309,9 @@\n \r\n   List<String> padHexString(List<String> hexString) {\r\n     List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n+    }).cast<List<bool>>().toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1);\r\n       hexArray[i].add(1);\r\n     }\r\n"
                },
                {
                    "date": 1740048433766,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -314,9 +314,9 @@\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1);\r\n       hexArray[i].add(1);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n+    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n   }\r\n }\r\n \r\n /// Simple helper class to hold a segment of text (or an emoji placeholder).\r\n"
                },
                {
                    "date": 1740048443217,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -311,9 +311,9 @@\n     List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((e) => e ? 1 : 0).toList();\r\n     }).cast<List<bool>>().toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n+      hexArray[i].insert(0, 1 as bool);\r\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n   }\r\n"
                },
                {
                    "date": 1740048611776,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -15,12 +15,13 @@\n   final DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   final ImageUtils imageUtils = ImageUtils();\r\n   final FileHelper fileHelper = FileHelper();\r\n \r\n-  /// Converts a message (with optional image/emoji placeholders) to LED hex.\r\n+  /// Converts a message (with optional image/emoji placeholders) to LED hex strings.\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n+      // Check for a placeholder pattern like \"<XX>>\"\r\n       if (message[x] == '<' &&\r\n           message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n@@ -52,20 +53,20 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Renders the provided message as a scrolling marquee on an LED matrix.\r\n-  /// The [cols]×[rows] display is scaled by [scale] for better resolution.\r\n-  /// [scrollOffset] (in pixels) determines the current horizontal position.\r\n+  /// Renders the provided message as a scrolling marquee for an LED matrix.\r\n+  /// The display area is defined by [cols]×[rows] (each cell scaled by [scale] for resolution).\r\n+  /// [scrollOffset] (in pixels) defines the horizontal offset into the marquee.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10,\r\n     double scrollOffset = 0,\r\n   }) async {\r\n-    // 1. Parse the message into segments (plain text and emoji placeholders).\r\n+    // 1. Parse message into segments (plain text and emoji placeholders in format <<XX>>)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n     for (final match in regex.allMatches(message)) {\r\n@@ -82,14 +83,17 @@\n       segments.add(\r\n           _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // 2. Determine display and font dimensions.\r\n+    // 2. Calculate display dimensions.\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n+\r\n+    // Multiply font size by scale factor for higher resolution rendering.\r\n     double baseFontSize = textStyle.fontSize ?? 16;\r\n     double fontSize = baseFontSize * scale;\r\n-    double emojiWidth = fontSize * 0.734; // estimated emoji width\r\n+    // Estimate emoji width (adjust multiplier as needed)\r\n+    double emojiWidth = fontSize * 0.734;\r\n \r\n     // 3. Measure the width of one copy of the message.\r\n     double messageWidth = 0;\r\n     for (var segment in segments) {\r\n@@ -107,29 +111,30 @@\n         messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // 4. Define a gap between message copies (here, one full display width).\r\n+    // 4. Define a gap between the message copies. Here we use one full display width.\r\n     double gapWidth = displayWidth.toDouble();\r\n+    // One marquee cycle consists of the message plus the gap.\r\n     double marqueeCycleWidth = messageWidth + gapWidth;\r\n \r\n-    // 5. Create an offscreen canvas wide enough for two cycles (for seamless looping).\r\n+    // 5. Create an offscreen canvas wide enough for two cycles for seamless scrolling.\r\n     int canvasWidth = (marqueeCycleWidth * 2).ceil();\r\n     int canvasHeight = displayHeight;\r\n     final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n     final Canvas canvas = Canvas(\r\n       recorder,\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill the background (white) so that anti-aliasing is handled properly.\r\n+    // Fill the background with white.\r\n     canvas.drawRect(\r\n         Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n         Paint()..color = Colors.white);\r\n \r\n     double centerY = canvasHeight / 2.0;\r\n \r\n-    // 6. Helper function to draw one copy of the message starting at a given x-offset.\r\n+    // 6. Draw one copy of the message starting at a given x-offset.\r\n     Future<void> drawMessageAtOffset(double offsetX) async {\r\n       double currentX = offsetX;\r\n       for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n@@ -140,8 +145,9 @@\n                     color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n+          // Center text vertically.\r\n           double offsetY = centerY - tp.height / 2;\r\n           tp.paint(canvas, Offset(currentX, offsetY));\r\n           currentX += tp.width;\r\n         } else {\r\n@@ -161,8 +167,9 @@\n               double offsetY = centerY - emojiWidth / 2;\r\n               canvas.drawImage(emojiImage, Offset(currentX, offsetY), Paint());\r\n               currentX += emojiWidth;\r\n             } else {\r\n+              // If no image found, skip a block.\r\n               currentX += emojiWidth;\r\n             }\r\n           } else {\r\n             currentX += emojiWidth;\r\n@@ -170,13 +177,13 @@\n         }\r\n       }\r\n     }\r\n \r\n-    // 7. Draw two copies: one at x=0 and one at x=marqueeCycleWidth.\r\n+    // 7. Draw two copies: one starting at x = 0 and another at x = marqueeCycleWidth.\r\n     await drawMessageAtOffset(0);\r\n     await drawMessageAtOffset(marqueeCycleWidth);\r\n \r\n-    // 8. Finalize the canvas drawing and retrieve the full image.\r\n+    // 8. Finalize the canvas drawing.\r\n     final ui.Picture picture = recorder.endRecording();\r\n     final ui.Image fullImage = await picture.toImage(canvasWidth, canvasHeight);\r\n     final ByteData? byteData =\r\n         await fullImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n@@ -184,21 +191,23 @@\n       throw Exception(\"Failed to convert rendered image to byte data.\");\r\n     }\r\n     final Uint8List imageData = byteData.buffer.asUint8List();\r\n \r\n-    // 9. Compute the effective offset (within one cycle) based on scrollOffset.\r\n+    // 9. Calculate effective offset within one marquee cycle.\r\n+    // This ensures that as scrollOffset increases, the window slides seamlessly.\r\n     int effectiveOffset = (scrollOffset % marqueeCycleWidth).floor();\r\n \r\n-    // 10. Downsample the portion of the image corresponding to the display area.\r\n+    // 10. Downsample the image to the LED matrix resolution.\r\n     List<List<bool>> matrix =\r\n         List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    const int threshold = 200; // brightness threshold\r\n+    // Use a brightness threshold that works with your anti-aliased drawing.\r\n+    const int threshold = 200;\r\n \r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n         int sumBrightness = 0;\r\n         int pixelCount = 0;\r\n-        // Each LED matrix cell corresponds to a block of size [scale]×[scale] pixels.\r\n+        // Each cell corresponds to a block of [scale]×[scale] pixels.\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n             int pixelX = effectiveOffset + col * scale + x;\r\n             int pixelY = row * scale + y;\r\n@@ -206,16 +215,17 @@\n             if (index + 3 < imageData.length) {\r\n               int r = imageData[index];\r\n               int g = imageData[index + 1];\r\n               int b = imageData[index + 2];\r\n+              // Average brightness of the pixel.\r\n               int brightness = ((r + g + b) / 3).round();\r\n               sumBrightness += brightness;\r\n               pixelCount++;\r\n             }\r\n           }\r\n         }\r\n         double avgBrightness = sumBrightness / pixelCount;\r\n-        // A lower average brightness means the pixel was drawn (black text) so we light it up.\r\n+        // If the average brightness is below the threshold, we consider the LED “on.”\r\n         matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n \r\n@@ -227,8 +237,9 @@\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n+    // Trim empty columns from the left.\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -241,8 +252,9 @@\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n+    // Trim empty columns from the right.\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -308,19 +320,19 @@\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n     List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).cast<List<bool>>().toList();\r\n+      return e.map((bit) => bit ? 1 : 0).toList();\r\n+    }).toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1 as bool);\r\n+      hexArray[i].insert(0, 1);\r\n       hexArray[i].add(1);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n \r\n-/// Simple helper class to hold a segment of text (or an emoji placeholder).\r\n+/// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n class _Segment {\r\n   final String text;\r\n   final bool isEmoji;\r\n   _Segment({required this.text, required this.isEmoji});\r\n"
                },
                {
                    "date": 1740048625723,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -321,11 +321,11 @@\n \r\n   List<String> padHexString(List<String> hexString) {\r\n     List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n       return e.map((bit) => bit ? 1 : 0).toList();\r\n-    }).toList();\r\n+    }).cast<List<bool>>().toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n+      hexArray[i].insert(0, 1 as bool);\r\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n"
                },
                {
                    "date": 1740048641155,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -326,9 +326,9 @@\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1 as bool);\r\n       hexArray[i].add(1);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n+    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n   }\r\n }\r\n \r\n /// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n"
                },
                {
                    "date": 1740048702103,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,42 +1,45 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:flutter/material.dart';\r\n-import 'package:get_it/get_it.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n+import 'package:flutter/material.dart';\r\n+import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n-  final InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n-  final DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n-  final ImageUtils imageUtils = ImageUtils();\r\n-  final FileHelper fileHelper = FileHelper();\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n+  ImageUtils imageUtils = ImageUtils();\r\n+  FileHelper fileHelper = FileHelper();\r\n \r\n-  /// Converts a message (with optional image/emoji placeholders) to LED hex strings.\r\n+  int controllerLength = 0;\r\n+\r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      // Check for a placeholder pattern like \"<XX>>\"\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -45,188 +48,186 @@\n         }\r\n       }\r\n     }\r\n     if (isInverted) {\r\n-      String joinedHex = hexStrings.join();\r\n-      String inverted = invertHex(joinedHex);\r\n-      hexStrings = padHexString(inverted.split(''));\r\n+      hexStrings = invertHex(hexStrings.join()).split('');\r\n+      hexStrings = padHexString(hexStrings);\r\n     }\r\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Renders the provided message as a scrolling marquee for an LED matrix.\r\n-  /// The display area is defined by [cols]×[rows] (each cell scaled by [scale] for resolution).\r\n-  /// [scrollOffset] (in pixels) defines the horizontal offset into the marquee.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n     int scale = 10,\r\n     double scrollOffset = 0,\r\n   }) async {\r\n-    // 1. Parse message into segments (plain text and emoji placeholders in format <<XX>>)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n+    \r\n+    // Parse message into segments\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n           isEmoji: false,\r\n         ));\r\n       }\r\n-      segments.add(_Segment(text: match.group(1)!, isEmoji: true));\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // 2. Calculate display dimensions.\r\n+    // Calculate dimensions and scaling\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    \r\n+    // Ensure text fits vertically\r\n+    if (scaledFontSize > displayHeight * 0.9) {\r\n+      scaledFontSize = displayHeight * 0.9;\r\n+    }\r\n+    \r\n+    double emojiWidth = scaledFontSize;\r\n+    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-    // Multiply font size by scale factor for higher resolution rendering.\r\n-    double baseFontSize = textStyle.fontSize ?? 16;\r\n-    double fontSize = baseFontSize * scale;\r\n-    // Estimate emoji width (adjust multiplier as needed)\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    // 3. Measure the width of one copy of the message.\r\n+    // Measure total width\r\n     double messageWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        messageWidth += widthWithGap;\r\n+      } else {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(\r\n-                  color: Colors.black, fontSize: fontSize)),\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize),\r\n+          ),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n-      } else {\r\n-        messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // 4. Define a gap between the message copies. Here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n-    // One marquee cycle consists of the message plus the gap.\r\n-    double marqueeCycleWidth = messageWidth + gapWidth;\r\n+    // Calculate marquee parameters\r\n+    double gapWidth = displayWidth * 0.5; // Half display width for smoother transition\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+    int canvasWidth = (marqueeWidth * 2).ceil(); // Two copies for seamless scrolling\r\n+    double effectiveOffset = -(scrollOffset % marqueeWidth);\r\n+    if (effectiveOffset > 0) effectiveOffset -= marqueeWidth;\r\n \r\n-    // 5. Create an offscreen canvas wide enough for two cycles for seamless scrolling.\r\n-    int canvasWidth = (marqueeCycleWidth * 2).ceil();\r\n-    int canvasHeight = displayHeight;\r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n+    // Create canvas\r\n+    final recorder = ui.PictureRecorder();\r\n+    final canvas = Canvas(\r\n       recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill the background with white.\r\n+    // Fill background\r\n     canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        Paint()..color = Colors.white);\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      Paint()..color = Colors.white,\r\n+    );\r\n \r\n-    double centerY = canvasHeight / 2.0;\r\n+    // Draw message copies\r\n+    Future<void> drawMessageCopy(double startX) async {\r\n+      double currentX = startX;\r\n+      double centerY = displayHeight / 2;\r\n \r\n-    // 6. Draw one copy of the message starting at a given x-offset.\r\n-    Future<void> drawMessageAtOffset(double offsetX) async {\r\n-      double currentX = offsetX;\r\n-      for (var segment in segments) {\r\n+      for (int i = 0; i < segments.length; i++) {\r\n+        final segment = segments[i];\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n-                text: segment.text,\r\n-                style: textStyle.copyWith(\r\n-                    color: Colors.black, fontSize: fontSize)),\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black),\r\n+            ),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n-          // Center text vertically.\r\n-          double offsetY = centerY - tp.height / 2;\r\n+          var metrics = tp.computeLineMetrics();\r\n+          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+          double offsetY = centerY - (tp.height - descent) / 2;\r\n           tp.paint(canvas, Offset(currentX, offsetY));\r\n           currentX += tp.width;\r\n         } else {\r\n           int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n+          var keys = controllerData.imageCache.keys.toList();\r\n           if (index < keys.length) {\r\n             var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+            var emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n+              final codec = await ui.instantiateImageCodec(\r\n                 emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(),\r\n                 targetHeight: emojiWidth.toInt(),\r\n               );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n+              final frame = await codec.getNextFrame();\r\n               double offsetY = centerY - emojiWidth / 2;\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), Paint());\r\n+              canvas.drawImage(frame.image, Offset(currentX, offsetY), Paint());\r\n               currentX += emojiWidth;\r\n-            } else {\r\n-              // If no image found, skip a block.\r\n-              currentX += emojiWidth;\r\n+              if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+                currentX += emojiGap;\r\n+              }\r\n             }\r\n-          } else {\r\n-            currentX += emojiWidth;\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // 7. Draw two copies: one starting at x = 0 and another at x = marqueeCycleWidth.\r\n-    await drawMessageAtOffset(0);\r\n-    await drawMessageAtOffset(marqueeCycleWidth);\r\n+    // Draw two copies for seamless scrolling\r\n+    await drawMessageCopy(effectiveOffset);\r\n+    await drawMessageCopy(effectiveOffset + marqueeWidth);\r\n \r\n-    // 8. Finalize the canvas drawing.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image fullImage = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await fullImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    // Convert to image\r\n+    final picture = recorder.endRecording();\r\n+    final image = await picture.toImage(canvasWidth, displayHeight);\r\n+    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    \r\n     if (byteData == null) {\r\n-      throw Exception(\"Failed to convert rendered image to byte data.\");\r\n+      throw Exception(\"Failed to convert image to byte data\");\r\n     }\r\n-    final Uint8List imageData = byteData.buffer.asUint8List();\r\n \r\n-    // 9. Calculate effective offset within one marquee cycle.\r\n-    // This ensures that as scrollOffset increases, the window slides seamlessly.\r\n-    int effectiveOffset = (scrollOffset % marqueeCycleWidth).floor();\r\n+    final pixels = byteData.buffer.asUint8List();\r\n+    \r\n+    // Create LED matrix with improved downsampling\r\n+    List<List<bool>> matrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    const threshold = 180;\r\n \r\n-    // 10. Downsample the image to the LED matrix resolution.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Use a brightness threshold that works with your anti-aliased drawing.\r\n-    const int threshold = 200;\r\n-\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n-        int sumBrightness = 0;\r\n-        int pixelCount = 0;\r\n-        // Each cell corresponds to a block of [scale]×[scale] pixels.\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        \r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < imageData.length) {\r\n-              int r = imageData[index];\r\n-              int g = imageData[index + 1];\r\n-              int b = imageData[index + 2];\r\n-              // Average brightness of the pixel.\r\n+            final pixelX = (col * scale + x) % canvasWidth;\r\n+            final pixelY = row * scale + y;\r\n+            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n+            \r\n+            if (idx + 3 < pixels.length) {\r\n+              int r = pixels[idx];\r\n+              int g = pixels[idx + 1];\r\n+              int b = pixels[idx + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n-              sumBrightness += brightness;\r\n-              pixelCount++;\r\n+              if (brightness < threshold) darkCount++;\r\n+              totalCount++;\r\n             }\r\n           }\r\n         }\r\n-        double avgBrightness = sumBrightness / pixelCount;\r\n-        // If the average brightness is below the threshold, we consider the LED “on.”\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n+        \r\n+        matrix[row][col] = darkCount > (totalCount * 0.2);\r\n       }\r\n     }\r\n \r\n     return matrix;\r\n@@ -324,9 +325,9 @@\n       return e.map((bit) => bit ? 1 : 0).toList();\r\n     }).cast<List<bool>>().toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n       hexArray[i].insert(0, 1 as bool);\r\n-      hexArray[i].add(1);\r\n+      hexArray[i].add(1 as bool);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n   }\r\n }\r\n"
                },
                {
                    "date": 1740048713761,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -332,9 +332,5 @@\n   }\r\n }\r\n \r\n /// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+\r\n"
                },
                {
                    "date": 1740048790510,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,9 +16,10 @@\n   _Segment({required this.text, required this.isEmoji});\r\n }\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -26,20 +27,23 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -55,192 +59,172 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    \r\n-    // Parse message into segments\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Calculate dimensions and scaling\r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    \r\n-    // Ensure text fits vertically\r\n-    if (scaledFontSize > displayHeight * 0.9) {\r\n-      scaledFontSize = displayHeight * 0.9;\r\n-    }\r\n-    \r\n-    double emojiWidth = scaledFontSize;\r\n-    double emojiGap = scaledFontSize * 0.2;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    // Measure total width\r\n-    double messageWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        messageWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        messageWidth += tp.width;\r\n-      }\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    // Calculate marquee parameters\r\n-    double gapWidth = displayWidth * 0.5; // Half display width for smoother transition\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n-    int canvasWidth = (marqueeWidth * 2).ceil(); // Two copies for seamless scrolling\r\n-    double effectiveOffset = -(scrollOffset % marqueeWidth);\r\n-    if (effectiveOffset > 0) effectiveOffset -= marqueeWidth;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    // Create canvas\r\n-    final recorder = ui.PictureRecorder();\r\n-    final canvas = Canvas(\r\n-      recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-    );\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    // Fill background\r\n-    canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-      Paint()..color = Colors.white,\r\n-    );\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    // Draw message copies\r\n-    Future<void> drawMessageCopy(double startX) async {\r\n-      double currentX = startX;\r\n-      double centerY = displayHeight / 2;\r\n-\r\n-      for (int i = 0; i < segments.length; i++) {\r\n-        final segment = segments[i];\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black),\r\n-            ),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          var metrics = tp.computeLineMetrics();\r\n-          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-          double offsetY = centerY - (tp.height - descent) / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          var keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            var emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              final codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              final frame = await codec.getNextFrame();\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              canvas.drawImage(frame.image, Offset(currentX, offsetY), Paint());\r\n-              currentX += emojiWidth;\r\n-              if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-                currentX += emojiGap;\r\n-              }\r\n-            }\r\n-          }\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Draw two copies for seamless scrolling\r\n-    await drawMessageCopy(effectiveOffset);\r\n-    await drawMessageCopy(effectiveOffset + marqueeWidth);\r\n-\r\n-    // Convert to image\r\n-    final picture = recorder.endRecording();\r\n-    final image = await picture.toImage(canvasWidth, displayHeight);\r\n-    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    \r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data\");\r\n-    }\r\n-\r\n-    final pixels = byteData.buffer.asUint8List();\r\n-    \r\n-    // Create LED matrix with improved downsampling\r\n-    List<List<bool>> matrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    const threshold = 180;\r\n-\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        \r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            final pixelX = (col * scale + x) % canvasWidth;\r\n-            final pixelY = row * scale + y;\r\n-            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n-            \r\n-            if (idx + 3 < pixels.length) {\r\n-              int r = pixels[idx];\r\n-              int g = pixels[idx + 1];\r\n-              int b = pixels[idx + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < threshold) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        \r\n-        matrix[row][col] = darkCount > (totalCount * 0.2);\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n-  // --- Helper methods for converting bitmaps to LED hex strings ---\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n-    // Trim empty columns from the left.\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -253,9 +237,8 @@\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n-    // Trim empty columns from the right.\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -274,32 +257,32 @@\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n-    List<List<int>> padded =\r\n+    List<List<int>> list =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n-        padded[i][k++] = 0;\r\n+        list[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n-          padded[i][k++] = image[i][j];\r\n+          list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n-        padded[i][k++] = 0;\r\n+        list[i][k++] = 0;\r\n       }\r\n     }\r\n-    logger.d(\"Padded image: $padded\");\r\n+    logger.d(\"Padded image: $list\");\r\n     List<String> allHexs = [];\r\n-    for (int i = 0; i < padded[0].length ~/ 8; i++) {\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(padded[k][j]);\r\n+          stBuilder.write(list[k][j]);\r\n         }\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n@@ -312,25 +295,22 @@\n \r\n   static String invertHex(String hex) {\r\n     StringBuffer invertedHex = StringBuffer();\r\n     for (int i = 0; i < hex.length; i++) {\r\n-      String invertedDigit =\r\n+      String invertedHexDigit =\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedDigit);\r\n+      invertedHex.write(invertedHexDigit);\r\n     }\r\n     return invertedHex.toString();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((bit) => bit ? 1 : 0).toList();\r\n-    }).cast<List<bool>>().toList();\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1 as bool);\r\n-      hexArray[i].add(1 as bool);\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-/// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n-\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740048989258,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -16,10 +16,9 @@\n   _Segment({required this.text, required this.isEmoji});\r\n }\r\n \r\n class Converters {\r\n-  InlineImageProvider controllerData =\r\n-      GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -27,23 +26,20 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData =\r\n-              image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs =\r\n-              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -59,172 +55,192 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    double scrollOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    \r\n+    // Parse message into segments\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start),\r\n+          isEmoji: false,\r\n+        ));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    // Calculate dimensions and scaling\r\n+    final int displayWidth = cols * scale;\r\n+    final int displayHeight = rows * scale;\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    \r\n+    // Ensure text fits vertically\r\n+    if (scaledFontSize > displayHeight * 0.9) {\r\n+      scaledFontSize = displayHeight * 0.9;\r\n+    }\r\n+    \r\n+    double emojiWidth = scaledFontSize;\r\n+    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    // Measure total width\r\n+    double messageWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        messageWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        messageWidth += tp.width;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    // Calculate marquee parameters\r\n+    double gapWidth = displayWidth * 0.5; // Half display width for smoother transition\r\n+    double marqueeWidth = messageWidth + gapWidth;\r\n+    int canvasWidth = (marqueeWidth * 2).ceil(); // Two copies for seamless scrolling\r\n+    double effectiveOffset = -(scrollOffset % marqueeWidth);\r\n+    if (effectiveOffset > 0) effectiveOffset -= marqueeWidth;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    // Create canvas\r\n+    final recorder = ui.PictureRecorder();\r\n+    final canvas = Canvas(\r\n+      recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+    );\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    // Fill background\r\n+    canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      Paint()..color = Colors.white,\r\n+    );\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    // Draw message copies\r\n+    Future<void> drawMessageCopy(double startX) async {\r\n+      double currentX = startX;\r\n+      double centerY = displayHeight / 2;\r\n+\r\n+      for (int i = 0; i < segments.length; i++) {\r\n+        final segment = segments[i];\r\n+        if (!segment.isEmoji) {\r\n+          final TextPainter tp = TextPainter(\r\n+            text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black),\r\n+            ),\r\n+            textDirection: TextDirection.ltr,\r\n+          );\r\n+          tp.layout();\r\n+          var metrics = tp.computeLineMetrics();\r\n+          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+          double offsetY = centerY - (tp.height - descent) / 2;\r\n+          tp.paint(canvas, Offset(currentX, offsetY));\r\n+          currentX += tp.width;\r\n+        } else {\r\n+          int index = int.parse(segment.text);\r\n+          var keys = controllerData.imageCache.keys.toList();\r\n+          if (index < keys.length) {\r\n+            var key = keys[index];\r\n+            var emojiBytes = controllerData.imageCache[key];\r\n+            if (emojiBytes != null) {\r\n+              final codec = await ui.instantiateImageCodec(\r\n+                emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(),\r\n+                targetHeight: emojiWidth.toInt(),\r\n+              );\r\n+              final frame = await codec.getNextFrame();\r\n+              double offsetY = centerY - emojiWidth / 2;\r\n+              canvas.drawImage(frame.image, Offset(currentX, offsetY), Paint());\r\n+              currentX += emojiWidth;\r\n+              if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+                currentX += emojiGap;\r\n+              }\r\n+            }\r\n+          }\r\n         }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    // Draw two copies for seamless scrolling\r\n+    await drawMessageCopy(effectiveOffset);\r\n+    await drawMessageCopy(effectiveOffset + marqueeWidth);\r\n+\r\n+    // Convert to image\r\n+    final picture = recorder.endRecording();\r\n+    final image = await picture.toImage(canvasWidth, displayHeight);\r\n+    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    \r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data\");\r\n+    }\r\n+\r\n+    final pixels = byteData.buffer.asUint8List();\r\n+    \r\n+    // Create LED matrix with improved downsampling\r\n+    List<List<bool>> matrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    const threshold = 180;\r\n+\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        \r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            final pixelX = (col * scale + x) % canvasWidth;\r\n+            final pixelY = row * scale + y;\r\n+            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n+            \r\n+            if (idx + 3 < pixels.length) {\r\n+              int r = pixels[idx];\r\n+              int g = pixels[idx + 1];\r\n+              int b = pixels[idx + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < threshold) darkCount++;\r\n+              totalCount++;\r\n+            }\r\n           }\r\n         }\r\n+        \r\n+        matrix[row][col] = darkCount > (totalCount * 0.2);\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n+  // --- Helper methods for converting bitmaps to LED hex strings ---\r\n \r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n+    // Trim empty columns from the left.\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -237,8 +253,9 @@\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n+    // Trim empty columns from the right.\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -257,32 +274,32 @@\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n-    List<List<int>> list =\r\n+    List<List<int>> padded =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n-        list[i][k++] = 0;\r\n+        padded[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n-          list[i][k++] = image[i][j];\r\n+          padded[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n-        list[i][k++] = 0;\r\n+        padded[i][k++] = 0;\r\n       }\r\n     }\r\n-    logger.d(\"Padded image: $list\");\r\n+    logger.d(\"Padded image: $padded\");\r\n     List<String> allHexs = [];\r\n-    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n+    for (int i = 0; i < padded[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(list[k][j]);\r\n+          stBuilder.write(padded[k][j]);\r\n         }\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n@@ -295,22 +312,25 @@\n \r\n   static String invertHex(String hex) {\r\n     StringBuffer invertedHex = StringBuffer();\r\n     for (int i = 0; i < hex.length; i++) {\r\n-      String invertedHexDigit =\r\n+      String invertedDigit =\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedHexDigit);\r\n+      invertedHex.write(invertedDigit);\r\n     }\r\n     return invertedHex.toString();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((e) => e ? 1 : 0).toList();\r\n-    }).toList();\r\n+    List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((bit) => bit ? 1 : 0).toList();\r\n+    }).cast<List<bool>>().toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1);\r\n\\ No newline at end of file\n-      hexArray[i].add(1);\r\n+      hexArray[i].insert(0, 1 as bool);\r\n+      hexArray[i].add(1 as bool);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray, true);\r\n+    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n   }\r\n-}\n+}\r\n+\r\n+/// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n+\r\n"
                },
                {
                    "date": 1740048995154,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,24 +1,18 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n-  InlineImageProvider controllerData = GetIt.instance.get<InlineImageProvider>();\r\n+  InlineImageProvider controllerData =\r\n+      GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n   ImageUtils imageUtils = ImageUtils();\r\n   FileHelper fileHelper = FileHelper();\r\n \r\n@@ -26,20 +20,23 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n           List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n           final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n-          List<List<int>> imageData = image!.map((list) => list.cast<int>()).toList();\r\n+          List<List<int>> imageData =\r\n+              image!.map((list) => list.cast<int>()).toList();\r\n           hexStrings += convertBitmapToLEDHex(imageData, true);\r\n           x += 5;\r\n         } else {\r\n-          List<String> hs = await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n+          List<String> hs =\r\n+              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n           hexStrings.addAll(hs);\r\n           x += 5;\r\n         }\r\n       } else {\r\n@@ -55,21 +52,26 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n+  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n+  ///\r\n+  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n+  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n+  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n+  /// of the marquee is shown in the LED matrix.\r\n   Future<List<List<bool>>> renderTextToMatrix(\r\n     String message,\r\n     TextStyle textStyle, {\r\n     int cols = 44,\r\n     int rows = 11,\r\n-    int scale = 10,\r\n-    double scrollOffset = 0,\r\n+    int scale = 10, // scale factor for better resolution\r\n+    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n   }) async {\r\n+    // Parse message into segments (text and emoji placeholders)\r\n     final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n     List<_Segment> segments = [];\r\n     int lastMatchEnd = 0;\r\n-    \r\n-    // Parse message into segments\r\n     for (final match in regex.allMatches(message)) {\r\n       if (match.start > lastMatchEnd) {\r\n         segments.add(_Segment(\r\n           text: message.substring(lastMatchEnd, match.start),\r\n@@ -80,167 +82,152 @@\n       segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n       lastMatchEnd = match.end;\r\n     }\r\n     if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+      segments.add(\r\n+          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n     }\r\n \r\n-    // Calculate dimensions and scaling\r\n+    // Fixed display dimensions\r\n     final int displayWidth = cols * scale;\r\n     final int displayHeight = rows * scale;\r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    \r\n-    // Ensure text fits vertically\r\n-    if (scaledFontSize > displayHeight * 0.9) {\r\n-      scaledFontSize = displayHeight * 0.9;\r\n-    }\r\n-    \r\n-    double emojiWidth = scaledFontSize;\r\n-    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-    // Measure total width\r\n+    // Constant font settings\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+\r\n+    // Measure one copy of the message's width\r\n     double messageWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        messageWidth += widthWithGap;\r\n-      } else {\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n         final TextPainter tp = TextPainter(\r\n           text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize),\r\n-          ),\r\n+              text: segment.text,\r\n+              style:\r\n+                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n           textDirection: TextDirection.ltr,\r\n         );\r\n         tp.layout();\r\n         messageWidth += tp.width;\r\n+      } else {\r\n+        messageWidth += emojiWidth;\r\n       }\r\n     }\r\n \r\n-    // Calculate marquee parameters\r\n-    double gapWidth = displayWidth * 0.5; // Half display width for smoother transition\r\n+    // Define a gap between copies; here we use one full display width.\r\n+    double gapWidth = displayWidth.toDouble();\r\n     double marqueeWidth = messageWidth + gapWidth;\r\n-    int canvasWidth = (marqueeWidth * 2).ceil(); // Two copies for seamless scrolling\r\n-    double effectiveOffset = -(scrollOffset % marqueeWidth);\r\n-    if (effectiveOffset > 0) effectiveOffset -= marqueeWidth;\r\n \r\n-    // Create canvas\r\n-    final recorder = ui.PictureRecorder();\r\n-    final canvas = Canvas(\r\n+    // Create a marquee canvas with two copies for seamless scrolling.\r\n+    int canvasWidth = (marqueeWidth * 2).ceil();\r\n+    int canvasHeight = displayHeight;\r\n+\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n       recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n     );\r\n \r\n-    // Fill background\r\n+    // Fill background with white.\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n     canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), displayHeight.toDouble()),\r\n-      Paint()..color = Colors.white,\r\n-    );\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-    // Draw message copies\r\n-    Future<void> drawMessageCopy(double startX) async {\r\n-      double currentX = startX;\r\n-      double centerY = displayHeight / 2;\r\n+    double centerY = canvasHeight / 2.0;\r\n \r\n-      for (int i = 0; i < segments.length; i++) {\r\n-        final segment = segments[i];\r\n+    // Helper to draw one copy of the message starting at a given x-offset.\r\n+    Future<void> drawMessage(double offsetX) async {\r\n+      double currentX = offsetX;\r\n+      for (var segment in segments) {\r\n         if (!segment.isEmoji) {\r\n           final TextPainter tp = TextPainter(\r\n             text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black),\r\n-            ),\r\n+                text: segment.text,\r\n+                style:\r\n+                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n             textDirection: TextDirection.ltr,\r\n           );\r\n           tp.layout();\r\n-          var metrics = tp.computeLineMetrics();\r\n-          double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-          double offsetY = centerY - (tp.height - descent) / 2;\r\n+          double offsetY = centerY - tp.height / 2;\r\n           tp.paint(canvas, Offset(currentX, offsetY));\r\n           currentX += tp.width;\r\n         } else {\r\n           int index = int.parse(segment.text);\r\n-          var keys = controllerData.imageCache.keys.toList();\r\n+          List keys = controllerData.imageCache.keys.toList();\r\n           if (index < keys.length) {\r\n             var key = keys[index];\r\n-            var emojiBytes = controllerData.imageCache[key];\r\n+            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n             if (emojiBytes != null) {\r\n-              final codec = await ui.instantiateImageCodec(\r\n+              ui.Codec codec = await ui.instantiateImageCodec(\r\n                 emojiBytes,\r\n                 targetWidth: emojiWidth.toInt(),\r\n                 targetHeight: emojiWidth.toInt(),\r\n               );\r\n-              final frame = await codec.getNextFrame();\r\n+              ui.FrameInfo fi = await codec.getNextFrame();\r\n+              ui.Image emojiImage = fi.image;\r\n               double offsetY = centerY - emojiWidth / 2;\r\n-              canvas.drawImage(frame.image, Offset(currentX, offsetY), Paint());\r\n+              Paint imagePaint = Paint();\r\n+              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n               currentX += emojiWidth;\r\n-              if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-                currentX += emojiGap;\r\n-              }\r\n             }\r\n           }\r\n         }\r\n       }\r\n     }\r\n \r\n-    // Draw two copies for seamless scrolling\r\n-    await drawMessageCopy(effectiveOffset);\r\n-    await drawMessageCopy(effectiveOffset + marqueeWidth);\r\n+    // Draw two copies of the message.\r\n+    await drawMessage(0);\r\n+    await drawMessage(marqueeWidth);\r\n \r\n-    // Convert to image\r\n-    final picture = recorder.endRecording();\r\n-    final image = await picture.toImage(canvasWidth, displayHeight);\r\n-    final byteData = await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    \r\n+    // Finalize the marquee image.\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n     if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data\");\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n     }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    final pixels = byteData.buffer.asUint8List();\r\n-    \r\n-    // Create LED matrix with improved downsampling\r\n-    List<List<bool>> matrix = List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    const threshold = 180;\r\n+    // Compute effective horizontal offset (looping within marqueeWidth)\r\n+    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n \r\n+    // Downsample only the display window from the wide marquee image.\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n+    const int threshold = 200;\r\n     for (int row = 0; row < rows; row++) {\r\n       for (int col = 0; col < cols; col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        \r\n+        int sum = 0;\r\n+        int count = 0;\r\n         for (int y = 0; y < scale; y++) {\r\n           for (int x = 0; x < scale; x++) {\r\n-            final pixelX = (col * scale + x) % canvasWidth;\r\n-            final pixelY = row * scale + y;\r\n-            final idx = (pixelY * canvasWidth + pixelX) * 4;\r\n-            \r\n-            if (idx + 3 < pixels.length) {\r\n-              int r = pixels[idx];\r\n-              int g = pixels[idx + 1];\r\n-              int b = pixels[idx + 2];\r\n+            int pixelX = effectiveOffset + col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index],\r\n+                  g = data[index + 1],\r\n+                  b = data[index + 2];\r\n               int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < threshold) darkCount++;\r\n-              totalCount++;\r\n+              sum += brightness;\r\n+              count++;\r\n             }\r\n           }\r\n         }\r\n-        \r\n-        matrix[row][col] = darkCount > (totalCount * 0.2);\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n     }\r\n-\r\n     return matrix;\r\n   }\r\n \r\n-  // --- Helper methods for converting bitmaps to LED hex strings ---\r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n-    // Trim empty columns from the left.\r\n     for (int j = 0; j < width; j++) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -253,9 +240,8 @@\n         finalSum += j;\r\n         break;\r\n       }\r\n     }\r\n-    // Trim empty columns from the right.\r\n     for (int j = width - 1; j >= 0; j--) {\r\n       int sum = 0;\r\n       for (int i = 0; i < height; i++) {\r\n         sum += image[i][j];\r\n@@ -274,32 +260,32 @@\n       diff = 8 - (height - finalSum) % 8;\r\n     }\r\n     int rOff = (diff / 2).floor();\r\n     int lOff = (diff / 2).ceil();\r\n-    List<List<int>> padded =\r\n+    List<List<int>> list =\r\n         List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n     for (int i = 0; i < height; i++) {\r\n       int k = 0;\r\n       for (int j = 0; j < rOff; j++) {\r\n-        padded[i][k++] = 0;\r\n+        list[i][k++] = 0;\r\n       }\r\n       for (int j = 0; j < width; j++) {\r\n         if (image[i][j] != -1) {\r\n-          padded[i][k++] = image[i][j];\r\n+          list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n       for (int j = 0; j < lOff; j++) {\r\n-        padded[i][k++] = 0;\r\n+        list[i][k++] = 0;\r\n       }\r\n     }\r\n-    logger.d(\"Padded image: $padded\");\r\n+    logger.d(\"Padded image: $list\");\r\n     List<String> allHexs = [];\r\n-    for (int i = 0; i < padded[0].length ~/ 8; i++) {\r\n+    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n       StringBuffer lineHex = StringBuffer();\r\n       for (int k = 0; k < height; k++) {\r\n         StringBuffer stBuilder = StringBuffer();\r\n         for (int j = i * 8; j < i * 8 + 8; j++) {\r\n-          stBuilder.write(padded[k][j]);\r\n+          stBuilder.write(list[k][j]);\r\n         }\r\n         String hex = int.parse(stBuilder.toString(), radix: 2)\r\n             .toRadixString(16)\r\n             .padLeft(2, '0');\r\n@@ -312,25 +298,28 @@\n \r\n   static String invertHex(String hex) {\r\n     StringBuffer invertedHex = StringBuffer();\r\n     for (int i = 0; i < hex.length; i++) {\r\n-      String invertedDigit =\r\n+      String invertedHexDigit =\r\n           (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n-      invertedHex.write(invertedDigit);\r\n+      invertedHex.write(invertedHexDigit);\r\n     }\r\n     return invertedHex.toString();\r\n   }\r\n \r\n   List<String> padHexString(List<String> hexString) {\r\n-    List<List<bool>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n-      return e.map((bit) => bit ? 1 : 0).toList();\r\n-    }).cast<List<bool>>().toList();\r\n+    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n+      return e.map((e) => e ? 1 : 0).toList();\r\n+    }).toList();\r\n     for (int i = 0; i < hexArray.length; i++) {\r\n-      hexArray[i].insert(0, 1 as bool);\r\n-      hexArray[i].add(1 as bool);\r\n+      hexArray[i].insert(0, 1);\r\n+      hexArray[i].add(1);\r\n     }\r\n-    return convertBitmapToLEDHex(hexArray.cast<List<int>>(), true);\r\n+    return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n }\r\n \r\n-/// Simple helper class to represent a segment of the message (either text or an emoji placeholder).\r\n-\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n"
                },
                {
                    "date": 1740049023432,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -20,10 +20,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -52,178 +51,155 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Render the given text (with optional emoji placeholders) as a scrolling marquee.\r\n-  ///\r\n-  /// The display area is fixed (cols×scale by rows×scale). If the rendered message exceeds\r\n-  /// this area, a wider marquee canvas is created (two copies of the message separated by a gap)\r\n-  /// so that the text can scroll seamlessly. The [scrollOffset] (in pixels) determines which part\r\n-  /// of the marquee is shown in the LED matrix.\r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10, // scale factor for better resolution\r\n-    double scrollOffset = 0, // horizontal scroll offset in pixels\r\n-  }) async {\r\n-    // Parse message into segments (text and emoji placeholders)\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start),\r\n-          isEmoji: false,\r\n-        ));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n+  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n+  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n+  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n+ Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 10, // scale factor for better resolution\r\n+  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n+}) async {\r\n+  // Parse the message for emoji placeholders in the format <<00>>\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(\r\n-          _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(\r\n+        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    // Fixed display dimensions\r\n-    final int displayWidth = cols * scale;\r\n-    final int displayHeight = rows * scale;\r\n+  // Capture the emoji keys once to ensure consistent ordering.\r\n+  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n \r\n-    // Constant font settings\r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-\r\n-    // Measure one copy of the message's width\r\n-    double messageWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style:\r\n-                  textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        messageWidth += tp.width;\r\n-      } else {\r\n-        messageWidth += emojiWidth;\r\n-      }\r\n+  // Calculate total width required to draw the entire message.\r\n+  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double emojiWidth = fontSize * 0.734;\r\n+  double totalWidth = 0;\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout();\r\n+      totalWidth += textPainter.width;\r\n+    } else {\r\n+      totalWidth += emojiWidth;\r\n     }\r\n+  }\r\n \r\n-    // Define a gap between copies; here we use one full display width.\r\n-    double gapWidth = displayWidth.toDouble();\r\n-    double marqueeWidth = messageWidth + gapWidth;\r\n+  // Create a canvas wide enough to hold the entire message.\r\n+  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+  final int canvasHeight = rows * scale;\r\n \r\n-    // Create a marquee canvas with two copies for seamless scrolling.\r\n-    int canvasWidth = (marqueeWidth * 2).ceil();\r\n-    int canvasHeight = displayHeight;\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(recorder,\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-      recorder,\r\n+  // Fill background with white.\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-    );\r\n+      bgPaint);\r\n \r\n-    // Fill background with white.\r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+  double currentX = 0;\r\n+  double centerY = canvasHeight / 2.0;\r\n \r\n-    double centerY = canvasHeight / 2.0;\r\n-\r\n-    // Helper to draw one copy of the message starting at a given x-offset.\r\n-    Future<void> drawMessage(double offsetX) async {\r\n-      double currentX = offsetX;\r\n-      for (var segment in segments) {\r\n-        if (!segment.isEmoji) {\r\n-          final TextPainter tp = TextPainter(\r\n-            text: TextSpan(\r\n-                text: segment.text,\r\n-                style:\r\n-                    textStyle.copyWith(color: Colors.black, fontSize: fontSize)),\r\n-            textDirection: TextDirection.ltr,\r\n-          );\r\n-          tp.layout();\r\n-          double offsetY = centerY - tp.height / 2;\r\n-          tp.paint(canvas, Offset(currentX, offsetY));\r\n-          currentX += tp.width;\r\n-        } else {\r\n-          int index = int.parse(segment.text);\r\n-          List keys = controllerData.imageCache.keys.toList();\r\n-          if (index < keys.length) {\r\n-            var key = keys[index];\r\n-            Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-            if (emojiBytes != null) {\r\n-              ui.Codec codec = await ui.instantiateImageCodec(\r\n-                emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(),\r\n-                targetHeight: emojiWidth.toInt(),\r\n-              );\r\n-              ui.FrameInfo fi = await codec.getNextFrame();\r\n-              ui.Image emojiImage = fi.image;\r\n-              double offsetY = centerY - emojiWidth / 2;\r\n-              Paint imagePaint = Paint();\r\n-              canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-              currentX += emojiWidth;\r\n-            }\r\n-          }\r\n+  // Draw each segment onto the canvas.\r\n+  for (var segment in segments) {\r\n+    if (!segment.isEmoji) {\r\n+      // Paint text.\r\n+      final TextPainter textPainter = TextPainter(\r\n+        text: TextSpan(\r\n+          text: segment.text,\r\n+          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+        ),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+      double offsetY = centerY - textPainter.height / 2;\r\n+      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += textPainter.width;\r\n+    } else {\r\n+      // Paint emoji.\r\n+      int index = int.parse(segment.text);\r\n+      if (index < emojiKeys.length) {\r\n+        var key = emojiKeys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          currentX += emojiWidth;\r\n         }\r\n       }\r\n     }\r\n+  }\r\n \r\n-    // Draw two copies of the message.\r\n-    await drawMessage(0);\r\n-    await drawMessage(marqueeWidth);\r\n+  // Convert canvas to image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n \r\n-    // Finalize the marquee image.\r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image wideImage = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await wideImage.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    // Compute effective horizontal offset (looping within marqueeWidth)\r\n-    int effectiveOffset = (scrollOffset % marqueeWidth).floor();\r\n-\r\n-    // Downsample only the display window from the wide marquee image.\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    // Adjusted threshold: using 200 instead of 128 to capture anti-aliased text.\r\n-    const int threshold = 200;\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = effectiveOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // 4 bytes per pixel\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index],\r\n-                  g = data[index + 1],\r\n-                  b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            }\r\n+  // Downsample to LED matrix using the provided xOffset.\r\n+  List<List<bool>> matrix =\r\n+      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < cols; col++) {\r\n+      int sum = 0;\r\n+      int count = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = xOffset + col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n+            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            sum += brightness;\r\n+            count++;\r\n+          } else {\r\n+            sum += 255;\r\n+            count++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < threshold;\r\n       }\r\n+      double avgBrightness = sum / count;\r\n+      matrix[row][col] = avgBrightness < 128;\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n"
                },
                {
                    "date": 1740049442075,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,15 +1,22 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -20,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -51,21 +59,20 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n-  /// canvas and convert it to an LED matrix (11 rows x 44 columns). If the entire\r\n-  /// message is longer than the LED display, the extra text is not clipped – instead,\r\n-  /// an [xOffset] parameter allows you to select a sliding window (for animation).\r\n- Future<List<List<bool>>> renderTextToMatrix(\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n   String message,\r\n   TextStyle textStyle, {\r\n   int cols = 44,\r\n   int rows = 11,\r\n   int scale = 10, // scale factor for better resolution\r\n-  int xOffset = 0, // horizontal offset (in pixels) for animation\r\n }) async {\r\n-  // Parse the message for emoji placeholders in the format <<00>>\r\n+  // Parse the message into segments (plain text and emoji)\r\n   final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n   List<_Segment> segments = [];\r\n   int lastMatchEnd = 0;\r\n   for (final match in regex.allMatches(message)) {\r\n@@ -77,72 +84,85 @@\n     segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n     lastMatchEnd = match.end;\r\n   }\r\n   if (lastMatchEnd < message.length) {\r\n-    segments.add(\r\n-        _Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n   }\r\n \r\n-  // Capture the emoji keys once to ensure consistent ordering.\r\n-  final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-  // Calculate total width required to draw the entire message.\r\n-  double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-  double emojiWidth = fontSize * 0.734;\r\n-  double totalWidth = 0;\r\n-  for (var segment in segments) {\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter textPainter = TextPainter(\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n+      }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n         text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n         textDirection: TextDirection.ltr,\r\n       );\r\n-      textPainter.layout();\r\n-      totalWidth += textPainter.width;\r\n-    } else {\r\n-      totalWidth += emojiWidth;\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n   }\r\n \r\n-  // Create a canvas wide enough to hold the entire message.\r\n-  final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-  final int canvasHeight = rows * scale;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n   final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(recorder,\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Fill background with white.\r\n+  // Fill the background with white\r\n   final Paint bgPaint = Paint()..color = Colors.white;\r\n   canvas.drawRect(\r\n       Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n       bgPaint);\r\n \r\n+  // Draw one copy of the message starting at x=0\r\n   double currentX = 0;\r\n-  double centerY = canvasHeight / 2.0;\r\n-\r\n-  // Draw each segment onto the canvas.\r\n-  for (var segment in segments) {\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n     if (!segment.isEmoji) {\r\n-      // Paint text.\r\n-      final TextPainter textPainter = TextPainter(\r\n+      final TextPainter tp = TextPainter(\r\n         text: TextSpan(\r\n-          text: segment.text,\r\n-          style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-        ),\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n         textDirection: TextDirection.ltr,\r\n       );\r\n-      textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-      double offsetY = centerY - textPainter.height / 2;\r\n-      textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += textPainter.width;\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n     } else {\r\n-      // Paint emoji.\r\n       int index = int.parse(segment.text);\r\n-      if (index < emojiKeys.length) {\r\n-        var key = emojiKeys[index];\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n         Uint8List? emojiBytes = controllerData.imageCache[key];\r\n         if (emojiBytes != null) {\r\n           ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n               targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n@@ -150,50 +170,51 @@\n           ui.Image emojiImage = fi.image;\r\n           double offsetY = centerY - emojiWidth / 2;\r\n           Paint imagePaint = Paint();\r\n           canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          currentX += emojiWidth;\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n   }\r\n-\r\n-  // Convert canvas to image.\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n   final ui.Picture picture = recorder.endRecording();\r\n   final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n   final ByteData? byteData =\r\n       await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-\r\n   if (byteData == null) {\r\n     throw Exception(\"Failed to convert image to byte data.\");\r\n   }\r\n   final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample to LED matrix using the provided xOffset.\r\n-  List<List<bool>> matrix =\r\n-      List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n   for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < cols; col++) {\r\n-      int sum = 0;\r\n-      int count = 0;\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n       for (int y = 0; y < scale; y++) {\r\n         for (int x = 0; x < scale; x++) {\r\n-          int pixelX = xOffset + col * scale + x;\r\n+          int pixelX = col * scale + x;\r\n           int pixelY = row * scale + y;\r\n-          if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4; // RGBA\r\n-            int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n             int brightness = ((r + g + b) / 3).round();\r\n-            sum += brightness;\r\n-            count++;\r\n-          } else {\r\n-            sum += 255;\r\n-            count++;\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n       }\r\n-      double avgBrightness = sum / count;\r\n-      matrix[row][col] = avgBrightness < 128;\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n   }\r\n   return matrix;\r\n }\r\n@@ -291,11 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n+}\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740049476732,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -88,9 +88,9 @@\n     segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n   }\r\n \r\n   // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+  double scaledFontSize = (textStyle.fontSize ?? ) * scale;\r\n   int canvasHeight = rows * scale;\r\n   // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n   if (scaledFontSize > canvasHeight * 0.9) {\r\n     scaledFontSize = canvasHeight * 0.9;\r\n"
                },
                {
                    "date": 1740049523870,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -68,9 +68,9 @@\n   String message,\r\n   TextStyle textStyle, {\r\n   int cols = 44,\r\n   int rows = 11,\r\n-  int scale = 10, // scale factor for better resolution\r\n+  int scale = 0, // scale factor for better resolution\r\n }) async {\r\n   // Parse the message into segments (plain text and emoji)\r\n   final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n   List<_Segment> segments = [];\r\n@@ -88,9 +88,9 @@\n     segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n   }\r\n \r\n   // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? ) * scale;\r\n+  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n   int canvasHeight = rows * scale;\r\n   // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n   if (scaledFontSize > canvasHeight * 0.9) {\r\n     scaledFontSize = canvasHeight * 0.9;\r\n"
                },
                {
                    "date": 1740049580885,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,22 +1,12 @@\n+import 'dart:async';\r\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n-import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n-import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n+import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -27,10 +17,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,168 +48,139 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 0, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    int xOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    double emojiWidth = fontSize * 0.734;\r\n+    double totalWidth = 0;\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout();\r\n+        totalWidth += textPainter.width;\r\n+      } else {\r\n+        totalWidth += emojiWidth;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n+    final int canvasHeight = rows * scale;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(recorder,\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n+    double currentX = 0;\r\n+    double centerY = canvasHeight / 2.0;\r\n+\r\n+    for (var segment in segments) {\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter textPainter = TextPainter(\r\n+          text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n+          ),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n+        double offsetY = centerY - textPainter.height / 2;\r\n+        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += textPainter.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        if (index < emojiKeys.length) {\r\n+          var key = emojiKeys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+            currentX += emojiWidth;\r\n+          }\r\n         }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    List<List<bool>> matrix =\r\n+        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int sum = 0;\r\n+        int count = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = xOffset + col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n+              int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              sum += brightness;\r\n+              count++;\r\n+            } else {\r\n+              sum += 255;\r\n+              count++;\r\n+            }\r\n           }\r\n         }\r\n+        double avgBrightness = sum / count;\r\n+        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+    return matrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -269,9 +229,9 @@\n         if (image[i][j] != -1) {\r\n           list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n-      for (int j = 0; j < lOff; j++) {\r\n+      for (int j = 0; j < lOff; j) {\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n     logger.d(\"Padded image: $list\");\r\n@@ -312,5 +272,29 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+}\r\n+\r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n+class MarqueeController {\r\n+  int xOffset = 0;\r\n+  int maxOffset = 0;\r\n+  Timer? timer;\r\n+\r\n+  void startMarquee(int maxOffset, Duration duration) {\r\n+    this.maxOffset = maxOffset;\r\n+    timer = Timer.periodic(duration, (timer) {\r\n+      xOffset = (xOffset + 1) % maxOffset;\r\n+      // Notify listeners or update state here\r\n+    });\r\n+  }\r\n+\r\n+  void stopMarquee() {\r\n+    timer?.cancel();\r\n+  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740049588976,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,8 +1,9 @@\n import 'dart:async';\r\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n"
                },
                {
                    "date": 1740049597608,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -6,8 +6,10 @@\n import 'package:flutter/material.dart';\r\n import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+import '../../providers/imageprovider.dart';\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n"
                },
                {
                    "date": 1740049606199,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,14 +2,13 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n-import '../../providers/imageprovider.dart';\r\n-\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n"
                },
                {
                    "date": 1740049613113,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -2,8 +2,10 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n"
                },
                {
                    "date": 1740049722842,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,16 +1,22 @@\n-import 'dart:async';\r\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n+import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n+import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n import 'package:badgemagic/providers/imageprovider.dart';\r\n import 'package:flutter/material.dart';\r\n-import 'package:flutter/scheduler.dart';\r\n import 'package:get_it/get_it.dart';\r\n \r\n+class _Segment {\r\n+  final String text;\r\n+  final bool isEmoji;\r\n+  _Segment({required this.text, required this.isEmoji});\r\n+}\r\n+\r\n class Converters {\r\n   InlineImageProvider controllerData =\r\n       GetIt.instance.get<InlineImageProvider>();\r\n   DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n@@ -21,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -52,139 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    int xOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 40, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    final List<dynamic> emojiKeys = controllerData.imageCache.keys.toList();\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    double emojiWidth = fontSize * 0.734;\r\n-    double totalWidth = 0;\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout();\r\n-        totalWidth += textPainter.width;\r\n-      } else {\r\n-        totalWidth += emojiWidth;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    final int canvasWidth = max(totalWidth.ceil(), cols * scale);\r\n-    final int canvasHeight = rows * scale;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(recorder,\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    double currentX = 0;\r\n-    double centerY = canvasHeight / 2.0;\r\n-\r\n-    for (var segment in segments) {\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter textPainter = TextPainter(\r\n-          text: TextSpan(\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n             text: segment.text,\r\n-            style: textStyle.copyWith(color: Colors.black, fontSize: fontSize),\r\n-          ),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        textPainter.layout(maxWidth: canvasWidth - currentX);\r\n-        double offsetY = centerY - textPainter.height / 2;\r\n-        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += textPainter.width;\r\n-      } else {\r\n-        int index = int.parse(segment.text);\r\n-        if (index < emojiKeys.length) {\r\n-          var key = emojiKeys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-            currentX += emojiWidth;\r\n-          }\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    List<List<bool>> matrix =\r\n-        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int sum = 0;\r\n-        int count = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = xOffset + col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            if (pixelX < canvasWidth && pixelY < canvasHeight) {\r\n-              int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              sum += brightness;\r\n-              count++;\r\n-            } else {\r\n-              sum += 255;\r\n-              count++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        double avgBrightness = sum / count;\r\n-        matrix[row][col] = avgBrightness < 128;\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n-    return matrix;\r\n   }\r\n+  return matrix;\r\n+}\r\n \r\n+\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -233,9 +269,9 @@\n         if (image[i][j] != -1) {\r\n           list[i][k++] = image[i][j];\r\n         }\r\n       }\r\n-      for (int j = 0; j < lOff; j) {\r\n+      for (int j = 0; j < lOff; j++) {\r\n         list[i][k++] = 0;\r\n       }\r\n     }\r\n     logger.d(\"Padded image: $list\");\r\n@@ -276,29 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class _Segment {\r\n-  final String text;\r\n-  final bool isEmoji;\r\n-  _Segment({required this.text, required this.isEmoji});\r\n-}\r\n-\r\n-class MarqueeController {\r\n-  int xOffset = 0;\r\n-  int maxOffset = 0;\r\n-  Timer? timer;\r\n-\r\n-  void startMarquee(int maxOffset, Duration duration) {\r\n-    this.maxOffset = maxOffset;\r\n-    timer = Timer.periodic(duration, (timer) {\r\n-      xOffset = (xOffset + 1) % maxOffset;\r\n-      // Notify listeners or update state here\r\n-    });\r\n-  }\r\n-\r\n-  void stopMarquee() {\r\n-    timer?.cancel();\r\n-  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740049829209,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,10 +27,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,168 +58,159 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 40, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    int xOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n+    if (scaledFontSize > canvasHeight * 0.9) {\r\n+      scaledFontSize = canvasHeight * 0.9;\r\n+    }\r\n+    double emojiWidth = scaledFontSize;\r\n+    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    double singleCopyWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        singleCopyWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segments[i].text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        singleCopyWidth += tp.width;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    int fixedWidth = cols * scale;\r\n+    int gapWidth = fixedWidth;\r\n+    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    double centerY = canvasHeight / 2;\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n+\r\n+    double currentX = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      final segment = segments[i];\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        double offsetY = centerY - (tp.height - tp.computeLineMetrics().first.descent) / 2;\r\n+        tp.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += tp.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          }\r\n         }\r\n+        currentX += emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          currentX += emojiGap;\r\n+        }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n+            }\r\n           }\r\n         }\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+\r\n+    // Apply xOffset to create the scrolling effect\r\n+    List<List<bool>> scrolledMatrix = List.generate(\r\n+        rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int scrolledCol = (col + xOffset) % matrix[row].length;\r\n+        scrolledMatrix[row][col] = matrix[row][scrolledCol];\r\n+      }\r\n+    }\r\n+\r\n+    return scrolledMatrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -312,5 +302,23 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+}\r\n+\r\n+class MarqueeController {\r\n+  int xOffset = 0;\r\n+  int maxOffset = 0;\r\n+  Timer? timer;\r\n+\r\n+  void startMarquee(int maxOffset, Duration duration) {\r\n+    this.maxOffset = maxOffset;\r\n+    timer = Timer.periodic(duration, (timer) {\r\n+      xOffset = (xOffset + 1) % maxOffset;\r\n+      // Notify listeners or update state here\r\n+    });\r\n+  }\r\n+\r\n+  void stopMarquee() {\r\n+    timer?.cancel();\r\n+  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740049844720,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,4 +1,5 @@\n+import 'dart:async';\r\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n"
                },
                {
                    "date": 1740049915735,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,5 +1,4 @@\n-import 'dart:async';\r\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n@@ -28,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,159 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    int xOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 0, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n-    if (scaledFontSize > canvasHeight * 0.9) {\r\n-      scaledFontSize = canvasHeight * 0.9;\r\n-    }\r\n-    double emojiWidth = scaledFontSize;\r\n-    double emojiGap = scaledFontSize * 0.2;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.9) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    double singleCopyWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        singleCopyWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segments[i].text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        singleCopyWidth += tp.width;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    int fixedWidth = cols * scale;\r\n-    int gapWidth = fixedWidth;\r\n-    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    double centerY = canvasHeight / 2;\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n-\r\n-    double currentX = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      final segment = segments[i];\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        double offsetY = centerY - (tp.height - tp.computeLineMetrics().first.descent) / 2;\r\n-        tp.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += tp.width;\r\n-      } else {\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          }\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n-        currentX += emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          currentX += emojiGap;\r\n-        }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+  }\r\n+  return matrix;\r\n+}\r\n \r\n-    // Apply xOffset to create the scrolling effect\r\n-    List<List<bool>> scrolledMatrix = List.generate(\r\n-        rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int scrolledCol = (col + xOffset) % matrix[row].length;\r\n-        scrolledMatrix[row][col] = matrix[row][scrolledCol];\r\n-      }\r\n-    }\r\n \r\n-    return scrolledMatrix;\r\n-  }\r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -303,23 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class MarqueeController {\r\n-  int xOffset = 0;\r\n-  int maxOffset = 0;\r\n-  Timer? timer;\r\n-\r\n-  void startMarquee(int maxOffset, Duration duration) {\r\n-    this.maxOffset = maxOffset;\r\n-    timer = Timer.periodic(duration, (timer) {\r\n-      xOffset = (xOffset + 1) % maxOffset;\r\n-      // Notify listeners or update state here\r\n-    });\r\n-  }\r\n-\r\n-  void stopMarquee() {\r\n-    timer?.cancel();\r\n-  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740049921257,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,10 +27,9 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' &&\r\n-          message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -59,168 +58,159 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n-Future<List<List<bool>>> renderTextToMatrix(\r\n-  String message,\r\n-  TextStyle textStyle, {\r\n-  int cols = 44,\r\n-  int rows = 11,\r\n-  int scale = 0, // scale factor for better resolution\r\n-}) async {\r\n-  // Parse the message into segments (plain text and emoji)\r\n-  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-  List<_Segment> segments = [];\r\n-  int lastMatchEnd = 0;\r\n-  for (final match in regex.allMatches(message)) {\r\n-    if (match.start > lastMatchEnd) {\r\n-      segments.add(_Segment(\r\n-          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+  Future<List<List<bool>>> renderTextToMatrix(\r\n+    String message,\r\n+    TextStyle textStyle, {\r\n+    int cols = 44,\r\n+    int rows = 11,\r\n+    int scale = 10,\r\n+    int xOffset = 0,\r\n+  }) async {\r\n+    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+    List<_Segment> segments = [];\r\n+    int lastMatchEnd = 0;\r\n+    for (final match in regex.allMatches(message)) {\r\n+      if (match.start > lastMatchEnd) {\r\n+        segments.add(_Segment(\r\n+            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n+      }\r\n+      String emojiIndex = match.group(1)!;\r\n+      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+      lastMatchEnd = match.end;\r\n     }\r\n-    String emojiIndex = match.group(1)!;\r\n-    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-    lastMatchEnd = match.end;\r\n-  }\r\n-  if (lastMatchEnd < message.length) {\r\n-    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-  }\r\n+    if (lastMatchEnd < message.length) {\r\n+      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+    }\r\n \r\n-  // Determine the scaled font size and set emoji dimensions.\r\n-  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n-  int canvasHeight = rows * scale;\r\n-  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n-  if (scaledFontSize > canvasHeight * 0.9) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n-  }\r\n-  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n-  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n+    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n+    int canvasHeight = rows * scale;\r\n+    if (scaledFontSize > canvasHeight * 0.9) {\r\n+      scaledFontSize = canvasHeight * 0.9;\r\n+    }\r\n+    double emojiWidth = scaledFontSize;\r\n+    double emojiGap = scaledFontSize * 0.2;\r\n \r\n-  // Measure one copy of the message (in scaled pixels)\r\n-  double singleCopyWidth = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    if (segments[i].isEmoji) {\r\n-      double widthWithGap = emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        widthWithGap += emojiGap;\r\n+    double singleCopyWidth = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      if (segments[i].isEmoji) {\r\n+        double widthWithGap = emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          widthWithGap += emojiGap;\r\n+        }\r\n+        singleCopyWidth += widthWithGap;\r\n+      } else {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segments[i].text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        singleCopyWidth += tp.width;\r\n       }\r\n-      singleCopyWidth += widthWithGap;\r\n-    } else {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segments[i].text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      singleCopyWidth += tp.width;\r\n     }\r\n-  }\r\n \r\n-  // fixedWidth is the display width (in scaled pixels)\r\n-  int fixedWidth = cols * scale;\r\n-  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n-  int gapWidth = fixedWidth;\r\n-  // Canvas width is one copy of the message plus the gap\r\n-  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n-  \r\n-  double centerY = canvasHeight / 2;\r\n+    int fixedWidth = cols * scale;\r\n+    int gapWidth = fixedWidth;\r\n+    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n \r\n-  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-  final Canvas canvas = Canvas(\r\n-      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+    double centerY = canvasHeight / 2;\r\n \r\n-  // Fill the background with white\r\n-  final Paint bgPaint = Paint()..color = Colors.white;\r\n-  canvas.drawRect(\r\n-      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-      bgPaint);\r\n+    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+    final Canvas canvas = Canvas(\r\n+        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-  // Draw one copy of the message starting at x=0\r\n-  double currentX = 0;\r\n-  for (int i = 0; i < segments.length; i++) {\r\n-    final segment = segments[i];\r\n-    if (!segment.isEmoji) {\r\n-      final TextPainter tp = TextPainter(\r\n-        text: TextSpan(\r\n-            text: segment.text,\r\n-            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-        textDirection: TextDirection.ltr,\r\n-      );\r\n-      tp.layout();\r\n-      // Compute descent so that letters with descenders are fully visible.\r\n-      var metrics = tp.computeLineMetrics();\r\n-      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n-      // Offset the text so that the bottom of the glyphs is centered.\r\n-      double offsetY = centerY - (tp.height - descent) / 2;\r\n-      tp.paint(canvas, Offset(currentX, offsetY));\r\n-      currentX += tp.width;\r\n-    } else {\r\n-      int index = int.parse(segment.text);\r\n-      List keys = controllerData.imageCache.keys.toList();\r\n-      if (index < keys.length) {\r\n-        var key = keys[index];\r\n-        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-        if (emojiBytes != null) {\r\n-          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-          ui.FrameInfo fi = await codec.getNextFrame();\r\n-          ui.Image emojiImage = fi.image;\r\n-          double offsetY = centerY - emojiWidth / 2;\r\n-          Paint imagePaint = Paint();\r\n-          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+    final Paint bgPaint = Paint()..color = Colors.white;\r\n+    canvas.drawRect(\r\n+        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+        bgPaint);\r\n+\r\n+    double currentX = 0;\r\n+    for (int i = 0; i < segments.length; i++) {\r\n+      final segment = segments[i];\r\n+      if (!segment.isEmoji) {\r\n+        final TextPainter tp = TextPainter(\r\n+          text: TextSpan(\r\n+              text: segment.text,\r\n+              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+          textDirection: TextDirection.ltr,\r\n+        );\r\n+        tp.layout();\r\n+        double offsetY = centerY - (tp.height - tp.computeLineMetrics().first.descent) / 2;\r\n+        tp.paint(canvas, Offset(currentX, offsetY));\r\n+        currentX += tp.width;\r\n+      } else {\r\n+        int index = int.parse(segment.text);\r\n+        List keys = controllerData.imageCache.keys.toList();\r\n+        if (index < keys.length) {\r\n+          var key = keys[index];\r\n+          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+          if (emojiBytes != null) {\r\n+            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+            ui.FrameInfo fi = await codec.getNextFrame();\r\n+            ui.Image emojiImage = fi.image;\r\n+            double offsetY = centerY - emojiWidth / 2;\r\n+            Paint imagePaint = Paint();\r\n+            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n+          }\r\n         }\r\n+        currentX += emojiWidth;\r\n+        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+          currentX += emojiGap;\r\n+        }\r\n       }\r\n-      currentX += emojiWidth;\r\n-      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-        currentX += emojiGap;\r\n-      }\r\n     }\r\n-  }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n-  final ui.Picture picture = recorder.endRecording();\r\n-  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-  final ByteData? byteData =\r\n-      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-  if (byteData == null) {\r\n-    throw Exception(\"Failed to convert image to byte data.\");\r\n-  }\r\n-  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n-  List<List<bool>> matrix = List.generate(\r\n-      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-  for (int row = 0; row < rows; row++) {\r\n-    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-      int darkCount = 0;\r\n-      int totalCount = 0;\r\n-      for (int y = 0; y < scale; y++) {\r\n-        for (int x = 0; x < scale; x++) {\r\n-          int pixelX = col * scale + x;\r\n-          int pixelY = row * scale + y;\r\n-          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-          if (index + 3 < data.length) {\r\n-            int r = data[index];\r\n-            int g = data[index + 1];\r\n-            int b = data[index + 2];\r\n-            int brightness = ((r + g + b) / 3).round();\r\n-            if (brightness < 128) darkCount++;\r\n-            totalCount++;\r\n+    final ui.Picture picture = recorder.endRecording();\r\n+    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+    final ByteData? byteData =\r\n+        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+    if (byteData == null) {\r\n+      throw Exception(\"Failed to convert image to byte data.\");\r\n+    }\r\n+    final Uint8List data = byteData.buffer.asUint8List();\r\n+\r\n+    List<List<bool>> matrix = List.generate(\r\n+        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+        int darkCount = 0;\r\n+        int totalCount = 0;\r\n+        for (int y = 0; y < scale; y++) {\r\n+          for (int x = 0; x < scale; x++) {\r\n+            int pixelX = col * scale + x;\r\n+            int pixelY = row * scale + y;\r\n+            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+            if (index + 3 < data.length) {\r\n+              int r = data[index];\r\n+              int g = data[index + 1];\r\n+              int b = data[index + 2];\r\n+              int brightness = ((r + g + b) / 3).round();\r\n+              if (brightness < 128) darkCount++;\r\n+              totalCount++;\r\n+            }\r\n           }\r\n         }\r\n+        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n-      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+\r\n+    // Apply xOffset to create the scrolling effect\r\n+    List<List<bool>> scrolledMatrix = List.generate(\r\n+        rows, (_) => List.generate(cols, (_) => false));\r\n+    for (int row = 0; row < rows; row++) {\r\n+      for (int col = 0; col < cols; col++) {\r\n+        int scrolledCol = (col + xOffset) % matrix[row].length;\r\n+        scrolledMatrix[row][col] = matrix[row][scrolledCol];\r\n+      }\r\n+    }\r\n+\r\n+    return scrolledMatrix;\r\n   }\r\n-  return matrix;\r\n-}\r\n \r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -312,5 +302,23 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n+}\r\n+\r\n+class MarqueeController {\r\n+  int xOffset = 0;\r\n+  int maxOffset = 0;\r\n+  Timer? timer;\r\n+\r\n+  void startMarquee(int maxOffset, Duration duration) {\r\n+    this.maxOffset = maxOffset;\r\n+    timer = Timer.periodic(duration, (timer) {\r\n+      xOffset = (xOffset + 1) % maxOffset;\r\n+      // Notify listeners or update state here\r\n+    });\r\n+  }\r\n+\r\n+  void stopMarquee() {\r\n+    timer?.cancel();\r\n+  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740050210406,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -27,9 +27,10 @@\n \r\n   Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n     List<String> hexStrings = [];\r\n     for (int x = 0; x < message.length; x++) {\r\n-      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n+      if (message[x] == '<' &&\r\n+          message[min(x + 5, message.length - 1)] == '>') {\r\n         int index = int.parse(message[x + 2] + message[x + 3]);\r\n         var key = controllerData.imageCache.keys.toList()[index];\r\n         if (key is List) {\r\n           String filename = key[0];\r\n@@ -58,159 +59,168 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  Future<List<List<bool>>> renderTextToMatrix(\r\n-    String message,\r\n-    TextStyle textStyle, {\r\n-    int cols = 44,\r\n-    int rows = 11,\r\n-    int scale = 10,\r\n-    int xOffset = 0,\r\n-  }) async {\r\n-    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n-    List<_Segment> segments = [];\r\n-    int lastMatchEnd = 0;\r\n-    for (final match in regex.allMatches(message)) {\r\n-      if (match.start > lastMatchEnd) {\r\n-        segments.add(_Segment(\r\n-            text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n-      }\r\n-      String emojiIndex = match.group(1)!;\r\n-      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n-      lastMatchEnd = match.end;\r\n+  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n+  /// onto an offscreen canvas and convert it to an LED matrix.\r\n+  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n+  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n+Future<List<List<bool>>> renderTextToMatrix(\r\n+  String message,\r\n+  TextStyle textStyle, {\r\n+  int cols = 44,\r\n+  int rows = 11,\r\n+  int scale = 40, // scale factor for better resolution\r\n+}) async {\r\n+  // Parse the message into segments (plain text and emoji)\r\n+  final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n+  List<_Segment> segments = [];\r\n+  int lastMatchEnd = 0;\r\n+  for (final match in regex.allMatches(message)) {\r\n+    if (match.start > lastMatchEnd) {\r\n+      segments.add(_Segment(\r\n+          text: message.substring(lastMatchEnd, match.start), isEmoji: false));\r\n     }\r\n-    if (lastMatchEnd < message.length) {\r\n-      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n-    }\r\n+    String emojiIndex = match.group(1)!;\r\n+    segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n+    lastMatchEnd = match.end;\r\n+  }\r\n+  if (lastMatchEnd < message.length) {\r\n+    segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n+  }\r\n \r\n-    double scaledFontSize = (textStyle.fontSize ?? 16) * scale;\r\n-    int canvasHeight = rows * scale;\r\n-    if (scaledFontSize > canvasHeight * 0.9) {\r\n-      scaledFontSize = canvasHeight * 0.9;\r\n-    }\r\n-    double emojiWidth = scaledFontSize;\r\n-    double emojiGap = scaledFontSize * 0.2;\r\n+  // Determine the scaled font size and set emoji dimensions.\r\n+  double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n+  int canvasHeight = rows * scale;\r\n+  // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n+  if (scaledFontSize > canvasHeight * 0.96) {\r\n+    scaledFontSize = canvasHeight * 0.9;\r\n+  }\r\n+  double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n+  double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n-    double singleCopyWidth = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      if (segments[i].isEmoji) {\r\n-        double widthWithGap = emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          widthWithGap += emojiGap;\r\n-        }\r\n-        singleCopyWidth += widthWithGap;\r\n-      } else {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segments[i].text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        singleCopyWidth += tp.width;\r\n+  // Measure one copy of the message (in scaled pixels)\r\n+  double singleCopyWidth = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    if (segments[i].isEmoji) {\r\n+      double widthWithGap = emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        widthWithGap += emojiGap;\r\n       }\r\n+      singleCopyWidth += widthWithGap;\r\n+    } else {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segments[i].text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      singleCopyWidth += tp.width;\r\n     }\r\n+  }\r\n \r\n-    int fixedWidth = cols * scale;\r\n-    int gapWidth = fixedWidth;\r\n-    int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  // fixedWidth is the display width (in scaled pixels)\r\n+  int fixedWidth = cols * scale;\r\n+  // For marquee effect, set gap equal to fixedWidth (so the message fully scrolls off before repeating)\r\n+  int gapWidth = fixedWidth;\r\n+  // Canvas width is one copy of the message plus the gap\r\n+  int canvasWidth = singleCopyWidth.ceil() + gapWidth;\r\n+  \r\n+  double centerY = canvasHeight / 2;\r\n \r\n-    double centerY = canvasHeight / 2;\r\n+  final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n+  final Canvas canvas = Canvas(\r\n+      recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n \r\n-    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n-    final Canvas canvas = Canvas(\r\n-        recorder, Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()));\r\n+  // Fill the background with white\r\n+  final Paint bgPaint = Paint()..color = Colors.white;\r\n+  canvas.drawRect(\r\n+      Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n+      bgPaint);\r\n \r\n-    final Paint bgPaint = Paint()..color = Colors.white;\r\n-    canvas.drawRect(\r\n-        Rect.fromLTWH(0, 0, canvasWidth.toDouble(), canvasHeight.toDouble()),\r\n-        bgPaint);\r\n-\r\n-    double currentX = 0;\r\n-    for (int i = 0; i < segments.length; i++) {\r\n-      final segment = segments[i];\r\n-      if (!segment.isEmoji) {\r\n-        final TextPainter tp = TextPainter(\r\n-          text: TextSpan(\r\n-              text: segment.text,\r\n-              style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n-          textDirection: TextDirection.ltr,\r\n-        );\r\n-        tp.layout();\r\n-        double offsetY = centerY - (tp.height - tp.computeLineMetrics().first.descent) / 2;\r\n-        tp.paint(canvas, Offset(currentX, offsetY));\r\n-        currentX += tp.width;\r\n-      } else {\r\n-        int index = int.parse(segment.text);\r\n-        List keys = controllerData.imageCache.keys.toList();\r\n-        if (index < keys.length) {\r\n-          var key = keys[index];\r\n-          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n-          if (emojiBytes != null) {\r\n-            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n-                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n-            ui.FrameInfo fi = await codec.getNextFrame();\r\n-            ui.Image emojiImage = fi.image;\r\n-            double offsetY = centerY - emojiWidth / 2;\r\n-            Paint imagePaint = Paint();\r\n-            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n-          }\r\n+  // Draw one copy of the message starting at x=0\r\n+  double currentX = 0;\r\n+  for (int i = 0; i < segments.length; i++) {\r\n+    final segment = segments[i];\r\n+    if (!segment.isEmoji) {\r\n+      final TextPainter tp = TextPainter(\r\n+        text: TextSpan(\r\n+            text: segment.text,\r\n+            style: textStyle.copyWith(fontSize: scaledFontSize, color: Colors.black)),\r\n+        textDirection: TextDirection.ltr,\r\n+      );\r\n+      tp.layout();\r\n+      // Compute descent so that letters with descenders are fully visible.\r\n+      var metrics = tp.computeLineMetrics();\r\n+      double descent = metrics.isNotEmpty ? metrics.first.descent : 0;\r\n+      // Offset the text so that the bottom of the glyphs is centered.\r\n+      double offsetY = centerY - (tp.height - descent) / 2;\r\n+      tp.paint(canvas, Offset(currentX, offsetY));\r\n+      currentX += tp.width;\r\n+    } else {\r\n+      int index = int.parse(segment.text);\r\n+      List keys = controllerData.imageCache.keys.toList();\r\n+      if (index < keys.length) {\r\n+        var key = keys[index];\r\n+        Uint8List? emojiBytes = controllerData.imageCache[key];\r\n+        if (emojiBytes != null) {\r\n+          ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n+              targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n+          ui.FrameInfo fi = await codec.getNextFrame();\r\n+          ui.Image emojiImage = fi.image;\r\n+          double offsetY = centerY - emojiWidth / 2;\r\n+          Paint imagePaint = Paint();\r\n+          canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n         }\r\n-        currentX += emojiWidth;\r\n-        if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n-          currentX += emojiGap;\r\n-        }\r\n       }\r\n+      currentX += emojiWidth;\r\n+      if (i < segments.length - 1 && segments[i + 1].isEmoji) {\r\n+        currentX += emojiGap;\r\n+      }\r\n     }\r\n+  }\r\n+  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n+  \r\n+  // Finalize the drawing and convert the result to an image.\r\n+  final ui.Picture picture = recorder.endRecording();\r\n+  final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n+  final ByteData? byteData =\r\n+      await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n+  if (byteData == null) {\r\n+    throw Exception(\"Failed to convert image to byte data.\");\r\n+  }\r\n+  final Uint8List data = byteData.buffer.asUint8List();\r\n \r\n-    final ui.Picture picture = recorder.endRecording();\r\n-    final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n-    final ByteData? byteData =\r\n-        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n-    if (byteData == null) {\r\n-      throw Exception(\"Failed to convert image to byte data.\");\r\n-    }\r\n-    final Uint8List data = byteData.buffer.asUint8List();\r\n-\r\n-    List<List<bool>> matrix = List.generate(\r\n-        rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n-        int darkCount = 0;\r\n-        int totalCount = 0;\r\n-        for (int y = 0; y < scale; y++) {\r\n-          for (int x = 0; x < scale; x++) {\r\n-            int pixelX = col * scale + x;\r\n-            int pixelY = row * scale + y;\r\n-            int index = (pixelY * canvasWidth + pixelX) * 4;\r\n-            if (index + 3 < data.length) {\r\n-              int r = data[index];\r\n-              int g = data[index + 1];\r\n-              int b = data[index + 2];\r\n-              int brightness = ((r + g + b) / 3).round();\r\n-              if (brightness < 128) darkCount++;\r\n-              totalCount++;\r\n-            }\r\n+  // Downsample: For each cell (scale x scale block), count dark pixels.\r\n+  List<List<bool>> matrix = List.generate(\r\n+      rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n+  for (int row = 0; row < rows; row++) {\r\n+    for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n+      int darkCount = 0;\r\n+      int totalCount = 0;\r\n+      for (int y = 0; y < scale; y++) {\r\n+        for (int x = 0; x < scale; x++) {\r\n+          int pixelX = col * scale + x;\r\n+          int pixelY = row * scale + y;\r\n+          int index = (pixelY * canvasWidth + pixelX) * 4;\r\n+          if (index + 3 < data.length) {\r\n+            int r = data[index];\r\n+            int g = data[index + 1];\r\n+            int b = data[index + 2];\r\n+            int brightness = ((r + g + b) / 3).round();\r\n+            if (brightness < 128) darkCount++;\r\n+            totalCount++;\r\n           }\r\n         }\r\n-        matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n       }\r\n+      matrix[row][col] = darkCount > (scale * scale * 0.2);\r\n     }\r\n+  }\r\n+  return matrix;\r\n+}\r\n \r\n-    // Apply xOffset to create the scrolling effect\r\n-    List<List<bool>> scrolledMatrix = List.generate(\r\n-        rows, (_) => List.generate(cols, (_) => false));\r\n-    for (int row = 0; row < rows; row++) {\r\n-      for (int col = 0; col < cols; col++) {\r\n-        int scrolledCol = (col + xOffset) % matrix[row].length;\r\n-        scrolledMatrix[row][col] = matrix[row][scrolledCol];\r\n-      }\r\n-    }\r\n \r\n-    return scrolledMatrix;\r\n-  }\r\n-\r\n   static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n     int height = image.length;\r\n     int width = image.isNotEmpty ? image[0].length : 0;\r\n     int finalSum = 0;\r\n@@ -302,23 +312,5 @@\n       hexArray[i].add(1);\r\n     }\r\n     return convertBitmapToLEDHex(hexArray, true);\r\n   }\r\n-}\r\n-\r\n-class MarqueeController {\r\n-  int xOffset = 0;\r\n-  int maxOffset = 0;\r\n-  Timer? timer;\r\n-\r\n-  void startMarquee(int maxOffset, Duration duration) {\r\n-    this.maxOffset = maxOffset;\r\n-    timer = Timer.periodic(duration, (timer) {\r\n-      xOffset = (xOffset + 1) % maxOffset;\r\n-      // Notify listeners or update state here\r\n-    });\r\n-  }\r\n-\r\n-  void stopMarquee() {\r\n-    timer?.cancel();\r\n-  }\r\n }\n\\ No newline at end of file\n"
                },
                {
                    "date": 1740050387443,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -1,8 +1,7 @@\n import 'dart:math';\r\n import 'dart:typed_data';\r\n import 'dart:ui' as ui;\r\n-import 'package:badgemagic/bademagic_module/utils/badge_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\n import 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\n import 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\n import 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\n@@ -92,9 +91,9 @@\n   double scaledFontSize = (textStyle.fontSize ?? 30) * scale;\r\n   int canvasHeight = rows * scale;\r\n   // Ensure the text fits vertically within the canvas (leave a tiny margin)\r\n   if (scaledFontSize > canvasHeight * 0.96) {\r\n-    scaledFontSize = canvasHeight * 0.9;\r\n+    scaledFontSize = canvasHeight * 0.96;\r\n   }\r\n   double emojiWidth = scaledFontSize; // emojis are square, matching text height\r\n   double emojiGap = scaledFontSize * 0.2; // gap between consecutive emojis\r\n \r\n"
                },
                {
                    "date": 1740050394954,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -195,9 +195,8 @@\n       rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n   for (int row = 0; row < rows; row++) {\r\n     for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n       int darkCount = 0;\r\n-      int totalCount = 0;\r\n       for (int y = 0; y < scale; y++) {\r\n         for (int x = 0; x < scale; x++) {\r\n           int pixelX = col * scale + x;\r\n           int pixelY = row * scale + y;\r\n"
                },
                {
                    "date": 1740050452244,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -177,9 +177,8 @@\n         currentX += emojiGap;\r\n       }\r\n     }\r\n   }\r\n-  // Leave the rest of the canvas blank (the gap) so that the next copy doesn't overlap.\r\n   \r\n   // Finalize the drawing and convert the result to an image.\r\n   final ui.Picture picture = recorder.endRecording();\r\n   final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n@@ -195,8 +194,9 @@\n       rows, (_) => List.generate((canvasWidth / scale).ceil(), (_) => false));\r\n   for (int row = 0; row < rows; row++) {\r\n     for (int col = 0; col < (canvasWidth / scale).ceil(); col++) {\r\n       int darkCount = 0;\r\n+      int totalCount = 0;\r\n       for (int y = 0; y < scale; y++) {\r\n         for (int x = 0; x < scale; x++) {\r\n           int pixelX = col * scale + x;\r\n           int pixelY = row * scale + y;\r\n"
                },
                {
                    "date": 1740050474930,
                    "content": "Index: \n===================================================================\n--- \n+++ \n@@ -58,12 +58,8 @@\n     logger.d(\"Hex strings: $hexStrings\");\r\n     return hexStrings;\r\n   }\r\n \r\n-  /// Updated: Render the given text (which may include emoji placeholders) using the provided [textStyle]\r\n-  /// onto an offscreen canvas and convert it to an LED matrix.\r\n-  /// The canvas width is set to the total rendered message width plus extra (display width),\r\n-  /// and the message is repeated (with a small gap between copies) so that there is no empty padding at the borders.\r\n Future<List<List<bool>>> renderTextToMatrix(\r\n   String message,\r\n   TextStyle textStyle, {\r\n   int cols = 44,\r\n@@ -177,10 +173,8 @@\n         currentX += emojiGap;\r\n       }\r\n     }\r\n   }\r\n-  \r\n-  // Finalize the drawing and convert the result to an image.\r\n   final ui.Picture picture = recorder.endRecording();\r\n   final ui.Image image = await picture.toImage(canvasWidth, canvasHeight);\r\n   final ByteData? byteData =\r\n       await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n"
                }
            ],
            "date": 1739986755872,
            "name": "Commit-0",
            "content": "import 'dart:math';\r\nimport 'dart:typed_data';\r\nimport 'dart:ui' as ui;\r\nimport 'package:badgemagic/bademagic_module/utils/byte_array_utils.dart';\r\nimport 'package:badgemagic/bademagic_module/utils/data_to_bytearray_converter.dart';\r\nimport 'package:badgemagic/bademagic_module/utils/file_helper.dart';\r\nimport 'package:badgemagic/bademagic_module/utils/image_utils.dart';\r\nimport 'package:badgemagic/providers/imageprovider.dart';\r\nimport 'package:flutter/material.dart';\r\nimport 'package:get_it/get_it.dart';\r\n\r\nclass Converters {\r\n  InlineImageProvider controllerData =\r\n      GetIt.instance.get<InlineImageProvider>();\r\n  DataToByteArrayConverter converter = DataToByteArrayConverter();\r\n  ImageUtils imageUtils = ImageUtils();\r\n  FileHelper fileHelper = FileHelper();\r\n\r\n  int controllerLength = 0;\r\n\r\n  Future<List<String>> messageTohex(String message, bool isInverted) async {\r\n    List<String> hexStrings = [];\r\n    for (int x = 0; x < message.length; x++) {\r\n      if (message[x] == '<' && message[min(x + 5, message.length - 1)] == '>') {\r\n        int index = int.parse(message[x + 2] + message[x + 3]);\r\n        var key = controllerData.imageCache.keys.toList()[index];\r\n        if (key is List) {\r\n          String filename = key[0];\r\n          List<dynamic>? decodedData = await fileHelper.readFromFile(filename);\r\n          final List<List>? image = decodedData?.cast<List<dynamic>>();\r\n          List<List<int>> imageData =\r\n              image!.map((list) => list.cast<int>()).toList();\r\n          hexStrings += convertBitmapToLEDHex(imageData, true);\r\n          x += 5;\r\n        } else {\r\n          List<String> hs =\r\n              await imageUtils.generateLedHex(controllerData.vectors[index]);\r\n          hexStrings.addAll(hs);\r\n          x += 5;\r\n        }\r\n      } else {\r\n        if (converter.charCodes.containsKey(message[x])) {\r\n          hexStrings.add(converter.charCodes[message[x]]!);\r\n        }\r\n      }\r\n    }\r\n    if (isInverted) {\r\n      hexStrings = invertHex(hexStrings.join()).split('');\r\n      hexStrings = padHexString(hexStrings);\r\n    }\r\n    logger.d(\"Hex strings: $hexStrings\");\r\n    return hexStrings;\r\n  }\r\n\r\n  /// Updated: Render the given text using the provided [textStyle] onto an offscreen\r\n  /// canvas and convert it to an LED matrix (11 rows x 44 columns).\r\n Future<List<List<bool>>> renderTextToMatrix(\r\n    String message,\r\n    TextStyle textStyle, {\r\n    int cols = 44,\r\n    int rows = 11,\r\n    int scale = 10, // scale factor for better resolution\r\n  }) async {\r\n    // Parse the message for emoji placeholders\r\n    final RegExp regex = RegExp(r'<<(\\d{2})>>');\r\n    List<_Segment> segments = [];\r\n    int lastMatchEnd = 0;\r\n    for (final match in regex.allMatches(message)) {\r\n      if (match.start > lastMatchEnd) {\r\n        segments.add(_Segment(\r\n          text: message.substring(lastMatchEnd, match.start), \r\n          isEmoji: false\r\n        ));\r\n      }\r\n      String emojiIndex = match.group(1)!;\r\n      segments.add(_Segment(text: emojiIndex, isEmoji: true));\r\n      lastMatchEnd = match.end;\r\n    }\r\n    if (lastMatchEnd < message.length) {\r\n      segments.add(_Segment(text: message.substring(lastMatchEnd), isEmoji: false));\r\n    }\r\n\r\n    // Calculate canvas size\r\n    final int width = cols * scale;\r\n    final int height = rows * scale;\r\n\r\n    // Create a PictureRecorder and Canvas\r\n    final ui.PictureRecorder recorder = ui.PictureRecorder();\r\n    final Canvas canvas = Canvas(\r\n        recorder, Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()));\r\n\r\n    // Fill background with white\r\n    final Paint bgPaint = Paint()..color = Colors.white;\r\n    canvas.drawRect(\r\n        Rect.fromLTWH(0, 0, width.toDouble(), height.toDouble()), bgPaint);\r\n\r\n    double currentX = 0;\r\n    double centerY = height / 2.0;\r\n    double fontSize = (textStyle.fontSize ?? 16) * scale;\r\n    double emojiWidth = fontSize*0.734; \r\n\r\n    for (var segment in segments) {\r\n      if (!segment.isEmoji) {\r\n        // Paint text\r\n        final TextPainter textPainter = TextPainter(\r\n          text: TextSpan(\r\n            text: segment.text,\r\n            style: textStyle.copyWith(\r\n                color: Colors.black, fontSize: fontSize),\r\n          ),\r\n          textDirection: TextDirection.ltr,\r\n        );\r\n        textPainter.layout(maxWidth: width.toDouble() - currentX);\r\n        double offsetY = 0 - textPainter.height / 3.8;\r\n        textPainter.paint(canvas, Offset(currentX, offsetY));\r\n        currentX += textPainter.width;\r\n      } else {\r\n        // Paint emoji\r\n        int index = int.parse(segment.text);\r\n        List keys = controllerData.imageCache.keys.toList();\r\n        if (index < keys.length) {\r\n          var key = keys[index];\r\n          Uint8List? emojiBytes = controllerData.imageCache[key];\r\n          if (emojiBytes != null) {\r\n            ui.Codec codec = await ui.instantiateImageCodec(emojiBytes,\r\n                targetWidth: emojiWidth.toInt(), targetHeight: emojiWidth.toInt());\r\n            ui.FrameInfo fi = await codec.getNextFrame();\r\n            ui.Image emojiImage = fi.image;\r\n            double offsetY = centerY - emojiWidth / 2;\r\n            Paint imagePaint = Paint();\r\n            canvas.drawImage(emojiImage, Offset(currentX, offsetY), imagePaint);\r\n            currentX += emojiWidth;\r\n          }\r\n        }\r\n      }\r\n    }\r\n\r\n    // Convert canvas to image for downsampling\r\n    final ui.Picture picture = recorder.endRecording();\r\n    final ui.Image image = await picture.toImage(width, height);\r\n    final ByteData? byteData =\r\n        await image.toByteData(format: ui.ImageByteFormat.rawRgba);\r\n\r\n    if (byteData == null) {\r\n      throw Exception(\"Failed to convert image to byte data.\");\r\n    }\r\n    final Uint8List data = byteData.buffer.asUint8List();\r\n\r\n    // Downsample to LED matrix\r\n    List<List<bool>> matrix =\r\n        List.generate(rows, (_) => List.generate(cols, (_) => false));\r\n    for (int row = 0; row < rows; row++) {\r\n      for (int col = 0; col < cols; col++) {\r\n        int sum = 0;\r\n        int count = 0;\r\n        for (int y = 0; y < scale; y++) {\r\n          for (int x = 0; x < scale; x++) {\r\n            int pixelX = col * scale + x;\r\n            int pixelY = row * scale + y;\r\n            int index =\r\n                (pixelY * width + pixelX) * 4; // 4 bytes per pixel (RGBA)\r\n            if (index + 3 < data.length) {\r\n              int r = data[index], g = data[index + 1], b = data[index + 2];\r\n              int brightness = ((r + g + b) / 3).round();\r\n              sum += brightness;\r\n              count++;\r\n            }\r\n          }\r\n        }\r\n        double avgBrightness = sum / count;\r\n        matrix[row][col] = avgBrightness < 128;\r\n      }\r\n    }\r\n    return matrix;\r\n  }\r\n\r\n  static List<String> convertBitmapToLEDHex(List<List<int>> image, bool trim) {\r\n    int height = image.length;\r\n    int width = image.isNotEmpty ? image[0].length : 0;\r\n    int finalSum = 0;\r\n    for (int j = 0; j < width; j++) {\r\n      int sum = 0;\r\n      for (int i = 0; i < height; i++) {\r\n        sum += image[i][j];\r\n      }\r\n      if (sum == 0 && trim) {\r\n        for (int i = 0; i < height; i++) {\r\n          image[i][j] = -1;\r\n        }\r\n      } else {\r\n        finalSum += j;\r\n        break;\r\n      }\r\n    }\r\n    for (int j = width - 1; j >= 0; j--) {\r\n      int sum = 0;\r\n      for (int i = 0; i < height; i++) {\r\n        sum += image[i][j];\r\n      }\r\n      if (sum == 0 && trim) {\r\n        for (int i = 0; i < height; i++) {\r\n          image[i][j] = -1;\r\n        }\r\n      } else {\r\n        finalSum += (height - j - 1);\r\n        break;\r\n      }\r\n    }\r\n    int diff = 0;\r\n    if ((height - finalSum) % 8 > 0) {\r\n      diff = 8 - (height - finalSum) % 8;\r\n    }\r\n    int rOff = (diff / 2).floor();\r\n    int lOff = (diff / 2).ceil();\r\n    List<List<int>> list =\r\n        List.generate(height, (i) => List.filled(width + rOff + lOff, 0));\r\n    for (int i = 0; i < height; i++) {\r\n      int k = 0;\r\n      for (int j = 0; j < rOff; j++) {\r\n        list[i][k++] = 0;\r\n      }\r\n      for (int j = 0; j < width; j++) {\r\n        if (image[i][j] != -1) {\r\n          list[i][k++] = image[i][j];\r\n        }\r\n      }\r\n      for (int j = 0; j < lOff; j++) {\r\n        list[i][k++] = 0;\r\n      }\r\n    }\r\n    logger.d(\"Padded image: $list\");\r\n    List<String> allHexs = [];\r\n    for (int i = 0; i < list[0].length ~/ 8; i++) {\r\n      StringBuffer lineHex = StringBuffer();\r\n      for (int k = 0; k < height; k++) {\r\n        StringBuffer stBuilder = StringBuffer();\r\n        for (int j = i * 8; j < i * 8 + 8; j++) {\r\n          stBuilder.write(list[k][j]);\r\n        }\r\n        String hex = int.parse(stBuilder.toString(), radix: 2)\r\n            .toRadixString(16)\r\n            .padLeft(2, '0');\r\n        lineHex.write(hex);\r\n      }\r\n      allHexs.add(lineHex.toString());\r\n    }\r\n    return allHexs;\r\n  }\r\n\r\n  static String invertHex(String hex) {\r\n    StringBuffer invertedHex = StringBuffer();\r\n    for (int i = 0; i < hex.length; i++) {\r\n      String invertedHexDigit =\r\n          (~int.parse(hex[i], radix: 16) & 0xF).toRadixString(16).toUpperCase();\r\n      invertedHex.write(invertedHexDigit);\r\n    }\r\n    return invertedHex.toString();\r\n  }\r\n\r\n  List<String> padHexString(List<String> hexString) {\r\n    List<List<int>> hexArray = hexStringToBool(hexString.join()).map((e) {\r\n      return e.map((e) => e ? 1 : 0).toList();\r\n    }).toList();\r\n    for (int i = 0; i < hexArray.length; i++) {\r\n      hexArray[i].insert(0, 1);\r\n      hexArray[i].add(1);\r\n    }\r\n    return convertBitmapToLEDHex(hexArray, true);\r\n  }\r\n}\r\nclass _Segment {\r\n  final String text;\r\n  final bool isEmoji;\r\n  _Segment({required this.text, required this.isEmoji});\r\n}"
        }
    ]
}